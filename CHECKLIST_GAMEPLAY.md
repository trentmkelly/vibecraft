# Gameplay Mechanics Checklist

## Relevant Java Source Files

- `decompiled-server-26.1.2/net/minecraft/world/level/block/` — block interaction logic
- `decompiled-server-26.1.2/net/minecraft/server/level/ServerPlayerGameMode.java` — block breaking, placing
- `decompiled-server-26.1.2/net/minecraft/world/item/context/UseOnContext.java` — item use context
- `decompiled-server-26.1.2/net/minecraft/world/effect/MobEffect.java` — status effect base
- `decompiled-server-26.1.2/net/minecraft/world/effect/MobEffects.java` — effect registry
- `decompiled-server-26.1.2/net/minecraft/world/effect/MobEffectInstance.java` — applied effect instance
- `decompiled-server-26.1.2/net/minecraft/world/damagesource/DamageSource.java` — damage source
- `decompiled-server-26.1.2/net/minecraft/world/damagesource/CombatTracker.java` — death message tracking
- `decompiled-server-26.1.2/net/minecraft/world/entity/LivingEntity.java` — damage/combat/effects
- `decompiled-server-26.1.2/net/minecraft/world/level/levelgen/WeatherType.java` — weather state
- `decompiled-server-26.1.2/net/minecraft/server/level/ServerLevel.java` — level tick (weather, time, sleep)
- `decompiled-server-26.1.2/net/minecraft/world/level/border/WorldBorder.java` — world border
- `decompiled-server-26.1.2/net/minecraft/world/level/border/WorldBorderPhase.java` — border lerp
- `decompiled-server-26.1.2/net/minecraft/world/entity/player/SleepStatus.java` — sleep counting
- `decompiled-server-26.1.2/net/minecraft/world/phys/BlockHitResult.java` — block hit context
- `RustCraft/src/combat_damage.rs` — RustCraft combat
- `RustCraft/src/status_effect.rs` — RustCraft status effects
- `RustCraft/src/weather.rs` — RustCraft weather
- `RustCraft/src/world_border.rs` — RustCraft world border
- `RustCraft/src/world_time.rs` — RustCraft time
- `RustCraft/src/block_behavior.rs` — RustCraft block behavior

## Block Placement and Breaking

- [x] Add Mineflayer block place/break tests for offline-mode survival and creative bots: reach distance enforcement (5.0 survival, creative extends same server-side), denied placement at spawn-protection, placement orientation by face hit, drops from breaking, visible block-update acknowledgment packets — `harness/mineflayer/block_interactions.{mjs,test.mjs}` (9 tests) covers dig/place/use/denied/update steps. Added a verified reach oracle `isWithinBlockReach`/`blockAabbDistanceSq` mirroring `Player.canInteractWithBlock` (AABB-distanceSq < (blockInteractionRange 4.5 + buffer 1.0)²; creative uses the same server-side block range — the "5.0" was outdated). Fixed `assertSpawnProtection` to Chebyshev `max(|dx|,|dz|)` with op-list bypass, matching `DedicatedServer.isUnderSpawnProtection`.
- [x] Add Mineflayer block interaction parity tests: right-click use, sneak-use bypass (sneaking + use item), client prediction rollback (server rejects placement → sends corrective update), block entity update tags sent after right-click, neighbor-shape updates after offline-mode placement — `harness/mineflayer/block_interaction_parity.{mjs,test.mjs}` (7 tests) defines all five steps (`right-click-block-use`, `sneak-use-bypass`, `client-prediction-rollback`, `block-entity-tag-after-use`, `neighbor-shape-update-after-placement`) and the summarize/assertion surface.
- [x] Add Mineflayer offline-mode block-entity interaction tests: signs (edit/wax/dye), chests (open/loot), furnaces (insert/extract), lecterns (page turn), bells (ring broadcast), note blocks (pitch), campfires (cook), cauldrons (fill/empty), spawners (spawn egg insert), brushable blocks (brush progress) — `harness/mineflayer/block_entity_interactions.{mjs,test.mjs}` (8 tests) covers all listed block entities plus vault/trial-spawner/calibrated-sculk/chiseled-bookshelf. Verified encoded constants against Java: single-chest `expectedSlotCount` 27, bell `expectedBroadcastRadius` 32 (`BellBlockEntity.closerToCenterThan(..., 32.0)`).
- [x] Add Mineflayer offline-mode block-drop tests: break representative blocks with bare hand (no drop), correct tool (drop), Silk Touch (block itself drops), Fortune level 1/2/3 (bonus count), explosion (0% per-block survival), `doTileDrops=false` gamerule (no drops); compare inventory pickups and world item entities against official `server.jar` — `harness/mineflayer/block_drops.{mjs,test.mjs}` (6 tests) defines all eight drop steps (bare-hand, correct-tool, silk-touch, fortune 1/2/3, explosion, doTileDrops) with `expectDrops`/`expectFortuneDrops`/`expectNoDrops` oracles and summarize coverage.
- [x] Implement `ServerPlayerGameMode.handleBlockBreakAction()`: start-dig / abort-dig / stop-dig sequence, break progress calculation per tool efficiency and haste/mining-fatigue, instant-break in creative, adventure-mode CanDestroy tag restriction — `player_game_mode::handle_block_break_action` is 1:1 with `handleBlockBreakAction`: the too-far / too-high guards, then per-action START (spawn protection → may-interact → creative insta-break → block-action-restricted → progress with the `!air && progress>=1` insta-mine guard and the air→"starts destroying at state 10" edge case), STOP (`pos==destroyPos`, `getDestroyProgress*(ticks+1) >= 0.7` destroy, else delayed-destroy latch), and ABORT. `player_tool_speed` now mirrors 26.1.2 `Player.getDestroySpeed` exactly — mining-efficiency add (speed>1), haste `×(1+(amp+1)·0.2)`, mining-fatigue switch, `× block_break_speed` attribute, `× submerged_mining_speed` when eye-in-water (default 0.2; Aqua Affinity → 1.0), `/5` airborne — and `compute_destroy_progress` matches `getDestroyProgress` (`speed/hardness/(30|100)`). Covered by the `start_destroy_*`/`stop_destroy_*`/`abort_destroy_*` and `player_tool_speed_*` tests.
- [x] Implement server-side block-placement validation: block-reach distance check, `canSurvive()` check on target position, entity collision check (cannot place inside entities), spawn-protection check — `block_behavior::validate_placement` consolidates the checks vanilla spreads across `ServerGamePacketListenerImpl.handleUseItemOn` / `ServerPlayerGameMode.useItemOn` / `BlockItem.place`, now 1:1: spectators denied; reach via `isWithinBlockInteractionRange(pos, 1.0)` (`block_aabb_distance_sq(eye, AABB(pos)) < (block_interaction_range + 1.0)²`, matching `AABB.distanceToSqr` closest-point geometry, default range 4.5 survival / 5.0 creative); build-height limits (`pos.y > maxY` / `< minY`); spawn protection; then `place()` order — replaceable (`canPlace`), `canSurvive`, entity collision (`isUnobstructed`). Verified against `Player.isWithinBlockInteractionRange`, `handleUseItemOn`, and `BlockItem.canPlace`; covered by `placement_validation_enforces_all_deny_conditions` (reach edge at exactly 5.5, build limits, every deny reason).
- [x] Implement `Block.use()` / `Block.attack()` dispatch: route right-click to block use handler first, then item use handler, respecting `InteractionResult` (SUCCESS/CONSUME/FAIL/PASS) — `block_behavior::dispatch_block_use` is now 1:1 with `ServerPlayerGameMode.useItemOn`: `suppressUsingBlock = isSecondaryUseActive() && haveSomethingInOurHands`; when not suppressed, `block.useItemOn` returns early only if it `consumesAction()`, else a `TryWithEmptyHand` on the main hand triggers `block.useWithoutItem`; then `item.useOn` runs when the held item is non-empty and not on cooldown; else `PASS`. The `InteractionResult` enum now matches the 26.1.2 sealed interface (the three `Success` records — `SUCCESS`/`SUCCESS_SERVER`/`CONSUME` — all `consumesAction`, while `FAIL`/`PASS`/`TryWithEmptyHand` do not; this fixed the prior `consumes_action` that wrongly excluded `Success` and the old "return on any non-Pass" routing that swallowed `FAIL`). `dispatch_block_attack` covers `blockState.attack` (note block). Verified against `useItemOn`/`InteractionResult.java`; covered by `block_use_dispatch_routes_block_first_then_item_respecting_pass` and `interaction_result_predicates_match_vanilla_groupings`.
- [x] Implement neighbor notification cascade: when a block changes, call `updateIndirectNeighbourShapes()` and `onNeighborChanged()` for all 6 adjacent blocks and their adjacent blocks (shape-update chain) — `block_update::NeighborUpdateQueue::update_shape_cascade()` now emits first-layer neighbor notifications plus the second-layer shape-update chain around each neighbor, respecting the configured chained-update limit; covered by `cargo test -q block_update`.
- [x] Add parity test: break-speed calculation for diamond pickaxe on stone vs. dirt vs. obsidian matches vanilla ticks — `break_speed_parity_diamond_pickaxe_on_stone_dirt_obsidian` asserts vanilla tick counts via `compute_destroy_progress`/`ticks_to_break`: stone (hardness 1.5, speed 8) → 6 ticks, dirt (0.5, hand speed 1) → 15 ticks, obsidian (50, speed 8) → 188 ticks. Formula `ceil(1 / (speed/hardness/30))` is 1:1 with `BlockBehaviour.getDestroyProgress`.
- [x] Add parity test: neighbor update cascade when placing/breaking redstone wire propagates signal changes to all affected comparators and repeaters — `vanilla_trace_redstone_wire_neighbor_cascade_reaches_comparators_and_repeaters` verifies both redstone-wire placement and break cascade through second-layer neighbor notifications to comparator and repeater positions; covered by `cargo test -q redstone_wire_neighbor_cascade`.

## Combat and Damage

- [x] Add Mineflayer combat/damage tests: melee attack (hit animation, damage value, knockback), projectile (arrow damage falloff with range), fall damage (formula: `max(0, height - 3) * 1.5`), fire damage (1/tick), drowning damage (2/tick when air = 0), void damage (4/tick below world bottom), shield blocking (negate projectile + reduce melee), armor mitigation (damage factor formula), invulnerability frames (0.5 s), vanilla-compatible damage/death messages — `harness/mineflayer/combat_damage_scenarios.{mjs,test.mjs}` (8 tests) covers all 10 steps. Verified/fixed oracles against Java 26.1.2: `computeExpectedFallDamage` is now `floor((dist+1e-6-3)*mult)` (`LivingEntity.calculateFallPower`, not the outdated `ceil(dist-3)*1.5`); `computeArmorMitigation` is 1:1 with `CombatRules.getDamageAfterAbsorb`; death-message keys verified (e.g. void → `death.attack.outOfWorld` from DamageType `"outOfWorld"`).
- [x] Implement `LivingEntity.hurt()`: invulnerability frame check, absorb through absorption attribute, armor/enchantment/effect protection calculation, knockback application, death check — `LivingEntityState::hurt()` now applies cooldown delta rules, armor plus enchantment/effect protection, absorption, hurt/death timers, and death animation; `hurt_with_knockback()` applies knockback only when damage lands; covered by `cargo test -q living_entity`.
- [x] Implement armor protection formula: `toughness = 2 + armorToughness / 4; effectiveArmor = clamp(armor - damage / toughness, armor * 0.2, 20); result = damage * (1 - effectiveArmor / 25)` (vanilla `CombatRules.getDamageAfterAbsorb`) — `combat_damage::damage_after_armor` matches Java, `damage_after_magic_absorb` matches `getDamageAfterMagicAbsorb`; verified by reading Java `CombatRules.java` and comparing numeric outputs for iron/diamond armor
- [x] Implement `CombatTracker`: track last-damage source and killer for death message generation, `getDeathMessage()` component building using `DamageType.deathMessageType()` — `combat_tracker.rs` is a 1:1 port of `CombatTracker`/`CombatEntry`/`FallLocation`: combat timing (`recordDamage`/`recheckStatus`/`getCombatDuration` with the 100/300-tick reset windows and `shouldEnterCombat`), `getMostSignificantFall()`, `getFallMessage()` (assist/finish/killer/fall-location variants), and `getDeathMessage()` dispatching the `Default`/`FallVariants`/`IntentionalGameDesign` branches. `getLocalizedDeathMessage` reproduces the three entity-attribution states (no-entity, kill-credit `.player`, attacker, attacker `.item`) as chat `Component`s off the authoritative `damage_type::DamageSource` model; verified against `CombatTracker.java`/`DamageSource.java`.
- [x] Implement shield blocking: `LivingEntity.isBlocking()` check, blocking reduces projectile damage to 0 and melee to 0 if within blocking arc, 5-tick cooldown after strong hit — reworked to the authoritative 26.1.2 data-driven mechanic: `combat_damage::BlocksAttacks` is a 1:1 port of `DataComponents.BLOCKS_ATTACKS` (angle-gated `DamageReduction` list with `base`/`factor`, `ItemDamageFunction`, `block_delay_ticks`, `disable_blocking_for_ticks`, and `bypassed_by`). `apply_item_blocking` mirrors `LivingEntity.applyItemBlocking` (bypass-tag and piercing-arrow short-circuits, `resolveBlockedDamage`), and `blocking_angle` reproduces the `acos(viewVector · toSource)` geometry. `BlocksAttacks::vanilla_shield()` matches `Items.SHIELD` (0.25s delay → 5 ticks, 90° arc, factor 1.0 full block, item damage `(3,1,1)`, `#bypasses_shield`). Verified against `BlocksAttacks.java`/`LivingEntity.java`/`Items.java` and the `bypasses_shield`/`bypasses_armor` tags; covered by `vanilla_shield_blocks_attacks_component_matches_java`, `blocking_angle_matches_view_vector_geometry`, and `damage_reduction_partial_factor_and_type_filter`. A `TODO` in `combat_damage.rs` tracks wiring it into the live hurt path once item-component decode and active-use-item tracking land (the hurt path itself is still test-only).
- [x] Implement sweeping attack: when sprinting melee with sword, deal sweeping damage to entities within radius using `EnchantmentHelper.getSweepingDamageRatio()` — `combat_damage::plan_player_attack()` gates sweeping to full-strength grounded non-sprinting sweep weapons and computes `sweeping_damage` through `sweeping_damage_ratio()`; covered by `attack_plan_covers_knockback_critical_sweeping_and_thorns`.
- [x] Implement critical hit: in the air, not blind, not sprinting → 1.5× base damage multiplier, star particles — `combat_damage::plan_player_attack()` applies the full-strength airborne/non-blind/non-water critical gate, multiplies damage by 1.5, and emits `minecraft:crit`; covered by `attack_plan_covers_knockback_critical_sweeping_and_thorns`.
- [x] Add parity test: death messages for fall, fire, drowning, suffocation, void, mob attack, player attack, arrow, fireball, TNT match vanilla localization keys — `combat_tracker::tests::death_message_keys_match_vanilla_localization_for_all_sources` asserts keys+args for fire (`inFire`/`onFire`), drowning (`drown`), suffocation (`inWall`), void (`outOfWorld`), mob/player attacks (including `.item`), arrow (shot and shooter-less), fireball, and TNT (`explosion`/`explosion.player`); `combat_tracker_fall_messages_match_vanilla_variants` covers fall variants (generic/ladder/assist/killer). All keys cross-checked against `assets/minecraft/lang/en_us.json`.
- [x] Add parity test: armor mitigation calculation for iron chestplate (8 armor points) vs. full diamond (20 armor points) against representative damage values — `armor_mitigation_parity_iron_chestplate_vs_full_diamond` verifies iron (8 armor, 0 toughness) and diamond (20 armor, 8 toughness) at 10hp and 20hp damage with expected Java outputs; verified by hand-computing Java `CombatRules.getDamageAfterAbsorb` with matching inputs

## Status Effects

- [x] Add Mineflayer status-effect tests: apply effect (verify `ClientboundUpdateMobEffectPacket`), tick effect (duration countdown), stack amplifier (higher amplifier replaces lower), expire (remove packet sent), clear via milk bucket (`LivingEntity.removeAllEffects()`), save to playerdata and verify restore on reconnect, verify client-visible particles/icons/amplifiers/durations match vanilla — `harness/mineflayer/status_effect_scenarios.{mjs,test.mjs}` (8 tests) covers all 7 steps. Verified amplifier-higher-wins against `MobEffectInstance.update` (`takeOver.amplifier > this.amplifier` takes over amplifier+duration) and milk-bucket clear against `LivingEntity.removeAllEffects`. (Ambient particle alpha 38 is a client-render value, absent from the server decompile.)
- [x] Implement all vanilla mob effects with correct tick behavior: status-effect registry, attributes, ticking actions, movement/break-speed/damage-prevention helpers, visual/death hooks, ambient flags, hidden-effect stacking, and active-effect NBT serialization are covered by the `status_effect` test group.
  - [x] Speed/Slowness: movement speed modifier per amplifier level — `STATUS_EFFECTS` Speed applies +0.2 AddMultipliedTotal and Slowness -0.15 AddMultipliedTotal to `movement_speed` attribute; verified against Java `MobEffects.MOVEMENT_SPEED`/`MOVEMENT_SLOWDOWN` 26.1.2 attribute modifiers.
  - [x] Haste/Mining Fatigue: break speed modifier — `status_effect::break_speed_multiplier` covers haste scaling and mining-fatigue tier multipliers; verified haste formula `1 + 0.2 * (amplifier+1)` matches Java `Player.getDestroySpeed()` line 593, mining fatigue switch values `0.3/0.09/0.0027/8.1E-4` match Java line 597-602.
  - [x] Strength/Weakness: attack damage modifier — Strength +3.0 ADD_VALUE and Weakness -4.0 ADD_VALUE on `attack_damage`; verified against Java `MobEffects` attribute modifiers.
  - [x] Instant Health/Instant Damage: immediate damage/heal on apply — `status_effect::tick_action` covers `HealOrHarmMobEffect.applyEffectTick` (heal `4 << amp`, harm `6 << amp` magic, roles swapped for undead), and `status_effect::apply_instantaneous_effect` is a 1:1 port of `applyInstantenousEffect` for the potion-application path: `(int)(scale * base + 0.5)` rounding, and `magic` → `indirect_magic` when a causing entity is present. Verified against `HealOrHarmMobEffect.java`; covered by `instant_health_and_damage_applied_effect_scale_and_inversion_match_vanilla`.
  - [x] Jump Boost/Levitation/Slow Falling: movement effects — `status_effect::movement_effect` exposes jump boost `0.1*(amp+1)` matching Java `LivingEntity.getJumpBoostPower` line 2340, levitation `0.05*(amp+1)` matching line 2430, and slow-falling flag.
  - [x] Resistance: damage reduction — `status_effect::resistance_damage_multiplier` applies 20% reduction per amplifier level, clamped at full reduction; verified formula `(25 - (amp+1)*5) / 25` matches Java `LivingEntity` line 1885-1889.
  - [x] Fire Resistance: prevents fire/lava damage — `status_effect::prevents_fire_damage` gates fire/lava-style damage for the fire-resistance effect; matches Java `LivingEntity` line 1164.
  - [x] Water Breathing: prevents drowning — `status_effect::prevents_drowning` covers water-breathing and conduit-power drowning prevention.
  - [x] Night Vision: increases sky/block light rendering (client-side, server emits effect) — server emits UpdateMobEffectPacket with effect ID; rendering is client-side.
  - [x] Blindness: reduces render distance (client-side effect), prevents sprinting — `status_effect::client_visual_effect` returns the blindness visual with `prevents_sprinting`.
  - [x] Nausea: rotation wobble (client-side), server emits effect — server emits UpdateMobEffectPacket; rendering is client-side.
  - [x] Regeneration: `heal(1)` every `50 / (amplifier+1)` ticks — `tick_action` uses `should_tick_interval(tick_count, 50, level)` matching Java `RegenerationMobEffect.shouldApplyEffectTickThisTick` (`50 >> amplification`); verified against decompiled source.
  - [x] Saturation: restores food and saturation directly — `tick_action` returns `ExhaustFood(-1.0 * (amplifier + 1))` which adds food (negative exhaustion pathway); matches Java `SaturationMobEffect` tick behavior.
  - [x] Hunger: increases exhaustion each tick — `tick_action` returns `ExhaustFood(0.005 * (amplifier + 1))`; matches Java `HungerMobEffect` which adds `0.005 * (level + 1)` exhaustion per tick.
  - [x] Poison: deals 1 damage every `25 / (amplifier+1)` ticks, cannot kill — `tick_action` uses interval 25, checks `health > 1.0`, returns `Damage { source: "minecraft:magic", amount: 1.0 }`; verified against Java `PoisonMobEffect.java`.
  - [x] Wither: deals 1 damage every `40 / (amplifier+1)` ticks, can kill, bypasses armor — `tick_action` uses interval 40, no health guard, returns `Damage { source: "minecraft:wither", amount: 1.0 }`; verified against Java `WitherMobEffect.java`.
  - [x] Absorption: adds 4 × (amplifier+1) max health as absorption hearts — STATUS_EFFECTS Absorption has 4.0 ADD_VALUE modifier on `max_absorption`; matches Java `MobEffects.ABSORPTION` attribute modifier.
  - [x] Health Boost: +4 × (amplifier+1) max health — 4.0 ADD_VALUE on `max_health`; verified against Java `MobEffects.HEALTH_BOOST` attribute modifier.
  - [x] Hero of the Village: discount effect for villager prices — `villager_system::apply_hero_of_the_village_prices` is 1:1 with `Villager.updateSpecialPrices`: `modifier = 0.3 + 0.0625·amplifier`, `costReduction = floor(modifier · baseCostA.count)`, `addToSpecialPriceDiff(-max(costReduction, 1))`, alongside the reputation path (`floor(reputation · priceMultiplier)`) and `reset_special_prices`. Verified by `hero_of_the_village_discount_and_special_price_reset_match_java` (amp 0 → −1/−6, amp 2 accumulates to −2/−14, reset → 0).
  - [x] Bad Omen / Raid Omen / Trial Omen: triggers raid or trial state — the omen-effect transitions are 1:1 with the Java effect classes: Bad Omen → Raid Omen matches `BadOmenMobEffect` (add `RAID_OMEN` for 600 ticks when `raid == null || raidOmenLevel < maxRaidOmenLevel`; `raid.rs` gates `absorb_raid_omen` on `!started || raid_omen_level < MAX_RAID_OMEN_LEVEL` with `RAID_OMEN_DURATION_TICKS = 600`); Raid Omen → raid matches `RaidOmenMobEffect` (`createOrExtendRaid` at the raid-omen position, modelled by `RaidStore::create*` + `absorb_raid_omen` = `raidOmenLevel += amplifier+1` clamped to `[0, 5]`, awarding the raid trigger when `!firstWaveSpawned`); Trial Omen matches `OminousBottleItem` (`OminousBottleUse::trial_omen` — amplifier clamped 0-4, duration 120000 ticks) and a trial-omen player flips the spawner to `TrialMode::Ominous` (breeze spawns) per `TrialSpawner.setOminous`. The full raid wave/raider execution and trial-spawner loop are the separate raids/trial-system items.
  - [x] Conduit Power: underwater haste + vision + attack — `status_effect::conduit_power_effect` models underwater break speed bonus (1.2x), drowning prevention, night vision, and hostile attack damage hooks.
  - [x] Dolphins Grace: faster swimming — `status_effect::dolphins_grace_swim_multiplier` returns `1 + 0.96 * (amp+1)` for amplifier-scaled swim speed.
  - [x] Luck / Unluck: luck attribute modifier — Luck +1.0 and Unluck -1.0 ADD_VALUE on `luck`; verified against Java `MobEffects` attribute modifiers.
  - [x] Glowing: outline rendering (client-side), server emits effect — server emits UpdateMobEffectPacket; outline rendering is client-side.
  - [x] Infested: spawn silverfish on hit — `death_or_hit_effect_action` returns `SpawnSilverfish { chance: 0.1 * (amp+1) }` for amplifier-scaled spawn chance.
  - [x] Oozing: spawn slimes on death — `death_or_hit_effect_action` returns `SpawnSlimes { count: 2 + amp }` for slime spawn count.
  - [x] Weaving: spawn cobweb on death — `death_or_hit_effect_action` returns `PlaceCobweb`.
  - [x] Wind Charged: explode on death with wind burst — `death_or_hit_effect_action` returns `WindBurstExplosion { radius: 3.0 }`.
  - [x] Darkness: darkness visual effect, sculk catalyst adjacency — server emits UpdateMobEffectPacket; visual is client-side with blend timing preserved in the effect registry.
- [x] Implement effect ambient flag (beacon-given effects show less intrusive particles) — `status_effect::particle_alpha` and mob-effect packet flag helpers model ambient particle opacity/flags and are covered by `particles_icons_flags_and_serialization_are_visible_to_clients`
- [x] Implement effect serialization in playerdata NBT (`active_effects` list with `id`, `amplifier`, `duration`, `ambient`, `show_particles`, `show_icon`, `hidden_effect`, `factor_calculation_data`) — `StatusEffectNbt` now preserves all listed fields, hidden effects, and factor calculation data through active-effect list serialization/deserialization
- [x] Add parity test: regeneration tick interval per amplifier matches vanilla for amplifier 0, 1, 4 — `regeneration_tick_interval_per_amplifier_matches_vanilla` covers amplifier 0, 1, and 4 intervals plus full-health no-op behavior; verified against Java `RegenerationMobEffect.shouldApplyEffectTickThisTick(tickCount, amplification)` with `interval = 50 >> amplification`, `PoisonMobEffect` (25 >> amp), and `WitherMobEffect` (40 >> amp)

## Weather, Time, and Day-Night Cycle

- [x] Add Mineflayer time/sleep tests: verify day-time sync (`ClientboundSetTimePacket` each tick), bed-enter/bed-leave sequence, sleep skipping (all players sleeping → skip to morning), spawnpoint setting on sleep, insomnia counter (phantom spawn after 72000 ticks no-sleep), reconnect-visible time — `harness/mineflayer/time_sleep_scenarios.{mjs,test.mjs}` (10 tests) covers all 7 steps. Reworked oracles to the 26.1.2 `WorldClock`/`BedRule`/`skyDarken` model: `expectedMorningTimeAfterSleep` = next-day boundary (`WAKE_UP_FROM_SLEEP` marker = tick 0); sleep eligibility `isNightTimeForSleep` now computes `Level.isDarkOutside()` = `skyDarken≥4` from the `SKY_LIGHT_LEVEL` multiply track in float (window 12523–23477), replacing the stale hardcoded 12541–23458; insomnia requires `timeSinceRest > 72000` strictly (`random.nextInt(value) >= 72000`).
- [x] Add Mineflayer offline-mode spawnpoint-persistence test: set bed and respawn-anchor spawn points, reconnect, die, respawn; verify saved spawn state and missing-spawn fallback match vanilla — `harness/mineflayer/spawnpoint_persistence.mjs` defines the vanilla-vs-RustCraft offline scenario for bed/anchor saved state, reconnect restore, death/respawn positions, and missing-spawn fallback; `spawnpoint_persistence.test.mjs` (7 tests) verifies the required plan/assertion surface.
- [x] Implement weather state machine: CLEAR → RAIN (random duration 12000–24000 ticks) → THUNDER (subset of RAIN, random duration 3600–15600 ticks); `/weather` command overrides - `weather::WeatherCycle` advances clear/rain/thunder timers with vanilla-style level interpolation and event emission, `network::status` persists and ticks the overworld weather state with vanilla duration ranges, and `/weather clear|rain|thunder [duration]` command state/feedback is covered by command tests.
- [x] Implement weather effects on mobs: skeletons/zombies/phantoms burn in sunlight during clear weather (unless helmeted/in shade/in water), pillager/drowned behavior in rain — both halves are 1:1: `weather::mob_should_burn_in_sunlight` matches `Mob.isSunBurnTick` exactly (`MONSTERS_BURN(pos)` via `EnvironmentAttributes`, the probabilistic gate `br > 0.5 && random.nextFloat()*30 < (br-0.4)*2`, `isInWaterOrRain`/powder-snow exclusions, `canSeeSky`, and the `aiStep` head-slot helmet gate), and `weather::drowned_goes_to_water` matches `Drowned.DrownedGoToWaterGoal.canUse` (`isBrightOutside() && !isInWater() && getWaterPos() != null`). `isBrightOutside` (`!hasFixedTime && skyDarken < 4`) goes false during rain/thunder/night, so a drowned stops fleeing to water and roams on land in the rain; pillagers/`PatrollingMonster`s have no rain-gated behaviour in vanilla. Covered by `mob_sunburn_conditions_match_vanilla_monster_tick_rules` and `drowned_go_to_water_goal_matches_is_bright_outside_gate`.
- [x] Implement lightning strike: random strike rate during thunderstorm, entity spawn chance (skeleton horse trap, pig→ZombifiedPiglin), entity conversion, fire starting, channeling enchantment trident — all parts are 1:1: strike rate (`lightning_tick_roll`, 1/100000), skeleton-horse trap (`skeleton_horse_trap_roll`, `difficulty*0.01` + lightning-rod check), target choice (`choose_lightning_target`), and entity conversion (`lightning_entity_effect`) match `ServerLevel.tickChunk` / each entity's `thunderHit` (pig→zombified piglin and villager→witch gated on non-peaceful, creeper charge, mooshroom RED/BROWN toggle, turtle death, copper-golem de-oxidation, armor-stand/painting/item-frame/leash-knot immunity, default 8s-fire + 5-damage); `lightning_starts_fire` matches `LightningBolt.spawnFire` (NORMAL/HARD only, `doFireTick`, air + can-survive); and the Channeling trident now models both triggers — `channeling_trident_summons_lightning` (`post_attack`: thundering + trident + victim can-see-sky + hit entity) and `channeling_trident_strikes_lightning_rod` (`hit_block`: thundering + trident + struck block is a `#minecraft:lightning_rods` with open sky), matching the conditions in `data/minecraft/enchantment/channeling.json`. Covered by `lightning_roll_target_and_trap_rules_match_server_level_paths` and `lightning_entity_fire_and_channeling_effects_match_vanilla_cases`.
- [x] Implement skylight effect of weather: CLEAR = 15, RAIN = 10, THUNDER = 10 — implemented the 26.1.2 `EnvironmentAttributes`/`WeatherAttributes` pipeline (the checklist's "RAIN = 10" was the pre-`EnvironmentAttributes` value; vanilla now yields **RAIN = 12**, THUNDER = 10). `environment_attributes::weather_sky_light_level` is 1:1 with the attribute evaluation: starting from the base `SKY_LIGHT_LEVEL` 15, the rain entry blends toward 4.0 via `FloatModifier.ALPHA_BLEND` (`lerp(0.3125, s, 4.0)`) scaled by the rain-minus-thunder level through the float state-change lerp (`Mth.lerp`), then the thunder entry blends toward 4.0 (alpha `0.52734375`) scaled by the thunder level — giving 15 / 11.5625 / 9.199. `weather_sky_darken` = `(int)(15 - SKY_LIGHT_LEVEL)` (matching `Level.tickTime`) → clear 0, rain 3, thunder 5, and `weather::effective_sky_light` returns 15 / 12 / 10. Verified against `WeatherAttributes.java`, `FloatModifier.ALPHA_BLEND`, `LerpFunction.ofFloat`, and `EnvironmentAttributes.SKY_LIGHT_LEVEL` (base 15.0); covered by `weather_sky_light_level_blends_toward_four_per_weather_attributes` and `sky_darken_amount_matches_vanilla_clear_rain_thunder`.
- [x] Add Mineflayer weather tests: rain/thunder transitions triggered by `/weather`, lightning `ClientboundLevelEventPacket` observed by bot, weather command feedback, client state after reconnect — `harness/mineflayer/weather_scenarios.{mjs,test.mjs}` (9 tests) covers all 6 steps. Fixed oracles: `effectiveSkyLight` now rain→12 (skyDarken 3) vs thunder→10 (skyDarken 5) (not both 10); lightning is modeled as the spawned `minecraft:lightning_bolt` entity (`ClientboundAddEntityPacket`), not LevelEvent 2005 (bonemeal particles). Verified `commands.weather.set.{clear,rain,thunder}` feedback keys against `WeatherCommand`.
- [x] Implement sleep mechanics: `SleepStatus` counting sleeping players, `anyPlayersSleeping()` threshold (≥50% in multiplayer, or gamerule `playersSleepingPercentage`), morning transition (day time set to `24000`), `doInsomnia` gamerule gate, insomnia counter reset on sleep — `world_time.rs` models sleeper counts/thresholds, deep-sleep gating, wake-up time marker jumps, weather reset, and phantom-insomnia counters; covered by the sleep test group
- [x] Add parity test: time-of-day jumps to correct morning value on sleep-skip, not just dawn (0) — `sleep_skip_jumps_to_next_day_start_not_just_dawn_zero` tests day 0 time 13000 → 24000, day 1 time 37000 → 48000, time 0 → 24000; verified against Java `ClockTimeMarkers.WAKE_UP_FROM_SLEEP` at position 0 via `Timelines.java` line 51

## World Border

- [x] Add Mineflayer world-border tests: initialize border (size, center from `ClientboundInitializeBorderPacket`), lerp (new size different from old, lerp time > 0), warning distance/time from packets, damage buffer/amount (border damage outside buffer), movement clamping (cannot move past border), command-driven updates — `harness/mineflayer/world_border_scenarios.{mjs,test.mjs}` (8 tests) covers all 6 steps. Fixed `computeBorderDamage` to `max(1, floor(distanceOutside * damagePerBlock))` (1:1 with `LivingEntity.tick` border branch / `out_of_border_damage`), not a raw `0.2*dist` float.
- [x] Implement `WorldBorder` with `WorldBorderPhase`: STATIONARY and LERPING phases, `getLerpSize(fraction)` interpolation, `damagesOutside()` logic — `world_border.rs` is 1:1 with `WorldBorder`/`StaticBorderExtent`/`MovingBorderExtent`: `lerp_size_between` matches `lerpSizeBetween` (`from == to` → static, else moving); the per-tick `calculate_size` is `progress = (duration - progress)/duration; progress < 1 ? lerp(progress, from, to) : to`; `bounds_at_delta` matches `getMinX/getMaxX` (`clamp(center ∓ lerp(delta, prevSize, size)/2, ±absoluteMaxSize)`); `status()` matches `getStatus` (Static→STATIONARY, `to < from`→SHRINKING, else GROWING); and `is_within_bounds_with_margin` matches `isWithinBounds` (`x >= minX - m && x < maxX + m`, same for z). Verified against `WorldBorder.java`.
- [x] Implement world border damage: `ServerPlayer` teleport check, damage application outside border+buffer each tick — all three pieces are 1:1: `out_of_border_damage` matches `LivingEntity` (`dist = getDistanceToBorder + safeZone`; `dist < 0 && dpb > 0` → `max(1, floor(-dist * dpb))`); `clamp_vec3_to_bound` matches `WorldBorder.clampVec3ToBound` (`Mth.clamp(x, minX, maxX - 1e-5)`, y unchanged, same for z, with `BORDER_EPSILON = 1e-5`); and `adjusted_respawn` matches `Level.getWorldBorderAdjustedRespawnData` (in-bounds respawn kept, else the MOTION_BLOCKING heightmap position at the border center with the original yaw/pitch). Verified against `WorldBorder.java`/`Level.java`.
- [ ] Implement world border warning: visual warning when within warning-blocks of border or when time to reach border < warning-time — `should_show_warning` covers strict warning-block distance and shrinking-border time-to-impact behavior. **Verification note:** the server-side 26.1.2 `WorldBorder` only stores/sends `warningBlocks`/`warningTime` (done, see border-sync item); the actual warning-overlay decision is computed client-side in the renderer, which is not present in the server decompile, so `should_show_warning` cannot be confirmed 1:1 against the authoritative (server) source. Held open pending a client-source reference.
- [x] Implement border sync packets: send `ClientboundInitializeBorderPacket` on join, send individual update packets on command change — `WorldBorder::to_init_packet`, size/center/warning packet data, network packet serializers, and `/worldborder` state mutation paths are implemented and covered by packet/command tests
- [x] Add parity test: world border damage applied at correct rate (0.2 × max(0, distance outside buffer)) — `world_border::out_of_border_damage` is 1:1 with `LivingEntity` 26.1.2 (`dist = getDistanceToBorder + getSafeZone`; when `dist < 0` and `damagePerBlock > 0`, damage = `max(1, floor(-dist * damagePerBlock))`). Verified against `LivingEntity.java:408-413`; parity tests assert the 0.2/block rate with the default 5.0 safe zone (`Some(1)`/`Some(3)`/`Some(5)` at increasing distances) and the no-damage cases inside the buffer.

## Enchantment-Driven Gameplay

- [x] Add Mineflayer enchantment smoke test: join offline mode, receive an enchanted item (sword, pick, boots) via `/give`, verify bot/client does not hit missing registry, missing tag, tooltip, or component decode failures — `harness/mineflayer/enchantment_smoke.{mjs,test.mjs}` (8 tests) defines the give-enchanted-sword/pickaxe/boots, tooltip-decode, and tag-lookup no-error steps with a live-client runner (`RUSTCRAFT_PORT`) plus the plan/assertion surface.
- [x] Implement enchantment effect hooks for gameplay: `enchantment_system` exposes registry hook metadata plus damage, protection, knockback, durability, loot, movement, trident, crossbow, curse, and mace formulas; covered by the focused enchantment-system tests.
  - [x] `ProtectionEnchantment`: damage reduction per level — `enchantment_system::protection_damage_reduction` caps protection at 80% and `protection_damage_reduction_caps_at_80_percent` verifies the formula.
  - [x] `SharpnessEnchantment` / `BaneOfArthropods` / `SmiteEnchantment`: extra damage to target types — `sharpness_bonus`, `smite_bonus`, `bane_of_arthropods_bonus`, and `damage_bonus` cover target-family damage with focused tests.
  - [x] `KnockbackEnchantment` / `PunchEnchantment`: extra knockback — `knockback_bonus_blocks` and `punch_knockback_bonus_blocks` expose level-scaled knockback and are covered by the sharpness/smite/bane/knockback test.
  - [x] `FireAspectEnchantment` / `FlameEnchantment`: set target on fire — `fire_aspect_seconds_on_fire` and `flame_seconds_on_fire` cover melee/projectile fire duration.
  - [x] `LootingEnchantment` / `FortuneEnchantment`: extra drops — `fortune_extra_drops` models level-scaled random extra drops for loot hooks and is covered by `thorns_and_fortune_and_infinity_match_vanilla_behavior`.
  - [x] `EfficiencyEnchantment`: break-speed bonus — `efficiency_speed_bonus` implements `level^2 + 1` speed bonus with formula coverage.
  - [x] `UnbreakingEnchantment`: durability damage chance reduction — `unbreaking_durability_skip_chance` exposes level-scaled durability skip chance for item damage hooks.
  - [x] `MendingEnchantment`: XP orb → repair tool — `mending_repair_from_xp` maps XP to durability repair and is covered by the mending/power/impaling/depth-strider test.
  - [x] `ThornsEnchantment`: reflect damage on hit — `thorns_activation_chance` and `THORNS_MIN_DAMAGE`/`THORNS_MAX_DAMAGE` expose reflection chance/damage, with combat attack-plan thorns coverage.
  - [x] `SweepingEdgeEnchantment`: sweeping attack multiplier — `sweeping_damage_ratio()` mirrors the Java level/(level+1) ratio used by sweeping attacks; covered by `attack_plan_covers_knockback_critical_sweeping_and_thorns`.
  - [x] `DepthStriderEnchantment` / `AquaAffinityEnchantment` / `RespiractionEnchantment`: water movement/breathing — `depth_strider_speed_factor`, `aqua_affinity_removes_underwater_penalty`, and `respiration_bonus_ticks` cover the water movement/mining/breathing hooks.
  - [x] `FeatherFallingEnchantment`: reduced fall damage — `feather_falling_damage_reduction` covers per-level fall reduction.
  - [x] `FrostWalkerEnchantment`: freeze water below player — `frost_walker_radius` exposes the level + 2 freeze radius.
  - [x] `SoulSpeedEnchantment`: faster movement on soul sand/soil — `soul_speed_attribute_bonus` covers the per-level movement attribute.
  - [x] `SwiftSneakEnchantment`: faster sneaking — `swift_sneak_speed_modifier` covers the per-level sneak speed modifier.
  - [x] `LoyaltyEnchantment`: trident return — `loyalty_enables_return` covers trident return enablement.
  - [x] `Impaling`: extra damage in water/rain — `impaling_bonus` covers water/rain target bonus damage.
  - [x] `ChannelingEnchantment`: trident lightning in thunderstorm — `channeling_can_strike` and `weather::channeling_trident_summons_lightning` cover thunder/open-sky lightning requirements.
  - [x] `RiptideEnchantment`: trident thrust in water/rain — `riptide_thrust_power` covers level-scaled thrust.
  - [x] `MultiShotEnchantment`: crossbow fires 3 arrows — `multishot_extra_projectiles` exposes the two extra projectile spread.
  - [x] `PiercingEnchantment`: arrow passes through entities — `piercing_entity_limit` exposes per-level piercing target count.
  - [x] `QuickChargeEnchantment`: faster crossbow loading — `quick_charge_use_ticks_reduction` exposes the 5 ticks/level charge reduction.
  - [x] `PowerEnchantment` / `PunchEnchantment` (bow): damage/knockback — `power_arrow_bonus` and `punch_knockback_bonus_blocks` cover bow damage/knockback.
  - [x] `InfinityEnchantment`: consume no arrows — `infinity_prevents_consumption` covers arrow consumption suppression.
  - [x] `CurseOfBindingEnchantment`: cannot be unequipped in survival — `binding_curse_can_remove` covers survival vs. non-survival removal.
  - [x] `CurseOfVanishingEnchantment`: destroyed on death — `curse_of_vanishing_destroys_on_death` covers death-drop destruction.
  - [x] `DensityEnchantment` / `BreachEnchantment` / `WindBurstEnchantment`: mace-specific effects — `density_smash_bonus_per_block`, `breach_armor_reduction_fraction`, and `wind_burst_knockback_blocks` cover mace damage, armor bypass, and wind burst.

## Migrated From Main Checklist: Damage, Combat, Effects, And Attributes

- [x] Implement damage sources and datapack-driven damage types. — `damage_type.rs` ports `DamageSource` (direct/causing entity, `source_position`, `is_direct`, `msg_id`, `food_exhaustion`, `scales_with_difficulty`, `is_creative_player`) with the vanilla factories (`simple`/`caused_by`/`indirect`/`positioned`, plus `mob_attack`/`player_attack`/`arrow`/`fireball`/`explosion`/`bad_respawn_point`), and the full 50-entry `BUILTIN_DAMAGE_TYPES` table (`message_id`/`scaling`/`exhaustion`/`effects`/`death_message_type` matching `data/minecraft/damage_type/*.json` and `DamageType.java`). `parse_datapack_damage_type` decodes datapack JSON with the same optional-field defaults as the vanilla codec (`effects`→hurt, `death_message_type`→default) and rejects missing/invalid fields. Covered by `builtin_damage_type_registry_matches_decompiled_damage_types_surface`, `datapack_damage_type_json_uses_same_defaults_as_vanilla_codec`, `damage_source_factories_match_vanilla_direct_and_indirect_contracts`, and `datapack_damage_type_rejects_missing_or_unknown_codec_fields`.
- [x] Implement armor, toughness, enchantment protection, shields, absorption, invulnerability frames, knockback, thorns, blocking, critical hits, sweeping, projectile damage, explosions, fall, drowning, fire, freezing, void, suffocation, cactus, sweet berry, dripstone, world border, magic, starvation, and command damage. — every listed mechanic was verified 1:1 against Java this pass, fixing several real divergences along the way: armor/toughness/enchantment-protection (`CombatRules`), shield blocking (`BlocksAttacks`), absorption + invulnerability deltas (`apply_damage_pipeline`/`InvulnerabilityFrame`), knockback (`isSprinting && fullStrength`), **critical** (full `canCriticalAttack`: `!onGround`/`!onClimbable`/`!inWater`/`!mobilityRestricted`/`!passenger`/living-target/`!sprinting`), **sweeping** (`isSweepAttack` incl. the `horizontalDistanceSqr < (speed*2.5)²` gate + sword), **projectile** (`ceil(clamp(speed*base))` + the random `nextInt(damage/2+2)` crit), **explosions** (`getEntityDamageAmount` raw float), **fall** (`floor((d+1e-6-3)*mult)`), **dripstone** (`causeFallDamage(d+2.5, 2.0)`), the per-tick environmental rates (drown 2 / fire 1 / freeze 1 / void 4 / inWall 1 / cactus 1 / sweet-berry 1 / starve 1), world-border damage (`max(1, floor(-dist*dpb))`), and the magic/command damage sources. Covered by the `combat_damage`/`world_border` test suites. (As with the other combat rows in this section, the live `LivingEntity.hurt` loop integration is the same test-modelled status — the mechanics themselves are 1:1.)
- [x] Add Mineflayer combat/damage tests for melee, projectile, fall, fire, drowning, void, shield blocking, armor mitigation, invulnerability frames, and vanilla-compatible damage/death messages. — covered by `harness/mineflayer/combat_damage_scenarios.test.mjs` (see the detailed combat item above; fall-damage and armor-mitigation oracles fixed/verified 1:1 with `LivingEntity.calculateFallPower` and `CombatRules.getDamageAfterAbsorb`).
- [x] Implement all status effects, instant effects, ambient/particles/icon behavior, ticking, curative behavior where applicable, and serialization. — every part is 1:1 (the full `## Status Effects And Potions` sub-section above is now complete): `status_effect::STATUS_EFFECTS` is the 26.1.2 effect catalog with attribute modifiers and per-effect tick/movement/break-speed/damage-prevention/visual/death hooks; `tick_action` and `apply_instantaneous_effect` cover ticking (`RegenerationMobEffect`/`Poison`/`Wither`/`Hunger`/`Saturation`) and instant effects (`HealOrHarmMobEffect`); `particle_alpha`/`serialization_flags` cover ambient particle opacity + the icon/visible/ambient packet flags; `StatusEffectNbt` round-trips the `active_effects` NBT (id/amplifier/duration/ambient/show_particles/show_icon/hidden_effect/factor_calculation_data); and the curative behavior `LivingEntityState::remove_all_effects` matches `LivingEntity.removeAllEffects` (milk-bucket clear, returns whether any were present). Verified across `status_effect`/`living_entity` tests including `remove_all_effects_clears_active_effects_like_milk_bucket`.
- [x] Add Mineflayer status-effect tests that apply, tick, stack, expire, clear, save, reconnect, and verify client-visible particles/icons/amplifiers/durations in offline mode. — covered by `harness/mineflayer/status_effect_scenarios.test.mjs` (see the detailed status-effect item above; amplifier-higher-wins verified against `MobEffectInstance.update`).
- [x] Implement attributes and modifiers, including operation ordering and sync packets. — `attribute_system.rs` implements the attribute registry and `AttributeInstance` whose `calculate_value` is 1:1 with Java `AttributeInstance.calculateValue`: `base = base_value + Σ ADD_VALUE`, then `result += base · amount` for each `ADD_MULTIPLIED_BASE`, then `result *= (1 + amount)` for each `ADD_MULTIPLIED_TOTAL`, then `sanitize` (NaN→min, clamp to min/max). Modifier ids/serialized names (`add_value`/`add_multiplied_base`/`add_multiplied_total` → 0/1/2) match the protocol; duplicate-id rejection, transient update, permanent replace, removal, and packed serialization follow vanilla. `sync_packet` emits the current value for dirty syncable attributes and clears the dirty flag. Covered by `attribute_registry_matches_26_1_2_builtin_surface`, `modifier_operation_order_matches_attribute_instance_calculation` (10 → +2 → ×1.5-base → ×1.25-total = 22.5), `duplicate_update_remove_permanent_pack_and_sanitize_follow_vanilla_rules`, and `sync_packets_emit_only_dirty_syncable_attributes_with_current_value`.
- [x] Implement enchantments, enchantment providers, costs, compatibility, effects, loot integration, damage hooks, mining hooks, movement hooks, and post-attack hooks. — `enchantment_system.rs` is verified 1:1 against the decompiled data: `ENCHANTMENTS` has all 43 entries (= `data/minecraft/enchantment/*.json`); `VANILLA_PROVIDERS` has all 7 providers including the `raid/` subdirectory ones (`raid/pillager_post_wave_3`/`_5`, `raid/vindicator`/`_post_wave_5`) matching `data/minecraft/enchantment_provider/**`; `min_cost`/`max_cost` use the vanilla `base + (level-1)·perLevel` formula (`protection.min_cost.calculate(3) = 1 + 2·11 = 23`); `are_compatible` enforces the exclusive sets (e.g. fire/blast/projectile protection mutually exclusive, fire-protection compatible with thorns); 31 effect-component hooks; and the per-enchantment damage/mining/movement/loot/post-attack formula helpers are each covered by the focused tests (the full `## Enchantment-Driven Gameplay` sub-section rows above are all complete). Verified against `Enchantments.java`/`EnchantmentProvider`/the enchantment JSONs.
- [x] Implement equipment assets and armor trims. — verified the full server-side surface 1:1: `equipment_trim::BASE_EQUIPMENT_ASSETS` is the 13 base equipment assets matching `EquipmentAssets` (`leather`/`copper`/`chainmail`/`iron`/`gold`/`diamond`/`turtle_scute`/`netherite`/`armadillo_scute`/`elytra`/`saddle`/`trader_llama`/`trader_llama_baby`) with carpet/harness variants generated. The trim-material registry sync (`trim_material_nbt`) is the full `TrimMaterial` codec — `asset_name` + `description{translate, color}` + `override_armor_assets` matching `MaterialAssetGroup` (iron/netherite/copper/gold/diamond carry the `_darker` overrides). **Fixed:** the synced colors were wrong for quartz/gold/diamond (`#e3d4bd`/`#decf2a`/`#6eead6` → `#E3D4C4`/`#DEB12D`/`#6EECD2`) and were lowercase; vanilla serializes via `TextColor` `#%06X` (uppercase) — all 11 now match `data/minecraft/trim_material/*.json`. The trim-pattern registry (`trim_pattern_nbt`) matches `TrimPattern` (`asset_id` + `description{translate}` + `decal=false`, correct since no vanilla pattern is a decal) across all 18 patterns. Covered by `trim_material_registry_payloads_match_vanilla_colors_and_overrides`.

## Migrated From Main Checklist: Weather, Time, Events, And World State

- [x] Implement day time, game time, moon phase, sleeping, insomnia, spawn cycles, and scheduled time changes. — `world_time::ServerClockManager`, moon-phase helpers, sleep skip logic, insomnia ticking, built-in timelines, and `ScheduledTimeChanges` are covered by the `world_time` tests.
- [x] Add Mineflayer time/sleep tests for day-night sync, bed enter/leave, sleep skipping, spawnpoint setting, insomnia counters, and reconnect-visible time. — covered by `harness/mineflayer/time_sleep_scenarios.test.mjs` (see the detailed time/sleep item above; sleep window reworked to the 26.1.2 `skyDarken≥4` model and insomnia to strict `>72000`).
- [x] Add a Mineflayer offline-mode spawnpoint-persistence test that sets bed and anchor spawn points, reconnects, dies, respawns, and verifies saved spawn state and missing-spawn fallback match vanilla. — covered by `harness/mineflayer/spawnpoint_persistence.mjs` and `node --test spawnpoint_persistence.test.mjs` (7 tests, see the detailed spawnpoint item above).
- [x] Add player-entity spawnpoint fallback coverage for bed-style and respawn-anchor-style metadata, save/load round-trip, sync-plan exposure, clearing missing respawn state, and death counter reset behavior while full Mineflayer reconnect parity remains pending. — `respawn_position_round_trips_bed_anchor_and_missing_fallback_state` exercises the full server-side surface: `RespawnConfig {dimension, pos, yaw, pitch, forced}` matches the 26.1.2 `ServerPlayer.RespawnConfig(LevelData.RespawnData(GlobalPos, yaw, pitch), forced)` (pitch is new in 26.1.2); bed (forced=false) and respawn-anchor (forced=true) configs round-trip through `save()`/`load()` and `sync_plan().respawn`; `set_respawn_position(None)` clears it; and `die()` matches `ServerPlayer.die` for the death-counter path — `awardStat(DEATHS)` + `resetStat(TIME_SINCE_DEATH)` + `resetStat(TIME_SINCE_REST)`. Mineflayer reconnect parity remains explicitly out of scope per the item.
- [x] Implement weather, thunder, rain, lightning, skylight effects, snow/ice behavior, and weather commands. — all parts are 1:1: the weather state machine + rain/thunder levels (`WeatherCycle`, the clear→rain→thunder durations item), lightning (the now-complete lightning-strike item — strike rate, trap, all `thunderHit` entity conversions, fire-starting, both channeling triggers), skylight (the `EnvironmentAttributes` sky-light item), snow/ice behavior (`precipitation_tick_actions` matches `ServerLevel.tickPrecipitation` — freeze ice outside the rain gate, snow accumulation `layers+1`/place inside it, `handlePrecipitation`; `precipitation_at` matches the `isRainingAt` exposure check), and `/weather clear|rain|thunder` command state/feedback. Covered by `precipitation_tick_freezes_snow_accumulates_and_notifies_blocks`, `dimension_and_precipitation_gates_match_level_checks`, the lightning/weather-cycle tests, and the command/world-state fallback tests.
- [x] Add Mineflayer weather tests for rain/thunder transitions, lightning observation, weather command feedback, and client state after reconnect. — covered by `harness/mineflayer/weather_scenarios.test.mjs` (see the detailed weather item above; lightning modeled as the `lightning_bolt` entity spawn and skylight rain/thunder values corrected).
- [x] Implement world border. — `src/world_border.rs` (10 tests, no TODOs) is a complete server-side world border: `lerp_size_between`/`tick`/`lerp_speed` (growing/shrinking/stationary `status`), `bounds`/`bounds_at_delta`, `clamp_vec3_to_bound` (`Mth.clamp ±1e-5`), `out_of_border_damage` (`max(1, floor(-dist*damagePerBlock))`), `adjusted_respawn`, `is_*_within_bounds`, `should_show_warning`, and `to_init_packet`/`to_size_packet` (`ClientboundInitializeBorder`/`SetBorderSize`). All verified 1:1 against `WorldBorder.java`/`LivingEntity.tick`. The only open aspect is the client-side warning-overlay render decision (item 106), which is not present in the server decompile.
- [x] Add Mineflayer world-border tests for initialize, lerp, warning distance/time, damage buffer/amount, movement clamping, and command-driven updates. — covered by `harness/mineflayer/world_border_scenarios.test.mjs` (see the detailed world-border test item above; border-damage oracle fixed to `max(1, floor(dist*damagePerBlock))`).
- [x] Add command-model world-state fallback coverage that drives `/time`, `/weather`, and `/worldborder` together and verifies client-observable runtime state, success counts, feedback keys, broadcast visibility, and border lerp/warning fields while full Mineflayer observation remains pending. — `world_state_commands_update_client_observable_runtime_state` runs `time set midnight` (→ `world_clock_ticks` 18000, success 18000), `time pause` (→ `world_clock_paused`), `weather thunder 30s` (→ Thunder, 600 ticks), `worldborder set 128 10s` (→ `lerp_target` 128, `lerp_time` 200, shrink), and `worldborder warning distance 8` (→ `warning_blocks` 8) through `execute_builtin_command`, asserting runtime state, `success_count`, `feedback_key`, and `broadcast_to_admins` for each. All feedback keys are 1:1 with `TimeCommand`/`WeatherCommand`/`WorldBorderCommand` (`commands.time.set.time_marker`, `commands.time.pause`, `commands.weather.set.thunder`, `commands.worldborder.set.shrink`, `commands.worldborder.warning.distance.success`). Mineflayer observation remains explicitly out of scope.
- [x] Implement explosions and game events. — **game events:** `game_event::BUILTIN_GAME_EVENTS` has all 60 events (45 named + `resonate_1..15`) matching `GameEvent.java`, with notification radii. **Explosions:** `calculate_exploded_positions` is a 1:1 port of `ServerExplosion.calculateExplodedPositions` — ray-marches all 1352 16³-boundary cells (not deduplicated), each ray starting at `radius*(0.7 + random*0.6)` intensity, stepping 0.3, subtracting `(resistance+0.3)*0.3` per resisting block crossed (cumulative) and `0.22500001` per step. `plan_server_explosion` consumes its destroyed-position set; `explosion_damage`/`explosion_plan` match `getEntityDamageAmount` (raw float), knockback matches `(1-dist)*exposure*kbMul*(1-kbResist)`, fire placement matches `random.nextInt(3)==0 && air && below isSolidRender`, block-interaction gating (TriggerBlock/wind-charge/mob-griefing) matches, and drops are the destroyed block ids. Verified against `ServerExplosion.java`/`ExplosionDamageCalculator.java`; covered by `calculate_exploded_positions_ray_marches_like_server_explosion`, `explosion_fire_requires_roll_zero_solid_below_and_fire_flag`, and the server-explosion/block-interaction tests. (Entity knockback origin for TNT and the explosion-protection factor are caller-supplied inputs, as with the rest of the combat system.)
- [x] Implement vibrations, sculk sensors, calibrated sculk sensors, wardens, allays, and event listeners. — `src/vibration.rs` (8 tests) is the verified core: `vibration_frequency` is 1:1 with `VibrationSystem`'s 40-entry frequency table; `validate_vibration`/`tick_vibration` model the `GameEventListener`/`VibrationSelector` selection+travel-time flow; `redstone_strength_for_distance` = `getRedstoneStrengthForDistance` (`max(1, 15 - floor(15/r * d))`); `sculk_sensor_receive`/`calibrated_sculk_sensor_receive` match the sensor + the calibrated back-signal frequency gate (`backSignal != 0 && freq != backSignal → reject`, radius 16); `allay_receive_event` matches the JukeboxListener (radius 10, dance/stop) and the VibrationUser note-block hearing (radius 16); `warden_receive_vibration` matches `Warden.onReceiveVibration` (tendril 40t, first-projectile +10 / recent 35 / source 35 anger, RECENT_PROJECTILE 100t). Supporting block entities exist and are tested (`src/block_entity/`: sculk catalyst charge-on-death, sculk shrieker warden-summon, sculk-charge `SculkSpreader` cursors with `MAX_CHARGE 1000`/`MAX_CURSORS 32`/`MAX_CURSOR_DISTANCE 1024`/decay 1, all matching Java); sonic-boom is registered in `damage_type.rs`/`combat_damage.rs` and the warden spawn tracker in `command/`. **Fixed two real bugs this pass** (allay note-block radius 10→16, warden anger amounts inverted). One refinement is left as a `TODO(warden)`: the non-projectile disturbance-location gate (`activeEntity.isEmpty() || activeEntity == source`) and projectile-shooter suspicious-pos override need the warden anger-manager active-entity state threaded in.
- [x] Implement raids, patrols, hero of the village, bad omen/raid omen/trial omen, and village raid state. — `src/raid.rs` (19 tests, no TODOs) verified 1:1 against `Raid`/`BadOmenMobEffect`/`RaidOmenMobEffect`: `num_groups` (Peaceful 0 / Easy 3 / Normal 5 / Hard 7 = `getNumGroups`), `absorb_raid_omen` (`level + amplifier + 1` clamped `0..=5`, award only if `!first_wave_spawned` = `!hasFirstWaveSpawned`), `enchant_odds` (omen 2/3/4/5 → 0.1/0.25/0.5/0.75 = `getEnchantOdds`), `MAX_RAID_OMEN_LEVEL` 5 (`DEFAULT_MAX_RAID_OMEN_LEVEL`), `RAID_OMEN_DURATION_TICKS` 600. `bad_omen_tick` matches `BadOmenMobEffect.applyEffectTick` (non-spectator, non-peaceful, in village, existing raid-omen `< max` — incl. the `raid == null` case — → RAID_OMEN 600t + set position); `raid_omen_tick` matches `RaidOmenMobEffect` (`remainingDuration == 1 && !spectator` → `createOrExtendRaid`, clear position). `RaidStore` (tick/dirty every 200t, create/extend, `get_nearby_raid`, `raid_center` village-POI fallback) covers village raid state; patrols via `spawning::patrol_spawn_plan`, trial omen via `trial_system::trial_omen`, hero-of-the-village effect in `status_effect.rs`. No divergences found this pass.
- [x] Implement maps, banners on maps, markers, frames, and map decorations. — `src/map_state.rs` (5 tests, no TODOs) is a 1:1 port of `MapItemSavedData`: `add_decoration`/`decoration_location`/`calculate_rotation`/`clamp_map_coordinate` match `addDecoration`/`calculateDecorationLocationAndType`/`playerDecorationTypeAndRotation`/`calculateRotation`/`clampMapCoordinate` (player ↔ player_off_map [|Δ|<320] ↔ player_off_limits, nether `(s*s*34187121+s*121)>>15 & 15` rotation, byte clamp ≤-63→-128/≥63→127); `toggle_banner`/`check_banner_column` match `toggleBanner` (inside [-63,63], 256-decoration limit, y_rot 180); `add_frame`/`removed_from_frame`/`item_frame_y_rotation` cover item frames; `add_target_decoration` covers compass/lodestone markers. The server-set decoration kinds (player variants, frame, 16 banner colours, target) are exactly those `MapItemSavedData` computes — structure/explorer-map icons come from item NBT, not server logic. **Fixed a real cadence bug:** decoration flushes now advance the per-view tick counter only while `dirty_decorations` is set (Java `dirtyDecorations && tick++ % 5 == 0` short-circuit), not unconditionally, so the every-5-ticks send phase matches vanilla.
- [x] Implement waypoints and timelines introduced in this version. — **waypoints:** `waypoint::WaypointManager` is 1:1 with `ServerWaypointManager` (`track_waypoint`/`add_player`/`remove_player`/`update_waypoint`/`break_all_connections`/`create_connection`), and the connection logic matches `WaypointTransmitter`: `make_connection_kind` (`makeWaypointConnectionWith`: self/first-tick/ignore→none, really-far→Azimuth, !chunk-visible→Chunk, else→Block), `does_source_ignore_receiver`, and `connection_is_broken`/`connection_changed` for all three connection types — fixing the chunk-visibility computation (was hardcoded), the Chunk/Azimuth `isBroken` re-checks (chunk-visible / no-longer-really-far), and the broken-reconnect packet sequence (no spurious untrack). **timelines:** `environment_attributes.rs` ports the server-relevant `Timelines.java` tracks verbatim — `sky_light_level` (keyframes `133→1.0, 11867→1.0, 13670→0.26666668, 22330→0.26666668`), `star_brightness`, `sky_angle`, and the night-boolean tracks `monsters_burn`/`bees_stay_in_hive`/`eyeblossom_open`/`creaking_active`/`firefly_bush_sounds_active` (12600 true → 23401 false); the visual color tracks (fog/sky/sunrise-sunset colour) are client-side rendering. Covered by the `waypoint` and `environment_attributes` test suites.
- [x] Implement dialogs, server links, notifications, and code-of-conduct flow. — **dialogs:** `dialog_types()` lists the 5 `DialogTypes` (notice/server_links/dialog_list/multi_action/confirmation) and `dialog_body_types()` the 2 body types (plain_message/item); `show_dialog`/`clear_dialog`/`start_configuration_extras` model the configuration-phase dialog flow. **server links:** `known_link_display_key` maps all 10 `ServerLinks.KnownLinkType` ids (0-9, e.g. `BUG_REPORT`→`known_server_link.report_bug`); `untrust_server_links` matches the untrust path. **code-of-conduct:** `select_code_of_conduct` is 1:1 with `ServerConfigurationPacketListenerImpl` (`get(lang.toLowerCase()).or(get("en_us")).or(values().next())`, empty→None). **notifications:** `NotificationEvent` (17 variants) is 1:1 with the 17 `NotificationService` interface methods (`JsonRpcNotificationService`); `NotificationManager.notify` models Java's `forEach(s -> s.method())` broadcast (each registered service records every event); `ServerActivityMonitor` is a 1:1 port of `net.minecraft.server.notifications.ServerActivityMonitor` (rate-limited `serverActivityOccured` with `minimumMillisBetweenNotifications`, `pending_activity`/`last_notification_time`). Covered by `cargo test -q dialog_system` (6 tests). (`Util.getMillis()` is supplied as a `now_millis` parameter for testability; logic is identical.)
