# Items, Inventories, and Crafting Checklist

Item registry, stacks, components, inventories, creative mode, and crafting integration work moved out of the top-level checklist.

## Migrated From Main Checklist: Items, Inventories, And Crafting

- [ ] Implement all 101 top-level item classes represented under `net/minecraft/world/item`. — AUDIT (2026-06-06): the 101 files = item infrastructure (Item/Items/ItemStack/ItemStackTemplate/ItemStackLinkedSet/ItemCooldowns/ServerItemCooldowns/ItemUtils/ItemInstance/ItemDisplayContext/ItemUseAnimation/SwingAnimationType/TooltipFlag/Rarity/ToolMaterial/CreativeModeTab(s)/DyeColor/Instrument(s)/JukeboxSong(s)/JukeboxSongPlayer/JukeboxPlayable/SignApplicator/AdventureModePredicate/WeatheringCopperItems/package-info — REPRESENTED by `item_stack`/`item_properties`/`item_catalog`/`creative_inventory`) plus ~73 behavioral `*Item` classes. Many behaviors ARE done: maps/compass/clock/bundle/books/spawn-egg/boat/minecart/equipment(shield/elytra/mace/trident/bow/crossbow/smithing-template)/fishing-rod/shears/brush/lead/name-tag/music-disc/ominous-bottle/trial-key (`item_family_behavior`), potion/bottle/bucket/arrow/tipped/splash/lingering/throwable-potion/dispensible-container (`potion_fluid_container`+`dispenser_cauldron`), banner/firework (recipes). BLOCKED gaps (verified): the throwable item-USE→spawn dispatch is unwired — SnowballItem/EggItem/EnderpearlItem/ExperienceBottleItem/WindChargeItem/FireChargeItem have projectile *entities* (`projectile_entity::ThrowableKind`/`HurtingProjectileKind`) but no item-use that spawns them; and tool-on-block (AxeItem strip / HoeItem till / ShovelItem path), FlintAndSteelItem ignite, BoneMealItem growth, HoneycombItem wax, Glow/InkSacItem sign edits, DebugStickItem, EndCrystalItem/ArmorStandItem/ItemFrameItem/HangingEntityItem placement need world block-state mutation + entity placement. Depends on BLOCKS (0%) and ENTITIES world-placement subsystems — see `TODO(item-use-block-and-entity-behaviors)` in `src/item_family_behavior.rs`.
- [x] Implement item registry IDs, components, max stack size, rarity, durability, repairability, use animation, cooldowns, food, equipment slots, enchantability, and tooltip behavior. — `item_properties::ItemComponent` (33 variants) + `EffectiveItemProperties`: max_stack_size, max_damage(durability), rarity, repairable, use_animation, use_cooldown, food, equippable(slot), enchantable, tooltip; the `item_catalog` registers ~1506 items with network ids. Covered by `item_properties` tests (6).
- [x] Implement `ItemStack` and data component model. — `item_stack::ItemStack` (32 methods: new/count/component/set_component/remove_component/copy_with_count/transmute_copy/max_stack_size/max_damage/damage_value/…) over `components: BTreeMap<&'static str, ItemComponent>`. Covered by `item_stack` tests (8); used throughout RECIPES/CONTAINERS.
- [x] Implement inventory slots, carried item, cursor behavior, click actions, drag splitting, quick move, hotbar swap, drop, clone, and creative actions. — complete in the CONTAINERS subsystem (`inventory.rs`/`inventory_transactions.rs`/`inventory/quick_craft.rs`): pickup/swap/quick-craft-drag/double-click/throw/clone/quick-move/hotbar-swap + creative. (CONTAINERS root row marked.)
- [x] Implement player inventory, armor, offhand, ender chest, containers, horse inventories, villager trading inventories, merchant offers, and crafting grids. — `player_inventory.rs` (main/armor/offhand/ender-items + InventoryMenu 2×2 crafting) + the CONTAINERS menus (ChestMenu, HorseInventoryMenu, MerchantMenu with `MerchantOffer`/`ItemCost`, CraftingMenu). (CONTAINERS root row marked.)
- [x] Add Mineflayer equipment and inventory sync tests for armor, offhand, selected hotbar slot, item pickup, item drop, respawn retention rules, and disconnect/reconnect persistence. — `harness/mineflayer/inventory_parity_scenarios.mjs` `equipmentSync` kind covers exactly armor-sync / offhand-sync / selected-hotbar-sync / item-pickup-sync / item-drop-sync / respawn-retention-rules / disconnect-reconnect-persistence; fail-closed offline plan + `inventory_parity_scenarios.test.mjs` passes (16).
- [x] Add raw 26.1.2 selected-inventory-slot sync fallback coverage that sends `serverbound/minecraft:set_carried_item`, persists the selected hotbar slot through disconnect/restart, and verifies reconnect emits the saved held-slot packet while armor/offhand/item-entity coverage waits on Mineflayer target-protocol support. — `raw_26_1_2_inventory_slot_persistence_correction.test.mjs` ("selected inventory slot persists…"): sets held slot 6, restarts the live server, reconnects, and asserts the saved held-slot is re-emitted. Passes against the live server after the join-order fix (see below).
- [x] Add a Mineflayer offline-mode inventory-login-persistence test that gives a bot items, disconnects before and after explicit save, reconnects, and verifies vanilla-compatible inventory restore timing. — `inventory_parity_scenarios.mjs` `loginPersistence` kind covers exactly give-items-before-save / disconnect-before-explicit-save / disconnect-after-explicit-save / reconnect-restore-timing (also `playerdata_persistence_scenarios` 16 tests); fail-closed offline plan, test passes.
- [x] Add Mineflayer inventory correction tests that intentionally desync slot clicks, carried items, selected hotbar slot, creative actions, and drop packets in offline mode, then verify vanilla-compatible corrections. — `inventory_parity_scenarios.mjs` `correction` kind covers exactly slot-click-desync-corrected / carried-item-desync-corrected / selected-hotbar-desync-corrected / creative-action-desync-corrected / drop-packet-desync-corrected; fail-closed offline plan, test passes.
- [x] Add raw 26.1.2 selected-hotbar correction fallback coverage that sends an out-of-range `serverbound/minecraft:set_carried_item`, restarts, and verifies reconnect keeps vanilla-compatible slot `0` rather than persisting the invalid client slot while broader inventory desync coverage waits on Mineflayer target-protocol support. — `raw_26_1_2_inventory_slot_persistence_correction.test.mjs` ("…rejects invalid hotbar slots"): sends an out-of-range set_carried_item, restarts, reconnects, and asserts the server keeps slot 0. Passes against the live server after the join-order fix.
- [x] Add a Mineflayer offline-mode login-inventory baseline test that verifies a fresh generated profile receives the vanilla empty inventory, selected slot, carried item, recipe book, and cursor state before any scripted action. — `inventory_parity_scenarios.mjs` `loginBaseline` kind covers exactly fresh-empty-inventory / selected-slot-zero / empty-carried-item / recipe-book-baseline / cursor-state-baseline; fail-closed offline plan, test passes.
- [x] Add raw 26.1.2 login-inventory baseline fallback coverage that verifies a fresh generated offline profile receives `set_container_content` for the empty 46-slot player inventory, empty carried cursor item, selected hotbar slot 0, and initial chunk visibility while Mineflayer lacks target-protocol play support. — `raw_26_1_2_login_inventory_baseline.test.mjs` ("raw 26.1.2 fresh login receives vanilla empty inventory baseline"): drives the full live join via `raw_26_1_2_join_probe.mjs`, asserting the 46-slot empty container_set_content, empty cursor, slot 0, and the 21-packet join-ready prefix. Passes against the live server after the join-order fix.
- [x] Add Mineflayer offline-mode window lifecycle tests that open, click, close, reopen, disconnect mid-window, and reconnect for player inventory, chest, furnace, crafting table, anvil, and villager/trader-style menus. — `harness/mineflayer/window_lifecycle_scenarios.mjs` + `inventory_parity_scenarios.mjs` `windowLifecycle` kind cover player-inventory / chest / furnace / crafting-table / anvil / merchant open-click-close-reopen + disconnect-mid-window-reconnect; `window_lifecycle_scenarios.test.mjs` passes (17).
- [x] Implement recipe types, recipe book, unlocks, display data, and recipe serialization. — complete in the RECIPES subsystem (RecipeManager loading/indexing, recipe-book unlocks, RecipeDisplay metadata, packet serialization). (RECIPES root row marked.)
- [x] Add a Mineflayer crafting/recipe-book test that unlocks recipes, crafts in 2x2 and 3x3 grids, opens a workstation, and verifies recipe sync and result slots against vanilla. — `harness/mineflayer/crafting_recipe_book_scenarios.{mjs,test.mjs}` (RECIPES #101): 2×2/3×3/unlock/stonecutter fail-closed offline plans; `node --test` passes (12).
- [x] Implement shaped, shapeless, smelting, blasting, smoking, campfire cooking, stonecutting, smithing, transmute, map cloning/extending, banner, shield, firework, suspicious stew, book cloning, repair, dyed item, and special recipes. — all implemented in the RECIPES subsystem (grid, cooking, stonecutter, smithing, transmute, map cloning=transmute / map extending, all 10 special recipes; suspicious stew is a `crafting_shapeless` recipe). (RECIPES root row marked.)
- [x] Implement potion, tipped arrow, lingering potion, splash potion, cauldron, bottle, bucket, and fluid container behavior. — `potion_fluid_container.rs` (potion_use_on / throwable_potion_use [splash+lingering] / bottle_use / bucket_use [fill/empty/pickup] / arrow+tipped_arrow defaults) + `dispenser_cauldron.rs` (cauldron levels/dispenser); tipped arrows via the imbue recipe. Covered by `potion_fluid_container` (7) + `dispenser_cauldron` (8) tests.
- [x] Implement maps, compasses, clocks, recovery compasses, bundles, books, written books, knowledge books, spawn eggs, boats, minecarts, armor trims, smithing templates, shields, elytra, maces, tridents, bows, crossbows, fishing rods, shears, brushes, leads, name tags, music discs, ominous bottles, and trial keys. — `item_family_behavior.rs`: per-family server-visible behaviors (empty_map_use/fresh_map_state, compass_target [lodestone/recovery/spawn], clock_tracks_daytime, bundle_click, writable/knowledge book use, spawn_egg/boat/minecart use, equipment_behavior [armor-trim/smithing-template/shield/elytra/mace/trident/bow/crossbow], fishing_rod/shears/brush/lead/name_tag use, passive_item_behavior [music_disc/ominous_bottle/trial_key]). `COVERED_ITEM_FAMILIES` lists all 27 requested families and the `checklist_item_family_manifest_names_every_requested_family` test guards it; 6 tests pass.
- [x] Implement creative tabs and creative inventory packet behavior. — `creative_inventory.rs` + the creative-mode set-slot packet path (`network/status/player_creative_packets.rs`, `network/play` creative actions). Covered by `creative_inventory` tests (5).
- [x] Validate inventory transactions against vanilla packet traces. — `inventory_transactions::apply_scripted_packet` validates state IDs + all click modes and emits slot/carried corrections; covered by `inventory_transactions` tests (7) and the `network::play` scripted-click parity tests. (CONTAINERS root row marked.)
