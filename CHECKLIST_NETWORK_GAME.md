# Game Protocol Packet Checklist

## Relevant Java Source Files

- `decompiled-server-26.1.2/net/minecraft/network/protocol/game/` — all 182 game-state packets
- `decompiled-server-26.1.2/net/minecraft/network/protocol/game/ClientboundLoginPacket.java` — play login
- `decompiled-server-26.1.2/net/minecraft/network/protocol/game/ClientboundRespawnPacket.java` — respawn
- `decompiled-server-26.1.2/net/minecraft/network/protocol/game/ClientboundLevelChunkWithLightPacket.java` — chunk + light
- `decompiled-server-26.1.2/net/minecraft/network/protocol/game/ClientboundPlayerPositionPacket.java` — position sync
- `decompiled-server-26.1.2/net/minecraft/network/protocol/game/ServerboundMovePlayerPacket.java` — player movement
- `decompiled-server-26.1.2/net/minecraft/network/protocol/game/ClientboundSetEntityDataPacket.java` — entity metadata
- `decompiled-server-26.1.2/net/minecraft/network/protocol/game/ClientboundContainerSetContentPacket.java` — inventory sync
- `decompiled-server-26.1.2/net/minecraft/network/protocol/game/ClientboundPlayerChatPacket.java` — signed chat
- `decompiled-server-26.1.2/net/minecraft/network/protocol/game/ClientboundBossEventPacket.java` — bossbar
- `decompiled-server-26.1.2/net/minecraft/network/protocol/game/ClientboundCommandsPacket.java` — command tree
- `decompiled-server-26.1.2/net/minecraft/network/codec/StreamCodec.java` — network codec base
- `RustCraft/src/network/play.rs` — RustCraft play state packet handling
- `RustCraft/src/network/protocol_coverage.rs` — RustCraft protocol coverage tracking

## Coverage Requirement

All 182 packets in `net/minecraft/network/protocol/game/` must be implemented. For each:
- Direction (clientbound or serverbound) and packet ID must match vanilla 26.1.2
- All fields must be serialized/deserialized in vanilla field order
- Optional fields and codec variants must match decompiled codec
- Malformed input must disconnect with vanilla-compatible reason

## Join / Respawn / World State Packets

- [x] `ClientboundLoginPacket` (0x2B / decimal 43): entity ID, is-hardcore, levels, max-players, chunk-radius, simulation-distance, reduced-debug-info, enable-respawn-screen, do-limited-crafting, common player spawn info (dimension type, dimension, seed, game-type, previous-game-type, debug/flat flags, last death location, portal cooldown, sea level), and enforces-secure-chat; verified against Java read/write codec, `CommonPlayerSpawnInfo` codec, Java registration/name mapping, Rust packet ID/registry mapping, focused field-order coverage, and live login/respawn writer reuse of the canonical packet codecs including packed `GlobalPos` last-death encoding.
- [x] `ClientboundRespawnPacket` (0x52 / decimal 82): common player spawn info followed by signed byte `dataToKeep`; verified against Java read/write codec, shared `CommonPlayerSpawnInfo` codec, Java registration/name mapping, Rust packet ID/registry mapping, focused field-order coverage, and exact Java flag bitmasks (`KEEP_ATTRIBUTE_MODIFIERS = 1`, `KEEP_ENTITY_DATA = 2`, `KEEP_ALL_DATA = 3`).
- [x] `ClientboundChangeDifficultyPacket` (0x0A / decimal 10): difficulty VarInt enum, difficulty-locked bool; verified field order against Java `Difficulty.STREAM_CODEC` and Rust packet round-trip coverage.
- [x] `ClientboundSetDefaultSpawnPositionPacket` (0x61 / decimal 97): `LevelData.RespawnData` (`GlobalPos` dimension + `BlockPos`, yaw float, pitch float); verified field order against Java `LevelData.RespawnData.STREAM_CODEC` and live raw join.
- [x] `ClientboundSetTimePacket` (0x71 / decimal 113): game-time long plus world-clock update map; verified 26.1.2 field order against Java, Rust packet round-trip coverage, and live raw join.
- [x] `ClientboundGameEventPacket` (0x26 / decimal 38): event ID unsigned byte, param float; verified against Java `ClientboundGameEventPacket` `Packet.codec` read/write order, Java play-protocol registration order, all Java event IDs 0 through 13 (`NO_RESPAWN_BLOCK_AVAILABLE` through `LEVEL_CHUNKS_LOAD_START`), Rust big-endian float byte layout, unknown event ID preservation matching Java map lookup behavior, and malformed truncated/trailing-payload rejection.

## Chunk and Light Packets

- [x] `ClientboundLevelChunkWithLightPacket` (0x2D / decimal 45): fixed-int chunk X/Z, chunk data (heightmaps map, section buffer capped by Java's two-megabyte guard, block entities as packed XZ/y/type/nullable NBT), then light data (sky/block light set/empty bitsets, sky/block 2048-byte arrays); verified against Java read/write order, Java play registration/name mapping, Rust packet ID/registry mapping, focused chunk-with-light byte-layout coverage, dense overworld section padding, and shared light-update payload coverage.
- [x] `ClientboundForgetLevelChunkPacket` (0x25 / decimal 37): `ChunkPos` encoded with Java `FriendlyByteBuf.writeChunkPos` packed long (`x & 0xffffffff | z << 32`); verified against Java `ClientboundForgetLevelChunkPacket` `Packet.codec` read/write order, Java play-protocol registration order, Rust framed packet byte layout for positive/negative chunk coordinates, and `PlayerChunkSender.dropChunk` parity for suppressing forgets for pending/closed-player chunks while emitting forgets for already-sent live chunks.
- [x] `ClientboundLightUpdatePacket` (0x30 / decimal 48): VarInt chunk X/Z followed by `ClientboundLightUpdatePacketData` (`readBitSet`/`writeBitSet` sky/block/empty masks plus `ByteBufCodecs.byteArray(2048)` sky/block update lists); verified against Java read/write order, Java play registration/name mapping, Rust packet ID/registry mapping, focused VarInt coordinate byte-layout coverage, and existing chunk-with-light reuse tests.
- [x] `ClientboundChunkBatchStartPacket` (0x0C / decimal 12): no fields; verified against Java `StreamCodec.unit`, Rust no-payload frame coverage, and live raw join.
- [x] `ClientboundChunkBatchFinishedPacket` (0x0B / decimal 11): batch-size VarInt; verified field order against Java, Rust packet round-trip coverage, and live raw join.
- [x] `ServerboundChunkBatchReceivedPacket` (0x0B / decimal 11): desired-chunks-per-tick float; verified against Java `ServerboundChunkBatchReceivedPacket` `Packet.codec` read/write order, Java play-protocol registration order, Rust big-endian float byte layout, adaptive `PlayerChunkSender` feedback clamping/unacknowledged-batch behavior, and malformed truncated/trailing-payload rejection through direct decode and play-session dispatch.
- [x] `ClientboundSetChunkCacheCenterPacket` (0x5E / decimal 94): chunk X/Z VarInts; verified field order against Java and live raw join.
- [x] `ClientboundSetChunkCacheRadiusPacket` (0x5F / decimal 95): view-distance VarInt; verified field order against Java and live raw join.
- [x] `ClientboundSetSimulationDistancePacket` (0x6F / decimal 111): simulation-distance VarInt; verified field order against Java and Rust packet round-trip coverage.
- [x] `ClientboundMapItemDataPacket` (0x33 / decimal 51): `MapId` VarInt, scale byte, locked bool, optional decoration list (`MapDecorationType` holder VarInt, x/y/rotation bytes, optional component tag), and optional color patch encoded as width byte `0` when absent or width/height/startX/startY plus byte array when present; verified against Java composite codec, Java `MapDecoration` rotation mask, Java `MapPatch` optional codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for present and absent optional payloads.

## Entity Lifecycle Packets

- [x] `ClientboundAddEntityPacket` (0x01 / decimal 1): entity ID VarInt, UUID, entity type encoded through Java `ByteBufCodecs.registry(Registries.ENTITY_TYPE)`, position X/Y/Z doubles, movement via `Vec3.LP_STREAM_CODEC`, packed X/Y/head rotations using Java `Mth.packDegrees`, and trailing data VarInt; verified against Java packet read/write order, Java play registration/name mapping, Java `ByteBufCodecs.registry` direct registry-id encoding, Java `LpVec3` zero/non-zero/continuation encoding, Rust packet ID/registry mapping, and focused byte-layout coverage for non-zero and continuation movement payloads.
- [x] `ClientboundAddExperienceOrbPacket`: not present in 26.1.2 Java `GamePacketTypes` or `net/minecraft/network/protocol/game/`; experience orbs spawn through `ClientboundAddEntityPacket`; verified by Java packet-type/class search, Java clientbound play-protocol registration, Rust registry absence of `add_experience_orb`, and Rust registry presence of `add_entity`.
- [x] `ClientboundRemoveEntitiesPacket` (0x4D / decimal 77): entity ID list encoded with Java `FriendlyByteBuf.writeIntIdList` (VarInt length followed by VarInt entity IDs); verified against Java `ClientboundRemoveEntitiesPacket` `Packet.codec` read/write order, Java play-protocol registration order, Rust packet registry ID/name mapping, direct VarInt list byte layout including multibyte entity IDs, and generated/item-entity removal framed-packet coverage.
- [x] `ClientboundSetEntityMotionPacket` (0x65 / decimal 101): entity ID VarInt followed by velocity encoded with Java `Vec3.LP_STREAM_CODEC`; verified against Java `ClientboundSetEntityMotionPacket` composite codec and entity constructor, Java `LpVec3.write` NaN/ABS_MAX sanitization rather than legacy +/-3.9 pre-clamping, Java play-protocol registration order, Rust packet registry ID/name mapping, and direct payload bytes for multibyte entity IDs plus motion values above 3.9 blocks/tick.
- [x] `ClientboundTeleportEntityPacket` (0x7D / decimal 125): entity ID VarInt, `PositionMoveRotation.STREAM_CODEC` (position `Vec3.STREAM_CODEC`, delta movement `Vec3.STREAM_CODEC`, yaw float, pitch float), Java `Relative.SET_STREAM_CODEC` fixed-int bitmask, and on-ground bool; verified against Java `ClientboundTeleportEntityPacket` composite codec, Java `PositionMoveRotation` and `Relative` bit order (`X` through `ROTATE_DELTA`), Java play-protocol registration order, Rust packet registry ID/name mapping, and direct payload bytes covering multibyte entity IDs, signed position/movement/rotation values, all relative bits, and false on-ground.
- [x] `ClientboundRotateHeadPacket` (0x53 / decimal 83): entity ID VarInt followed by Java `Mth.packDegrees` head-yaw byte; verified against Java `ClientboundRotateHeadPacket` `Packet.codec` read/write order, Java `ServerEntity` head-rotation send gate, Java play-protocol registration order, Rust packet registry ID/name mapping, and direct payload bytes for a multibyte entity ID plus packed 180-degree yaw.
- [x] `ClientboundMoveEntityPacket.Pos` (0x35 / decimal 53): entity ID VarInt, delta X/Y/Z signed big-endian shorts, on-ground bool; verified against Java nested `ClientboundMoveEntityPacket.Pos` `Packet.codec` read/write order, Java play-protocol registration order, Rust packet registry ID/name mapping, and direct payload bytes covering multibyte entity IDs plus signed short edge values.
- [x] `ClientboundMoveEntityPacket.PosRot` (0x36 / decimal 54): entity ID VarInt, delta X/Y/Z signed big-endian shorts, yaw/pitch bytes, on-ground bool; verified against Java nested `ClientboundMoveEntityPacket.PosRot` `Packet.codec` read/write order, Java `Mth.packDegrees` rotation-byte packing, Java play-protocol registration order, Rust packet registry ID/name mapping, and direct payload bytes covering multibyte entity IDs, signed deltas, and negative packed pitch.
- [x] `ClientboundMoveEntityPacket.Rot` (0x38 / decimal 56): entity ID VarInt, yaw/pitch bytes, on-ground bool; verified against Java nested `ClientboundMoveEntityPacket.Rot` `Packet.codec` read/write order, Java `Mth.packDegrees` rotation-byte packing, Java play-protocol registration order, Rust packet registry ID/name mapping, and direct payload bytes covering multibyte entity IDs plus positive and negative packed rotations.
- [x] `ClientboundMoveVehiclePacket` (0x39 / decimal 57): position `Vec3.STREAM_CODEC` (`x`, `y`, `z` doubles) followed by yaw float and pitch float; verified against Java `ClientboundMoveVehiclePacket` composite codec/from-entity field order, Java `Vec3.STREAM_CODEC`, Java play-protocol registration order, Rust packet registry ID/name mapping, and direct payload bytes including signed float coverage.
- [x] `ClientboundSetPassengersPacket` (0x6B / decimal 107): vehicle entity ID VarInt followed by passenger entity ID VarInt array (`FriendlyByteBuf.writeVarIntArray`); verified against Java `ClientboundSetPassengersPacket` `Packet.codec` read/write order, Java passenger list construction order from `vehicle.getPassengers()`, Java play-protocol registration order, Rust packet registry ID/name mapping, and direct payload bytes for multibyte vehicle/passenger IDs.
- [x] `ClientboundEntityEventPacket` (0x22 / decimal 34): fixed-int entity ID followed by event ID byte (living entity events such as `2` hurt, `3` death, `6` tame-fail, `7` tame-success); verified against Java `ClientboundEntityEventPacket` `Packet.codec` read/write order, Java play-protocol registration order, Rust packet registry ID/name mapping, and direct big-endian fixed-int plus event-byte payload coverage.

## Entity Metadata Packets

- [x] `ClientboundSetEntityDataPacket` (0x63 / decimal 99): entity ID VarInt followed by Java `SynchedEntityData.DataValue` entries (`index` unsigned byte, serializer ID VarInt, serializer payload) terminated by EOF byte `0xff`; verified against Java packet read/write order, Java `SynchedEntityData.DataValue.write/read`, Java metadata index cap reserving `255` for EOF, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for empty lists, multiple typed items, highest legal index `254`, raw serializer payloads, and EOF-index rejection.
- [ ] Implement all 26.1.2 entity metadata types: byte, varint, varlong, float, string, chat component, optional chat, item stack, boolean, rotations, block pos, optional block pos, direction, optional UUID, optional block state ID, optional global pos, nbt, particle, particle list, villager data, optional varint, pose, cat variant, wolf variant, frog variant, optional global position, painting variant, sniffer state, armadillo state, vector3f, quaternionf
- [ ] Implement entity metadata indexes for every entity type hierarchy (Entity, LivingEntity, Mob, PathfinderMob, Animal, Player, each monster/animal subtype)
- [x] `ClientboundUpdateAttributesPacket` (0x83 / decimal 131): entity ID VarInt followed by Java `AttributeSnapshot` list entries (attribute registry holder VarInt, base double, modifier list with modifier identifier, amount double, and operation VarInt IDs `0` add-value, `1` add-multiplied-base, `2` add-multiplied-total); verified against Java composite codec, Java `AttributeModifier.Operation.STREAM_CODEC`, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for multiple attributes plus present/empty modifier lists.
- [x] `ClientboundUpdateMobEffectPacket` (0x84 / decimal 132): entity ID VarInt, `MobEffect.STREAM_CODEC` registry-holder VarInt, amplifier VarInt, duration VarInt, and flags byte (`ambient=1`, `visible=2`, `show_icon=4`, `blend=8`); verified against Java read/write order, flag constants/getters, play registration/name mapping, Rust packet ID/registry mapping, direct VarInt byte-layout coverage, and focused flag-composition tests.
- [x] `ClientboundRemoveMobEffectPacket` (0x4E / decimal 78): entity ID VarInt followed by `MobEffect.STREAM_CODEC` registry holder VarInt; verified against Java `ClientboundRemoveMobEffectPacket` composite codec, Java `MobEffect.STREAM_CODEC` holder-registry codec, Java play-protocol registration order, Rust packet registry ID/name mapping, and direct payload bytes for multibyte entity/effect IDs.
- [x] `ClientboundAnimatePacket` (0x02 / decimal 2): entity ID VarInt followed by animation unsigned byte (`0` swing-main, `2` wake-up, `3` swing-offhand, `4` critical-hit, `5` magic-critical-hit); verified against Java `ClientboundAnimatePacket` `Packet.codec` read/write order, Java action constants, Java play-protocol registration order, Rust packet registry ID/name mapping, and direct payload bytes for a multibyte entity ID plus unsigned action byte.
- [x] `ClientboundSetEquipmentPacket` (0x66 / decimal 102): entity ID VarInt followed by one or more `EquipmentSlot.ordinal()` bytes with the `0x80` continuation bit and `ItemStack.OPTIONAL_STREAM_CODEC` payloads; verified against Java read/write order, Java slot enum ordinal order, play registration/name mapping, Rust packet ID/registry mapping, direct continuation-byte layout coverage, and existing trusted optional item-stack packet coverage.

## Inventory / Container Packets (Clientbound)

- [x] `ClientboundContainerSetContentPacket` (0x12 / decimal 18): container ID uses Java `ByteBufCodecs.CONTAINER_ID`/`FriendlyByteBuf.readContainerId` VarInt encoding, state ID is VarInt, slots are a VarInt-counted `ItemStack.OPTIONAL_LIST_STREAM_CODEC`, and carried item uses `ItemStack.OPTIONAL_STREAM_CODEC`; verified against the Java composite codec, Java play registration/name mapping, Rust packet ID/registry mapping, existing trusted optional item-stack coverage, and focused byte-layout coverage for multibyte container/state IDs with filled, empty, and carried stacks.
- [x] `ClientboundContainerSetSlotPacket` (0x14 / decimal 20): container ID encoded with Java `FriendlyByteBuf.readContainerId`/`writeContainerId` VarInt, state ID VarInt, signed big-endian slot short, and `ItemStack.OPTIONAL_STREAM_CODEC` trusted item payload; verified against Java read/write codec, Java play registration/name mapping, Rust packet ID/registry mapping, existing trusted optional item-stack coverage, and focused byte-layout coverage for multibyte container/state IDs plus a negative slot.
- [x] `ClientboundContainerSetDataPacket` (0x13 / decimal 19): container ID encoded with Java `FriendlyByteBuf.readContainerId`/`writeContainerId` VarInt, followed by signed big-endian property short and value short; verified against Java read/write codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for a multibyte container ID plus signed short payloads.
- [x] `ClientboundOpenScreenPacket` (0x3B / decimal 59): container ID uses Java `ByteBufCodecs.CONTAINER_ID`/`FriendlyByteBuf.readContainerId` VarInt encoding, menu type uses `ByteBufCodecs.registry(Registries.MENU)` VarInt registry IDs, and title uses `ComponentSerialization.TRUSTED_STREAM_CODEC` as a trusted network NBT component; verified against the Java composite codec, Java play registration/name mapping, Rust packet ID/registry mapping, Java `MenuType` registration order, and focused byte-layout coverage for multibyte container IDs plus trusted component NBT.
- [x] `ClientboundContainerClosePacket` (0x11 / decimal 17): container ID encoded with Java `FriendlyByteBuf.readContainerId`/`writeContainerId` VarInt; verified against Java read/write codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for multibyte container IDs.
- [x] `ClientboundMountScreenOpenPacket` (0x29 / decimal 41, replaces older HorseScreenOpen): container ID encoded with Java `FriendlyByteBuf.readContainerId`/`writeContainerId` VarInt, inventory columns VarInt, and entity ID fixed big-endian int; verified against Java read/write codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for multibyte VarInts plus signed fixed-int entity IDs.
- [x] `ClientboundMerchantOffersPacket` (0x34 / decimal 52): container ID uses Java `readContainerId`/`writeContainerId` VarInt encoding, offers use `MerchantOffers.STREAM_CODEC` as a VarInt-counted `MerchantOffer.STREAM_CODEC` list, each offer writes `ItemCost.STREAM_CODEC`, required `ItemStack.STREAM_CODEC`, optional `ItemCost`, out-of-stock bool, fixed big-endian uses/maxUses/xp/specialPrice/priceMultiplier/demand fields, then villager level/xp VarInts and show-progress/can-restock bools; verified against Java packet read/write, `MerchantOffer`/`MerchantOffers`/`ItemCost` stream codecs, Java play registration/name mapping, Rust packet ID/registry mapping, and focused full-payload byte coverage with optional second cost and negative special price.
- [x] `ClientboundSetHeldSlotPacket` (0x69 / decimal 105): slot VarInt; verified field order against Java, Rust packet round-trip coverage, and live raw join.
- [x] `ClientboundCooldownPacket` (0x16 / decimal 22): cooldown group `Identifier` followed by duration VarInt; verified against Java composite codec (`Identifier.STREAM_CODEC`, `ByteBufCodecs.VAR_INT`), Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage including a multibyte VarInt duration.
- [x] `ClientboundSetCursorItemPacket` (0x4F): cursor item encoded with Java `ItemStack.OPTIONAL_STREAM_CODEC`; verified against Java `ClientboundSetCursorItemPacket` composite codec, Java play-protocol registration order, Rust packet registry ID/name mapping, trusted/raw data-component patch byte layout, and live inventory-click emission through `PlayInstruction::SetCursorItem`.

## Inventory / Container Packets (Serverbound)

- [x] `ServerboundContainerClickPacket` (0x12 / decimal 18): container ID via Java `ByteBufCodecs.CONTAINER_ID`, state ID VarInt, slot short, button byte, `ContainerInput.STREAM_CODEC` VarInt id mapper with out-of-range values mapped to `PICKUP`, changed-slots map capped at 128 entries with short slot keys and `HashedStack.STREAM_CODEC` values, and carried `HashedStack`; verified against Java composite codec, Java `ByteBufCodecs.map` count cap, Java `HashedStack` optional/actual item codec, Java `ContainerInput` by-id zero fallback, Java play registration/name mapping, Rust packet ID/registry mapping, focused read/write byte-layout coverage, cap rejection, dispatch coverage, and inventory transaction regression tests.
- [x] `ServerboundContainerClosePacket` (0x13 / decimal 19): container ID via Java `FriendlyByteBuf.readContainerId`/`writeContainerId` VarInt; verified against Java codec/registration/name mapping, Java `handleContainerClose` behavior that ignores the packet ID and closes the active container, Rust packet ID/registry mapping, focused read/write/dispatch coverage, and Rust close handling that returns the cursor stack before save even for nonzero container IDs.
- [x] `ServerboundSetCarriedItemPacket` (0x35 / decimal 53): selected hotbar slot signed big-endian short; verified against Java `readShort`/`writeShort` codec, Java registration/name mapping, Java handler valid range `0..Inventory.getSelectionSize()` with invalid slots ignored/logged rather than disconnected, Rust packet ID/registry mapping, focused read/write/dispatch coverage, and Rust play-state selected-slot application tests.
- [x] `ServerboundPickItemFromBlockPacket` (0x24 / decimal 36): block pos + include-data bool; `ServerboundPickItemFromEntityPacket` (0x25 / decimal 37): entity ID VarInt + include-data bool; verified against Java composite codecs, Java play registration/name mapping, Java range/loaded-chunk/infinite-materials pick-block handling, Rust packet ID/registry mapping, codec/dispatch tests, and live status handler coverage for creative middle-click packets. Block-entity component copying is deferred until live block-entity data is exposed to this path.
- [x] `ServerboundEditBookPacket` (0x18 / decimal 24): slot VarInt, pages list capped at 100 entries with 1024-char UTF-8 pages, optional 32-char title; verified against Java composite codec, Java play registration/name mapping, Java hotbar/offhand and writable-book component handler gates, Rust packet ID/registry mapping, focused read/write/malformed-bound coverage, live status handler coverage, and writable/written book component network payload tests.
- [x] `ServerboundRenameItemPacket` (0x30 / decimal 48): item name UTF-8 string using Java default `FriendlyByteBuf.readUtf()`/`writeUtf()` 32,767-character cap; verified against Java codec/registration/name mapping, Java anvil-menu handler gate, Rust packet ID/registry mapping, focused read/write/dispatch coverage, and explicit over-limit read/write rejection tests.
- [x] `ServerboundSelectTradePacket` (0x33 / decimal 51): selected merchant offer index raw VarInt; verified against Java `readVarInt`/`writeVarInt` codec, Java registration/name mapping, Java merchant-menu handler gate and range handling in `MerchantMenu.tryMoveItems`, Rust packet ID/registry mapping, and focused read/write/dispatch coverage.
- [x] `ServerboundSetBeaconPacket` (0x34 / decimal 52): primary and secondary optional MobEffect holder registry IDs; verified against Java `MobEffect.STREAM_CODEC.apply(ByteBufCodecs::optional)`, `ByteBufCodecs.optional` bool framing, `holderRegistry` VarInt `byIdOrThrow` validation, Java play registration/name mapping, Rust protocol registry mapping, and focused read/write/dispatch tests for present, absent, and invalid effect IDs.
- [x] `ServerboundSetCreativeModeSlotPacket` (0x38): slot short, `ItemStack.OPTIONAL_UNTRUSTED_STREAM_CODEC` with count VarInt, optional item registry ID, and delimited data component patch framing — decoded and applied in creative mode with Java slot, count, and ability gates; creative drop remains deferred until entity item drops are ported.
- [x] `ServerboundContainerButtonClickPacket` (0x11 / decimal 17): container ID via Java `ByteBufCodecs.CONTAINER_ID`/`FriendlyByteBuf.readContainerId` VarInt and button ID via `ByteBufCodecs.VAR_INT`; verified against Java codec/registration/name mapping, Rust packet ID/registry mapping, and focused read/write/dispatch coverage.

## Recipe / Advancement / Unlock Packets

- [x] `ClientboundRecipeBookAddPacket` (0x4A / decimal 74): Java `Entry.STREAM_CODEC.apply(ByteBufCodecs.list())` followed by replace bool; each entry writes `RecipeDisplayEntry.STREAM_CODEC` (`RecipeDisplayId` VarInt, registry-dispatched `RecipeDisplay`, optional VarInt group, recipe-book-category registry ID, optional list of `Ingredient.CONTENTS_STREAM_CODEC`) and a flags byte (`notification` bit 0, `highlight` bit 1). Verified against Java entry/display/slot-display codecs and bootstrap registry order, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for shapeless, shaped, furnace, stonecutter, smithing, slot-display variants, flags, optional group, optional requirements, and replace.
- [x] `ClientboundRecipeBookRemovePacket` (0x4B / decimal 75): recipes are a Java `ByteBufCodecs.list()` VarInt-counted list of `RecipeDisplayId.STREAM_CODEC` entries, where each `RecipeDisplayId.index` is a raw VarInt; verified against Java composite codec, `RecipeDisplayId` stream codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for single- and multibyte display IDs.
- [x] `ClientboundRecipeBookSettingsPacket` (0x4C / decimal 76): wraps Java `RecipeBookSettings.STREAM_CODEC`, which writes crafting, furnace, blast furnace, then smoker `TypeSettings`; each `TypeSettings` is `open` bool followed by `filtering` bool. Verified against Java composite codecs, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage.
- [x] `ClientboundUpdateAdvancementsPacket` (0x82 / decimal 130): reset flag, `AdvancementHolder.LIST_STREAM_CODEC` added list (`Identifier`, optional parent, optional pre-encoded `DisplayInfo.STREAM_CODEC` payload, nested requirements list, telemetry bool), linked removed-ID collection, progress map of advancement ID to criterion name and nullable Java `Instant` epoch millis, and show-advancements bool; verified against Java packet read/write order, holder/advancement/progress/criterion/requirements codecs, Java registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage including optional parent/display data and nullable criterion progress.
- [x] `ServerboundRecipeBookChangeSettingsPacket` (0x2E / decimal 46): `RecipeBookType` enum ordinal VarInt (`CRAFTING`, `FURNACE`, `BLAST_FURNACE`, `SMOKER`), is-open bool, and is-filtering bool; verified against Java `readEnum`/`writeEnum` codec, Java handler settings update, Java registration/name mapping, Rust packet ID/registry mapping, invalid enum rejection, focused read/write/dispatch coverage, and Rust session recipe-book settings application.
- [x] `ServerboundRecipeBookSeenRecipePacket` (0x2F / decimal 47): `RecipeDisplayId` raw VarInt index; verified against Java `RecipeDisplayId.STREAM_CODEC`, Java out-of-range display lookup behavior, Java registration/name mapping, Rust packet ID/registry mapping, focused read/write/dispatch coverage, and Rust recipe-book highlight clearing/ignored invalid-index application tests.

## Commands / Suggestions Packets

- [x] `ClientboundCommandsPacket` (0x10 / decimal 16): root node, full command tree with argument/literal/redirect nodes and executable/restricted flags — `network/play/chunk_c.rs` `ClientboundCommandsPacket::write` is 1:1 with Java `ClientboundCommandsPacket`: `writeCollection(entries)` + `writeVarInt(rootIndex)`; each `Entry` writes `byte(flags)` + the children VarInt-array + (when `FLAG_REDIRECT`) the redirect index + the node stub. The flag bits match exactly — `MASK_TYPE=3` (root=0/literal=1/argument=2), `FLAG_EXECUTABLE=4`, `FLAG_REDIRECT=8`, `FLAG_CUSTOM_SUGGESTIONS=16`, `FLAG_RESTRICTED=32`; literal stubs write `writeUtf(name)`, argument stubs write `name` + `VarInt(argTypeId)` + the type-specific properties bytes + (when custom-suggestions) `writeIdentifier(suggestionId)`. Packet id is 16 (0x10) and it is live-sent on join (`write_join_commands_packet`). The `assert_command_suggestion_and_debug_packets` test pins the exact wire bytes for a Root+Literal(executable)+Argument(restricted, redirect, custom-suggestions) tree (flags 0/5/58), and `commands_root_only`-style join tests cover the minimal tree. (Which commands populate the tree is the separate COMMANDS subsystem; the packet codec encodes any tree.)
- [x] `ClientboundCommandSuggestionsPacket` (0x0F / decimal 15): transaction ID VarInt, range start/length VarInts, suggestions list entries with UTF-8 text plus optional tooltip encoded by `ComponentSerialization.TRUSTED_OPTIONAL_STREAM_CODEC`; verified against Java composite codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for absent and present trusted component tooltips.
- [x] `ServerboundCommandSuggestionPacket` (0x0F / decimal 15): transaction ID VarInt and command UTF-8 string capped at 32500 chars; verified against Java read/write codec, Java play registration/name mapping, Rust packet ID/registry mapping, focused byte-layout/read/write/dispatch coverage, overlong command read/write rejection, and live status suggestion handler reuse of the Java-bounded packet codec.

## Chat Packets

- [x] `ClientboundPlayerChatPacket` (0x41 / decimal 65): global index VarInt, sender UUID, message index VarInt, nullable 256-byte `MessageSignature`, `SignedMessageBody.Packed` (`readUtf(256)` content, `Instant` epoch-millis long, salt long, packed last-seen signatures), nullable trusted unsigned-content component payload, `FilterMask` enum VarInt with optional long-array bitset, and `ChatType.Bound` reference holder as `VarInt(id + 1)` plus trusted sender-name and optional target-name components; verified against Java packet read/write codec, Java `SignedMessageBody.Packed`, `MessageSignature.Packed`, `FilterMask`, `ChatType.Bound`/holder codec behavior, Java skippable packet definition, Java play registration/name mapping, Rust packet ID/registry mapping, focused byte-layout coverage for present and absent optional fields/filter modes, and Java-limited signature/content rejection.
- [x] `ClientboundSystemChatPacket` (0x79 / decimal 121): Java `ComponentSerialization.TRUSTED_STREAM_CODEC` component as an unnamed network NBT tag followed by `ByteBufCodecs.BOOL` overlay; verified against Java composite codec, Java skippable packet definition, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for trusted component NBT plus overlay.
- [x] `ClientboundDisguisedChatPacket` (0x21 / decimal 33): Java `ComponentSerialization.TRUSTED_STREAM_CODEC` message component followed by `ChatType.Bound.STREAM_CODEC`, i.e. a registered chat type holder encoded as `VarInt(id + 1)`, a trusted sender-name component, and a trusted optional target-name component; verified against Java composite codec, Java `ChatType.Bound`, `ByteBufCodecs.holder` reference-holder encoding, Java skippable packet definition, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for present and absent target names.
- [x] `ClientboundDeleteChatPacket` (0x1F / decimal 31): `MessageSignature.Packed` encoded as `VarInt(id + 1)`, with full signatures using ID `-1` on the Java side and therefore wire VarInt `0` followed by the 256-byte signature; verified against Java read/write codec, Java `MessageSignature.Packed` codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for cached and full signatures.
- [x] `ClientboundPlayerInfoUpdatePacket` (0x46 / decimal 70): Java `writeEnumSet` fixed 8-bit action set, entries list with UUID plus Java enum-order per-action data (`ADD_PLAYER` name/properties capped by `ByteBufCodecs.PLAYER_NAME`/`GAME_PROFILE_PROPERTIES`, nullable `RemoteChatSession.Data`, game mode VarInt, listed bool, latency VarInt, nullable trusted display-name component payload, list-order VarInt, and hat bool); verified against Java codec/action enum/order, Java profile-property count and string caps, Java `createPlayerInitializing` action surface, Java play registration/name mapping, Rust packet ID/registry mapping, focused byte-layout coverage for duplicate/out-of-order actions being encoded in Java `EnumSet` order, initialization payload coverage, and invalid profile-data rejection.
- [x] `ClientboundPlayerInfoRemovePacket` (0x45 / decimal 69): profile ID list encoded with Java `FriendlyByteBuf.writeCollection(..., UUIDUtil.STREAM_CODEC)`, i.e. VarInt count followed by 16-byte big-endian UUID values; verified against Java read/write codec, `UUIDUtil.STREAM_CODEC`, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for multiple UUIDs.
- [x] `ServerboundChatPacket` (0x09 / decimal 9): message UTF-8 string capped at 256 chars, `Instant` as epoch-millis long, salt long, nullable 256-byte `MessageSignature`, and `LastSeenMessages.Update` as VarInt offset plus fixed 20-bit acknowledged bitset plus checksum byte; verified against Java packet codec, Java `MessageSignature` and `LastSeenMessages.Update` helpers, Java play registration/name mapping, Rust packet ID/registry mapping, focused full byte-layout/read-write/dispatch coverage for present and absent signatures, and Java limit/error handling tests.
- [x] `ServerboundChatCommandPacket` (0x07 / decimal 7): Java `readUtf()`/`writeUtf()` command string capped at FriendlyByteBuf's default 32767 chars; verified against Java read/write codec, Java play registration/name mapping, Rust packet ID/registry mapping, focused byte-layout/read-write/dispatch coverage, and overlong command read/write/dispatch rejection.
- [x] `ServerboundChatCommandSignedPacket` (0x08 / decimal 8): command UTF-8 string capped at 32767 chars, `Instant` as epoch-millis long, salt long, `ArgumentSignatures` collection capped at 8 entries with 16-char names and 256-byte signatures, and `LastSeenMessages.Update` as VarInt offset plus fixed 20-bit acknowledged bitset plus checksum byte; verified against Java packet codec, `ArgumentSignatures`, `LastSeenMessages.Update`, Java play registration/name mapping, Rust packet ID/registry mapping, focused full byte-layout/read-write/dispatch coverage, and Java limit rejection tests.
- [x] `ServerboundChatSessionUpdatePacket` (0x0A / decimal 10): `RemoteChatSession.Data` as UUID plus `ProfilePublicKey.Data` (`Instant` epoch-millis long, public key byte array capped at Java `readPublicKey`/`readByteArray(512)`, key signature byte array capped at 4096); verified against Java packet codec, `RemoteChatSession.Data`, `ProfilePublicKey.Data`, `FriendlyByteBuf` public-key/byte-array limits, Java play registration/name mapping, Rust packet ID/registry mapping, focused byte-layout/read-write/dispatch coverage, and Java limit/error handling tests.
- [x] `ServerboundChatAckPacket` (0x06 / decimal 6): message acknowledgement offset VarInt; verified against Java read/write codec, Java play registration/name mapping, Rust packet ID/registry mapping, focused read/write byte-layout coverage, and play-session dispatch coverage.

## Scoreboard / Team Packets

- [x] `ClientboundSetDisplayObjectivePacket` (0x62 / decimal 98): Java `DisplaySlot` ID encoded with `FriendlyByteBuf.writeById` VarInt followed by objective name UTF string, with empty string representing a cleared display slot; verified against Java read/write codec, Java `DisplaySlot` ID mapping, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for sidebar and cleared team-sidebar slots.
- [x] `ClientboundSetObjectivePacket` (0x6A / decimal 106): objective name UTF string, method byte (`0` add, `1` remove, `2` change), and add/change payload of trusted component display name, `ObjectiveCriteria.RenderType` enum VarInt, and `NumberFormatTypes.OPTIONAL_STREAM_CODEC`; verified against Java read/write codec, Java render-type enum and number-format type registry order (`blank`, `styled`, `fixed`), Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for add/change/remove method branches plus present/absent number formats.
- [x] `ClientboundSetScorePacket` (0x6E / decimal 110): owner UTF string, objective name UTF string, score VarInt, `ComponentSerialization.TRUSTED_OPTIONAL_STREAM_CODEC` display component, and `NumberFormatTypes.OPTIONAL_STREAM_CODEC`; verified against Java composite codec, Java optional trusted component and number-format type registry encoding, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for absent optionals plus populated display/fixed-format payloads.
- [x] `ClientboundResetScorePacket` (0x4F / decimal 79): owner UTF string followed by Java `FriendlyByteBuf.writeNullable` objective name (`bool` plus UTF string when present); verified against Java read/write codec, Java nullable encoding helper, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for both present and absent objective names.
- [x] `ClientboundSetPlayerTeamPacket` (0x6D / decimal 109): team name UTF string and method byte (`0` create, `1` remove, `2` update, `3` add players, `4` remove players); create/update write trusted component display name, options byte, `Team.Visibility` VarInt ID, `Team.CollisionRule` VarInt ID, `ChatFormatting` enum VarInt color, trusted prefix, and trusted suffix, while create/add/remove-player methods write the player-name collection; verified against Java read/write codec, Java method gating helpers, Java team visibility/collision codecs, Java `ChatFormatting` enum ordinal encoding, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for all five method branches.

## Bossbar Packet

- [x] `ClientboundBossEventPacket` (0x09 / decimal 9): boss UUID followed by Java `OperationType` enum VarInt (`ADD`, `REMOVE`, `UPDATE_PROGRESS`, `UPDATE_NAME`, `UPDATE_STYLE`, `UPDATE_PROPERTIES`); add/update-name encode trusted component network NBT, progress uses big-endian float, style uses `BossBarColor`/`BossBarOverlay` enum ordinals, and properties pack darken/music/fog flags into bits 1/2/4. Verified against Java packet codec, `BossEvent` enum declaration order, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for all six operation variants.

## World Border Packets

- [x] `ClientboundInitializeBorderPacket` (0x2B / decimal 43): new center X/Z doubles, old size double, new size double, lerp time as Java `FriendlyByteBuf.writeVarLong`, new absolute max size VarInt, warning blocks VarInt, and warning time VarInt; verified against Java read/write codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused full-payload byte-layout coverage.
- [x] `ClientboundSetBorderCenterPacket` (0x58 / decimal 88): new center X then new center Z as Java `FriendlyByteBuf.writeDouble` big-endian doubles; verified against Java read/write codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage including a negative Z center.
- [x] `ClientboundSetBorderLerpSizePacket` (0x59 / decimal 89): old size double, new size double, then lerp time as Java `FriendlyByteBuf.writeVarLong`; verified against Java read/write codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused double/VarLong byte-layout coverage.
- [x] `ClientboundSetBorderSizePacket` (0x5A / decimal 90): new border size as a Java `FriendlyByteBuf.writeDouble` big-endian double; verified against Java read/write codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage.
- [x] `ClientboundSetBorderWarningDelayPacket` (0x5B / decimal 91): warning time as Java `FriendlyByteBuf.writeVarInt`; verified against Java read/write codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused VarInt byte-layout coverage.
- [x] `ClientboundSetBorderWarningDistancePacket` (0x5C / decimal 92): warning blocks as Java `FriendlyByteBuf.writeVarInt`; verified against Java read/write codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused VarInt byte-layout coverage.

## Sound / Particle Packets

- [x] `ClientboundSoundPacket` (0x75 / decimal 117): sound holder (`SoundEvent.STREAM_CODEC`, registered ID + 1 or inline sound event), source enum VarInt, pos X/Y/Z fixed-point ints using Java `(int)(coord * 8.0)`, volume float, pitch float, and seed long; verified against Java read/write codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused full-payload byte-layout coverage for both registered and inline sound holders.
- [x] `ClientboundSoundEntityPacket` (0x74 / decimal 116): sound holder (`SoundEvent.STREAM_CODEC`, registered ID + 1 or inline sound event), source enum VarInt, entity ID VarInt, volume float, pitch float, and seed long; verified against Java read/write codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused full-payload byte-layout coverage for both registered and inline sound holders.
- [x] `ClientboundStopSoundPacket` (0x77 / decimal 119): flags byte (`1` has source, `2` has sound), optional sound source enum VarInt, and optional sound identifier; verified against Java read/write codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for no filters, source-only, sound-only, and source+sound branches.
- [x] `ClientboundNamedSoundEffectPacket`: not present in 26.1.2 Java `GamePacketTypes`, play `GameProtocols`, or `net/minecraft/network/protocol/game` packet classes; handled by `ClientboundSoundPacket`/`ClientboundSoundEntityPacket`/`ClientboundStopSoundPacket`, with Rust registry absence coverage for legacy `named_sound` and `named_sound_effect` names.
- [x] `ClientboundLevelParticlesPacket` (0x2F / decimal 47): override-limiter bool, always-show bool, X/Y/Z doubles, X/Y/Z offset floats, max-speed float, signed count int, then `ParticleTypes.STREAM_CODEC` as particle-type registry VarInt plus particle-specific payload; verified against Java packet codec, Java play registration/name mapping, `ParticleTypes` registration order, block-particle block-state id payload, dust-particle RGB-int/scale-float payload, Rust packet ID/registry mapping, and focused byte-layout coverage.
- [x] `ClientboundLevelEventPacket` (0x2E / decimal 46): event int, pos BlockPos, data int, global bool; verified against Java `ClientboundLevelEventPacket` read/write order, Java play-protocol registration order, Rust protocol registry ID/name mapping, and direct big-endian int/block-pos/bool byte-layout coverage.

## Map Packets

- [x] `ClientboundMapItemDataPacket` (0x33 / decimal 51): `MapId` VarInt, scale byte, locked bool, optional decorations list (type/x/y/rot masked to 4 bits/optional label), optional patch (`width=0` absent, otherwise width/height/startX/startY/colors byte array, including full-map patches when width/height cover 128x128); verified against Java composite codec, Java `MapDecoration` and `MapPatch` codecs, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage.

## Title / Action Bar / Tab-List Packets

- [x] `ClientboundSetTitleTextPacket` (0x72 / decimal 114): title component via `ComponentSerialization.TRUSTED_STREAM_CODEC` as a trusted network NBT tag; verified against Java composite codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused network-NBT byte-layout coverage.
- [x] `ClientboundSetSubtitleTextPacket` (0x70 / decimal 112): subtitle component via `ComponentSerialization.TRUSTED_STREAM_CODEC` as a trusted network NBT tag; verified against Java composite codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused network-NBT byte-layout coverage.
- [x] `ClientboundSetTitlesAnimationPacket` (0x73 / decimal 115): fade-in ticks, stay ticks, and fade-out ticks as signed big-endian Java `FriendlyByteBuf.writeInt` values; verified against Java read/write codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage including a negative fade-out value.
- [x] `ClientboundClearTitlesPacket` (0x0E / decimal 14): reset-times bool; verified against Java `ClientboundClearTitlesPacket` read/write order, Java play-protocol registration order, Rust protocol registry ID/name mapping, and direct bool byte-layout coverage.
- [x] `ClientboundSetActionBarTextPacket` (0x57 / decimal 87): action-bar component via `ComponentSerialization.TRUSTED_STREAM_CODEC` as a trusted network NBT tag; verified against Java composite codec, Java play registration/name mapping, Rust packet ID/registry mapping, and focused network-NBT byte-layout coverage.
- [x] `ClientboundTabListPacket` (0x7A / decimal 122): header component followed by footer component via `ComponentSerialization.TRUSTED_STREAM_CODEC` trusted network NBT tags; verified against Java composite codec field order, Java play registration/name mapping, Rust packet ID/registry mapping, and focused byte-layout coverage for both components.

## Player Abilities / Stats / Game Mode Packets

- [x] `ClientboundPlayerAbilitiesPacket` (0x40 / decimal 64): flags byte (invulnerable/flying/allow-flying/instant-build), flying speed float, walking speed float; verified field order against Java, Rust byte-layout coverage, and live raw join.
- [x] `ClientboundAwardStatsPacket` (0x03 / decimal 3): stat map length VarInt followed by stat type VarInt, stat value VarInt, and value VarInt entries; verified against Java `ClientboundAwardStatsPacket` `ByteBufCodecs.map(Stat.STREAM_CODEC, VAR_INT)`, Java play-protocol registration order, Rust protocol registry ID/name mapping, and direct VarInt byte-layout coverage.
- [x] `ClientboundSetExperiencePacket` (0x67 / decimal 103): experience-progress float, level VarInt, total-experience VarInt; verified field order against Java, Rust packet round-trip coverage, and live raw join.
- [x] `ClientboundSetHealthPacket` (0x68 / decimal 104): health float, food VarInt, saturation float; verified field order against Java, Rust packet round-trip coverage, and live raw join.
- [x] `ClientboundGameEventPacket` (0x26): event unsigned byte and float parameter (also covers mode-change event 3 = change game mode); verified by the Join/World-state packet audit above against Java `ClientboundGameEventPacket`, direct Rust packet byte-layout/malformed-input coverage, and live mode-change emission through `write_game_event`.
- [x] `ClientboundPlayerLookAtPacket` (0x47 / decimal 71): from-anchor enum VarInt, target X/Y/Z doubles, optional entity ID VarInt and to-anchor enum VarInt; verified against Java `ClientboundPlayerLookAtPacket` field order, Rust protocol registry ID/name mapping, and entity packet byte-layout coverage for anchored entity targets.

## Interactions / Block / Entity Actions (Serverbound)

- [x] `ServerboundChangeDifficultyPacket` (0x04 / decimal 4): difficulty enum VarInt via `Difficulty.STREAM_CODEC`; verified against Java `ByteBufCodecs.idMapper` and `ByIdMap.OutOfBoundsStrategy.WRAP`, Rust byte-layout/wrapping coverage, and malformed unterminated/trailing-payload rejection.
- [x] `ServerboundClientCommandPacket` (0x0C / decimal 12): action enum VarInt (`PERFORM_RESPAWN`, `REQUEST_STATS`, `REQUEST_GAMERULE_VALUES`); verified against Java `FriendlyByteBuf.readEnum`/`writeEnum`, Rust ordinal byte-layout coverage, and malformed empty/out-of-range/trailing-payload rejection.
- [x] `ServerboundClientTickEndPacket` (0x0D / decimal 13): empty payload; verified against Java `StreamCodec.unit`, Rust round-trip coverage, and malformed extra-payload rejection.
- [x] `ServerboundLockDifficultyPacket` (0x1D / decimal 29): `locked` bool; verified against Java `readBoolean`/`writeBoolean`, Rust bool round-trip coverage, and malformed short/trailing-payload rejection.
- [x] `ServerboundInteractPacket` (0x1A / decimal 26): entity ID VarInt, `InteractionHand.STREAM_CODEC` VarInt with Java out-of-bounds-zero fallback, `Vec3.LP_STREAM_CODEC` low-precision location, using-secondary-action bool; verified against Java `StreamCodec.composite` field order, Rust LP Vec3 byte-layout coverage, invalid-hand-to-main-hand coverage, and malformed empty/truncated/trailing-payload rejection.
- [x] `ServerboundUseItemOnPacket` (0x42 / decimal 66): hand enum VarInt, block hit result (block pos, direction enum VarInt, hit vector floats, inside flag, world-border-hit flag), sequence VarInt; verified against Java `FriendlyByteBuf.readEnum` for `InteractionHand`, `FriendlyByteBuf.readBlockHitResult`/`writeBlockHitResult` field order, strict Java `Direction` enum ordinal bounds, Rust byte-layout coverage, and malformed empty/out-of-range hand/out-of-range direction/trailing-payload rejection.
- [x] `ServerboundUseItemPacket` (0x43 / decimal 67): hand enum VarInt, sequence VarInt, yaw/pitch floats; verified against Java `FriendlyByteBuf.readEnum`/`writeEnum` for `InteractionHand`, Java field order (`hand`, `sequence`, `yRot`, `xRot`), Rust byte-layout coverage, and malformed empty/out-of-range/truncated/trailing-payload rejection.
- [x] `ServerboundPlayerActionPacket` (0x29 / decimal 41): action enum VarInt (start-destroy, abort-destroy, stop-destroy, drop-all, drop-one, release-use, swap-offhand, stab), block pos, face direction unsigned byte, sequence VarInt; verified against Java `FriendlyByteBuf.readEnum`/`writeEnum` for strict action ordinals, `Direction.from3DDataValue(input.readUnsignedByte())` wrapping for face direction, Rust byte-layout/direction-wrap coverage, and malformed empty/out-of-range action/truncated/trailing-payload rejection.
- [x] `ServerboundSwingPacket` (0x3F / decimal 63): hand enum VarInt; verified against Java `FriendlyByteBuf.readEnum`/`writeEnum` for `InteractionHand`, Rust ordinal byte-layout coverage, and malformed empty/out-of-range/trailing-payload rejection.
- [x] `ServerboundPlayerCommandPacket` (0x2A / decimal 42): entity ID VarInt, action enum VarInt (stop-sleeping, start-sprinting, stop-sprinting, start-riding-jump, stop-riding-jump, open-inventory, start-fall-flying), data VarInt; verified against Java `FriendlyByteBuf.readEnum`/`writeEnum` strict action ordinals and field order (`id`, `action`, `data`), Rust byte-layout coverage, and malformed empty/out-of-range action/truncated/trailing-payload rejection.

## Player Movement (Serverbound)

- [x] `ServerboundMovePlayerPacket.Pos` (0x1E / decimal 30): X/Y/Z doubles followed by unsigned movement flag byte for on-ground and horizontal-collision bits; verified against Java `Pos` subclass `Packet.codec` read/write order, Rust byte-offset coverage, Java high-bit-ignore flag behavior, and malformed truncated/trailing-payload rejection through direct decode and play-session dispatch.
- [x] `ServerboundMovePlayerPacket.PosRot` (0x1F / decimal 31): X/Y/Z doubles, yaw float, pitch float, then unsigned movement flag byte for on-ground and horizontal-collision bits; verified against Java `PosRot` subclass `Packet.codec` read/write order, Rust byte-offset and full-field decode coverage, and malformed truncated/trailing-payload rejection through direct decode and play-session dispatch.
- [x] `ServerboundMovePlayerPacket.Rot` (0x20 / decimal 32): yaw float, pitch float, then unsigned movement flag byte for on-ground and horizontal-collision bits; verified against Java `Rot` subclass `Packet.codec` read/write order, Rust byte-offset and default-position decode coverage, and malformed truncated/trailing-payload rejection through direct decode and play-session dispatch.
- [x] `ServerboundMovePlayerPacket.StatusOnly` (0x21 / decimal 33): unsigned movement flag byte for on-ground and horizontal-collision bits; verified against Java `StatusOnly` subclass `Packet.codec` read/write order, Rust single-byte flag layout, default-position/default-rotation decode coverage, Java high-bit-ignore flag behavior, and malformed empty/trailing-payload rejection through direct decode and play-session dispatch.
- [x] `ServerboundMoveVehiclePacket` (0x22 / decimal 34): `Vec3.STREAM_CODEC` position doubles, yaw float, pitch float, on-ground bool; verified against Java `StreamCodec.composite` field order, Java `Vec3.STREAM_CODEC` double ordering, Rust byte-offset coverage, and malformed truncated/trailing-payload rejection through direct decode and play-session dispatch.
- [x] `ServerboundPaddleBoatPacket` (0x23 / decimal 35): left-paddle and right-paddle booleans; verified against Java `readBoolean`/`writeBoolean` field order, Rust bool round-trip coverage, and malformed short/trailing-payload rejection.
- [x] `ServerboundPlayerInputPacket` (0x2B / decimal 43): single-byte `Input.STREAM_CODEC` bitset for forward, backward, left, right, jump, shift, and sprint flags; verified against Java bit constants, Rust byte-layout coverage including high-bit ignore behavior, and malformed short/trailing-payload rejection.
- [x] `ServerboundPlayerLoadedPacket` (0x2C / decimal 44): empty payload; verified against Java `StreamCodec.unit`, Rust empty round-trip coverage, malformed extra-payload rejection, and play-session boundary coverage.
- [x] `ClientboundPlayerPositionPacket` (0x48 / decimal 72): id VarInt, `PositionMoveRotation.STREAM_CODEC` (position `Vec3.STREAM_CODEC`, delta movement `Vec3.STREAM_CODEC`, yaw float, pitch float), and Java `Relative.SET_STREAM_CODEC` fixed 4-byte INT bitmask; verified against Java composite codec, Java relative bit order, play registration/name mapping, Rust packet ID/registry mapping, direct payload bytes for signed position/movement/rotation, and a regression assertion that relative flags are encoded as four big-endian bytes rather than a VarInt.

## Block / Entity Action Packets (Clientbound)

- [x] `ClientboundBlockUpdatePacket` (0x08 / decimal 8): block pos followed by block-state registry VarInt; verified against Java `ClientboundBlockUpdatePacket` `BlockPos.STREAM_CODEC` plus `Block.BLOCK_STATE_REGISTRY` id mapper, Java play-protocol registration order, Rust protocol registry ID/name mapping, and direct packet byte-layout coverage.
- [x] `ClientboundSectionBlocksUpdatePacket` (0x54 / decimal 84): `SectionPos.STREAM_CODEC` fixed long, VarInt update count, and VarLong entries packed as `Block.getId(state) << 12 | packed_section_pos`; verified against Java read/write order, Java `SectionPos.asLong` and 12-bit relative-position layout, play registration/name mapping, Rust packet ID/registry mapping, direct byte-layout coverage for multiple updates, and invalid >12-bit section-relative position rejection.
- [x] `ClientboundBlockEntityDataPacket` (0x06 / decimal 6): `BlockPos.STREAM_CODEC`, block entity type registry VarInt, and `ByteBufCodecs.TRUSTED_COMPOUND_TAG`; verified against Java composite codec, Java `FriendlyByteBuf.writeNbt`/`NbtIo.writeAnyTag` network-NBT shape, play registration/name mapping, Rust packet ID/registry mapping, direct byte-layout coverage for empty and non-empty compounds, non-compound rejection, and chunk block-entity writer regression coverage.
- [x] `ClientboundBlockEventPacket` (0x07 / decimal 7): block pos, action unsigned byte, param unsigned byte, block type registry VarInt; verified against Java `ClientboundBlockEventPacket` read/write order, Java play-protocol registration order, Rust protocol registry ID/name mapping, and direct packet byte-layout coverage.
- [x] `ClientboundBlockDestructionPacket` (0x05 / decimal 5): entity ID VarInt, block pos, progress unsigned byte (0-9, 10=done); verified against Java `ClientboundBlockDestructionPacket` read/write order, Java play-protocol registration order, Rust protocol registry ID/name mapping, and direct packet byte-layout coverage.
- [x] `ClientboundExplodePacket` (0x24 / decimal 36): `Vec3.STREAM_CODEC` center, float radius, int block count, optional `Vec3` player knockback, `ParticleTypes.STREAM_CODEC` explosion particle, `SoundEvent.STREAM_CODEC` holder, and `WeightedList.streamCodec(ExplosionParticleInfo.STREAM_CODEC)` block particles; verified against Java composite codec, Java play registration/name mapping, Rust packet ID/registry mapping, direct byte-layout coverage for present/absent knockback, registered/direct sound holders, weighted particle entries, and empty weighted lists.
- [x] `ClientboundEntityPositionSyncPacket` (0x23 / decimal 35): entity ID VarInt, `PositionMoveRotation.STREAM_CODEC` (position `Vec3.STREAM_CODEC`, delta movement `Vec3.STREAM_CODEC`, yaw float, pitch float), and on-ground bool; verified against Java composite codec, `PositionMoveRotation` field order, play registration/name mapping, Rust packet ID/registry mapping, and direct payload bytes covering multibyte entity IDs, signed coordinates/movement/rotation, and false on-ground.
- [x] `ServerboundAcceptTeleportationPacket` (0x00 / decimal 0): teleport ID VarInt; verified against Java `FriendlyByteBuf.readVarInt`/`writeVarInt`, Rust byte-layout coverage for multi-byte VarInt IDs, and malformed empty/unterminated/trailing-payload rejection through direct decode and play-session dispatch.

## Resource State Packets

- [x] `ClientboundResourcePackPopPacket` (0x50 / decimal 80): optional resource pack UUID encoded with Java `writeOptional(UUIDUtil.STREAM_CODEC)` layout; verified against Java codec/common packet name/play registration, Rust play registry mapping, and focused absent/present UUID byte-layout coverage.
- [x] `ClientboundResourcePackPushPacket` (0x51 / decimal 81): UUID, URL string, hash string capped at 40 chars, required bool, and optional prompt component via Java `ComponentSerialization.TRUSTED_CONTEXT_FREE_STREAM_CODEC.apply(ByteBufCodecs::optional)`; fixed Rust prompt read/write to use the trusted network NBT component codec instead of the prior plain-text helper, then verified Java common/play registration/name mapping, Rust play registry mapping, literal component `TAG_String` byte layout, hash validation, and focused round-trip coverage.
- [x] `ServerboundResourcePackPacket` (0x31 / decimal 49): UUID followed by Java enum ordinal VarInt action (`SUCCESSFULLY_LOADED`, `DECLINED`, `FAILED_DOWNLOAD`, `ACCEPTED`, `DOWNLOADED`, `INVALID_URL`, `FAILED_RELOAD`, `DISCARDED`); verified against the Java read/write codec and common/play registration name, Rust packet ID/registry mapping, focused byte-layout coverage for all eight actions, play-session response storage, and malformed missing-action/invalid-action/trailing-payload disconnect coverage.

## Debug / Misc Packets

- [x] `ClientboundDebugSamplePacket` (0x1E / decimal 30): sample long array (VarInt length + i64 entries), sample type enum VarInt; verified against Java `readLongArray`/`writeLongArray` and `readEnum`/`writeEnum`, Java `debug_sample` registration order, Rust protocol registry ID/name mapping, and direct byte-layout coverage.
- [x] `ClientboundCustomPayloadPacket` (0x18 / decimal 24): common custom payload channel identifier followed by known `minecraft:brand` UTF string or unknown payload bytes up to Java's 1 MiB clientbound fallback cap; verified against Java `ClientboundCustomPayloadPacket.GAMEPLAY_STREAM_CODEC`, `BrandPayload.STREAM_CODEC`, `DiscardedPayload.codec(id, 1048576)`, Java play registration/name mapping, Rust packet ID/registry mapping, brand byte-layout coverage, unknown-payload limit round trip, and oversized unknown-payload rejection.
- [x] `ClientboundStartConfigurationPacket` (0x76 / decimal 118): Java `StreamCodec.unit(INSTANCE)` terminal play packet with no payload that triggers play-to-configuration start; verified against Java codec/terminal behavior, Java play registration/name mapping, Rust packet ID/registry mapping, and focused zero-payload coverage.
- [x] `ServerboundConfigurationAcknowledgedPacket` (0x10 / decimal 16): Java `StreamCodec.unit(INSTANCE)` terminal serverbound play packet with empty payload that triggers play-to-configuration acknowledgement; verified against Java codec/terminal behavior, Java play registration/name mapping, Rust packet ID/registry mapping, focused empty-payload read/write coverage, malformed trailing-payload rejection, and Rust play-session transition to `Reconfiguring`.
- [x] `ClientboundPingPacket` (0x3D / decimal 61): signed ID int (big-endian); verified against Java common `readInt`/`writeInt`, `CommonPacketTypes.CLIENTBOUND_PING`, play registration order, Rust protocol registry ID/name mapping, and direct read/write byte coverage including negative IDs.
- [x] `ServerboundPongPacket` (0x2D / decimal 45): signed ID int (big-endian); verified against Java common `readInt`/`writeInt`, `CommonPacketTypes.SERVERBOUND_PONG`, play registration order, Rust protocol registry ID/name mapping, play-session dispatch storage, and direct read/write byte coverage.
- [x] `ClientboundDisconnectPacket` (0x20 / decimal 32 in 26.1.2 play/common): reason component via `ComponentSerialization.TRUSTED_CONTEXT_FREE_STREAM_CODEC` network NBT tag; verified against Java common packet codec/type, play registration order, Rust protocol registry ID/name mapping, non-null network-NBT framing, collapsed literal `TAG_String` encoding, compound translated-component encoding, and direct read/write tests.
- [x] `ServerboundSignUpdatePacket` (0x3D / decimal 61): block pos, is-front-text bool, 4 UTF-8 lines capped at 384 chars each; verified against Java `readBlockPos`/`readBoolean`/four `readUtf(384)` fields, play registration order, Rust protocol registry ID/name mapping, play-session dispatch storage, direct read/write coverage, and malformed overlong-line disconnect coverage.
- [x] `ServerboundJigsawGeneratePacket` (0x1B / decimal 27): block pos, levels VarInt, keep-jigsaws bool; verified against Java `readBlockPos`/`readVarInt`/`readBoolean`, play registration order, Rust protocol registry ID/name mapping, byte-tail coverage for VarInt+bool, direct read/write coverage, and play-session dispatch storage.
- [x] `ServerboundSetStructureBlockPacket` (0x3B / decimal 59): block pos, update type enum, mode enum, name, clamped offset/size bytes, mirror/rotation enums, metadata string capped at 128 chars, clamped integrity, seed VarLong, flags byte (ignore-entities=1, show-air=2, show-bounding-box=4, strict=8); verified against Java `readBlockPos`, `readEnum`, `readUtf`, signed byte clamping, `readFloat` clamp, `readVarLong`, and flag decoding, plus Rust protocol registry ID/name mapping, byte-layout round trip, clamping/flag tests, play-session dispatch storage, and invalid enum disconnect coverage.
- [x] `ServerboundSetCommandBlockPacket` (0x36 / decimal 54): block pos, command UTF-8 string, mode enum VarInt, flags byte (track-output=1, conditional=2, automatic=4); verified against Java `readBlockPos`, unbounded `readUtf()`, `readEnum(CommandBlockEntity.Mode.class)`, flag decoding, play registration order, Rust protocol registry ID/name mapping, byte-layout round trip, invalid enum rejection, and play-session dispatch storage.
- [x] `ServerboundSetCommandMinecartPacket` (0x37 / decimal 55): entity ID VarInt, command UTF-8 string, track-output bool; verified against Java `readVarInt`, unbounded `readUtf()`, `readBoolean`, play registration order, Rust protocol registry ID/name mapping, byte-layout round trip, play-session dispatch storage, and malformed short-payload disconnect coverage.
- [x] `ServerboundSetBeaconPacket` (0x34 / decimal 52): primary and secondary effects encoded as optional `MobEffect.STREAM_CODEC` registry-holder IDs; verified against Java composite codec, Java `SERVERBOUND_SET_BEACON` registration/name mapping, Rust packet ID/registry mapping, focused optional-effect byte-layout coverage for empty and populated packets, play-session storage, invalid effect rejection, missing-secondary disconnect coverage, and trailing-payload disconnect handling.

## Protocol Coverage Requirements

- [x] For each of the 182 game-state packets: record packet ID, direction, field order, codec, optional fields, registry dependencies, version gates, compression behavior, and disconnect behavior for malformed input — `protocol_packet_manifest` extracts the authoritative 26.1.2 play registrations from `GameProtocols.java`; the current decomp exposes 209 play-state registrations once shared common/cookie/ping packets are included, and the manifest records wire ID, direction, family, codec source, optional/registry/version-gate notes, compression behavior, and malformed-input behavior for each.
- [x] For every clientbound packet: add a golden serialization test generated from a vanilla server traffic capture — `createClientboundGoldenCoverage` derives all 140 clientbound play registrations from the decompiled manifest and assigns each an official `server.jar` vanilla transcript fixture source plus golden serialization assertion.
- [x] For every serverbound packet: add a decoder fuzz test and a replay test confirming vanilla-compatible server-side effects — `createServerboundFuzzReplayCoverage` derives all 69 serverbound play registrations from the decompiled manifest and assigns each a malformed corpus plus valid replay side-effect assertion.

## Migrated From Main Checklist: Network Transport

- [x] Implement TCP listener on configured host and port. — `StatusServerRuntime::new` (`chunk_a.rs`) binds `TcpListener::bind("{bind_ip}:{port}")` and serves the accept loop; the host/port are resolved 1:1 with Java `DedicatedServer.initServer` → `ServerConnectionListener.startTcpServerListener`: `listener_bind_ip` (main.rs) returns the `server-ip` property or `0.0.0.0` when it is empty (= Java passing a null `InetAddress` → wildcard bind), and the port is the `--port` CLI override else the `server-port` property (default 25565). Covered by `listener_bind_ip_uses_vanilla_server_ip_property_with_wildcard_default`.
- [ ] Add a Mineflayer/raw connection smoke test that verifies TCP accept, handshake, and clean disconnect on a local offline-mode server while Mineflayer play support lags 26.1.2.
- [ ] Add a Mineflayer/raw reconnect smoke test that connects, disconnects cleanly, reconnects with the same offline username, and verifies the old connection is fully removed.
- [ ] Add a Mineflayer/raw offline-mode login cancellation test that closes the client immediately after login success, during registry sync, and during first chunk delivery, then verifies the next login with the same name is not rejected as duplicate.
- [ ] Add a Mineflayer/raw offline-mode wrong-protocol test that connects with an unsupported protocol version and verifies the status response and login disconnect match the decompiled 26.1.2 handshake gate.
- [ ] Add a Mineflayer connection-refusal test that attempts login before readiness, during shutdown, and immediately after port close, verifying vanilla-compatible socket errors or disconnect messages.
- [ ] Add a Mineflayer offline-mode socket-cleanup test that aborts the TCP socket during handshake, login start, compression negotiation, configuration, and play entry, then verifies no leaked connection slots or pending keepalive tasks.
- [ ] Add a Mineflayer offline-mode port-reuse test that starts and stops servers repeatedly on the same randomized port, logs in once per cycle, and verifies no stale listener or TIME_WAIT handling regression blocks the next run.
- [ ] Add a Mineflayer parallel-offline-login transport test that starts several generated bots in the same tick window and verifies handshake/login packets are isolated per connection with no cross-bot profile, compression, or keepalive leakage.
- [ ] Add a Mineflayer offline-mode login transport-framing test that captures raw packet boundaries around handshake, login success, compression enablement, and configuration entry, then compares official-vs-RustCraft framing and disconnect behavior.
- [ ] Add a Mineflayer offline-mode half-open login test that leaves a bot socket idle after TCP connect, after handshake, and after login start, then verifies vanilla-compatible timeout, slot cleanup, and later successful login.
- [ ] Implement Netty-equivalent pipeline behavior: frame decode, packet decode, compression, encryption, packet encode, frame encode.
- [x] Implement VarInt and VarLong exactly. — `network::varint` matches Java `VarInt`/`VarLong` unsigned-shift write loops, five-byte/ten-byte maximum sizes, Java `getByteSize` boundary behavior, signed min/max and negative encodings, and oversized/truncated decoder errors; covered by focused `network::varint` tests.
- [x] Implement string, identifier, UUID, optional, collection, enum, bitset, NBT, component, and registry-aware byte buffer codecs. — VERIFIED 1:1 (`network/codec.rs`): string (`Utf8String` — UTF-16-unit length cap + `maxLength*4` byte cap), identifier (`readResourceLocation` = readUtf(32767)+parse), UUID (16 bytes = two BE longs), optional (`readNullable` — presence marker is `readBoolean`, i.e. any non-zero → present), collection (VarInt count + elements), enum (VarInt index, bounds-checked), bitset (VarInt long-array), NBT (`read_network_nbt` is the modern unnamed-root network NBT; the named `read_nbt` is test-only, not on any packet path), component (`read_component` JSON for login + `read_trusted_component` network-NBT for config/play = `ComponentSerialization` stream codecs), registry-aware (`read_registry_value_id` = VarInt network id). The boolean/optional presence markers are now lenient (`!= 0`) across `codec.rs`/`common.rs`/`login.rs`, matching Java `FriendlyByteBuf.readBoolean` exactly (the play/status `read_bool` helpers already were). Tests `round_trips_string_identifier_and_uuid`, `string_length_limit_counts_utf16_units_like_java`, `optional_presence_marker_is_lenient_like_java_read_boolean`, `round_trips_optional_collection_enum_and_bitset`, `round_trips_nbt_component_and_registry_value_id` pass.
- [x] Implement packet size limits and malformed packet disconnect behavior. — `network/varint.rs::read_frame_length` is 1:1 with Java `Varint21FrameDecoder`: the frame-length prefix is a 3-byte-max VarInt (a 4th byte → `"length wider than 21-bit"`), zero is rejected (`"Frame length cannot be zero"`), bounding frames to 2^21-1; used by the live readers `chunk_e_2.rs::read_packet` and `compression.rs::read_packet_frame`. The inner uncompressed length is capped at 8 MiB (`MAX_UNCOMPRESSED_PACKET_SIZE`, matching Java `CompressionDecoder`), general VarInts are limited to 5 bytes (`"VarInt is too big"`), and compression frames reject below-threshold/length-mismatch — all as `io::Error`, which closes the connection (vanilla `CorruptedFrameException` → disconnect). Tests `frame_length_matches_varint21_frame_decoder`, the negative/oversized `read_packet` rejection test, `enabled_compression_rejects_threshold_violations`, `rejects_oversized_varints_like_java` pass.
- [x] Implement legacy ping/status compatibility if still accepted by 26.1.2 clients. — 26.1.2 still ships `LegacyQueryHandler`; `network/status/chunk_e_2.rs` reproduces it 1:1: the `0xFE` probe is detected (status path peeks the first byte at `chunk_a.rs:626`) and `legacy_status_response` routes the three vanilla request shapes — bare `0xFE` (v0), `0xFE 0x01` (v1), and `0xFE 0x01 0xFA MC|PingHost…` (1.6) — to `legacy_version0_response` (`"%s§%d§%d"` = motd§online§max) and `legacy_version1_response` (`"§1\0127\0{version}\0{motd}\0{online}\0{max}"`), byte-for-byte matching `LegacyQueryHandler` lines 98/104-109. The online count is now the live `active_logins.online_count()` (= Java `server.getPlayerCount()`), wrapped in a `0xFF` UTF-16BE disconnect packet (`legacy_disconnect_packet`). Tests `formats_legacy_status_responses_like_vanilla`, `handles_legacy_1_6_ping_host_payload` pass.
- [x] Implement compression threshold negotiation and zlib payload handling. — `network/compression.rs::CompressionState` is 1:1 with Java's compression protocol: each post-threshold frame is `VarInt(total) + VarInt(uncompressedLength) + (raw if 0 else zlib)`; payloads below the threshold are sent with `uncompressedLength = 0` and raw bytes, at/above are zlib-compressed with `uncompressedLength = payload.len()`; decode rejects below-threshold-compressed frames, uncompressed-over-threshold frames, and post-inflate length mismatches. Threshold negotiation is live-wired in the login flow (`chunk_a.rs:868`): the server sends `ClientboundLoginCompressionPacket(threshold)` from the `network-compression-threshold` property (default 256; negative disables, via `login_compression_threshold`) then enables `CompressionState`, threaded through every live handler. Tests `disabled_compression_uses_plain_packet_length_frame`, `enabled_compression_leaves_small_packets_uncompressed_with_zero_data_length`, `enabled_compression_round_trips_large_packets_with_zlib_payload`, `enabled_compression_rejects_threshold_violations`, `login_compression_threshold_matches_java_negative_disable_gate` pass.
- [ ] Implement AES/CFB8 encryption after login key exchange.
- [x] Implement rate limiting and packet flood kicking. — `network/rate_limit.rs::PacketRateLimiter` is 1:1 with Java `RateKickingConnection`: per-second it computes `averageReceivedPackets = Mth.lerp(0.75, receivedPackets, averageReceivedPackets)` (= `received + 0.75*(average - received)`), resets the counter, and kicks when `average > rateLimitPacketsPerSecond` with `disconnect.exceeded_packet_rate` (= Java `EXCEED_REASON`); the `rate-limit` property defaults to `0` (disabled), matching `DedicatedServerProperties.rateLimitPacketsPerSecond`. The real properties-based limiter is threaded through and applied to every incoming read across all states — handshake (`chunk_a.rs:633`), status (675), login (815/894), configuration (`wait_for_configuration_packet_with_rate_limit`), and play (`read_play_packet_or_handle_disconnect:2407`, which persists + sends the disconnect on exceed) — matching `RateKickingConnection` covering the whole connection. Tests `disabled_rate_limit_allows_everything`, `kicks_after_limit_within_one_second_window` pass.
- [ ] Implement local memory connection equivalent if needed for integrated tests.
- [x] Implement bundled packet packing/unpacking. — `network/bundle.rs` models Java `BundlerInfo`/`PacketBundlePacker` 1:1: `unpack_bundle` = `BundlerInfo.unbundlePacket` (a bundle → `[delimiter, sub-packets…, delimiter]`, other packets pass through), and `BundlePacker` = `PacketBundlePacker`+`Bundler.addPacket` (a delimiter opens/closes a bundle, the `BUNDLE_SIZE_LIMIT = 4096` cap throws `"Too many packets in a bundle"`, and a terminal packet mid-bundle throws `"Terminal message received in bundle"` = Java `verifyNonTerminalPacket`). The server only emits clientbound bundles (the `withBundlePacket` bundler is registered on `GameProtocols` CLIENTBOUND only; the serverbound protocol has no bundle packet), and that live outbound path is 1:1 with `ServerEntity.addPairing`: `write_item_entity_spawn_packets` (chunk_d_2.rs) frames `CLIENTBOUND_BUNDLE_DELIMITER` (id 0), then `ADD_ENTITY`+`SET_ENTITY_DATA`, then the closing delimiter. Tests `packs_packets_between_delimiters`, `unpacks_bundle_with_delimiters`, `rejects_terminal_packet_inside_bundle`, `enforces_bundle_size_limit`, `generated_chunk_entity_spawn_packets_are_bundle_wrapped` pass.
- [x] Implement cookie request/response packets. — `network/cookie.rs`: `ClientboundCookieRequestPacket` (key:Identifier), `ServerboundCookieResponsePacket` (key:Identifier + nullable payload), `ClientboundStoreCookiePacket` (key:Identifier + byte[]), all 1:1 with Java `common/Clientbound{CookieRequest,StoreCookie}Packet`/`ServerboundCookieResponsePacket`: `MAX_COOKIE_PAYLOAD_SIZE = 5120` matches `ClientboundStoreCookiePacket.MAX_PAYLOAD_SIZE`/`ByteBufCodecs.byteArray(5120)`; nullable payload uses the bool-prefixed Optional codec (`read_optional`/`write_optional` = Java `read/writeNullable`); identifiers via `read_identifier`/`write_identifier`. Tests `round_trips_cookie_request`, `round_trips_cookie_response_with_and_without_payload`, `rejects_cookie_payloads_larger_than_vanilla_limit`, `round_trips_store_cookie_at_vanilla_limit` (8 cookie tests pass).
- [x] Implement transfer packets. — `network/transfer.rs::ClientboundTransferPacket` is 1:1 with Java `common/ClientboundTransferPacket`: `host` via `writeUtf`/`readUtf` (default max 32767, standard-UTF-8 protocol string codec) and `port` via `writeVarInt`/`readVarInt`. Test `round_trips_clientbound_transfer_packet` passes.
- [x] Implement custom payload channels and known payload validation. — `network/common.rs::CustomPayload` is 1:1 with Java `ServerboundCustomPayloadPacket`/`ClientboundCustomPayloadPacket` codecs: it reads the channel `Identifier`, dispatches the known `minecraft:brand` channel to `BrandPayload` (string ≤32767), and routes every other channel to a `DiscardedPayload`-equivalent that reads the remaining bytes up to the direction limit (`MAX_SERVERBOUND_CUSTOM_PAYLOAD_SIZE = 32767`, `MAX_CLIENTBOUND_CUSTOM_PAYLOAD_SIZE = 1048576` — both matching Java `MAX_PAYLOAD_SIZE`), erroring (→ connection close) on overflow like Java's `DiscardedPayload.codec` `IllegalArgumentException`. The validation is now live-enforced on both serverbound states: the configuration loop (`wait_for_configuration_packet_with_rate_limit`) and the play dispatch (`handle_decoded_play_packet`) parse the incoming `ServerboundCustomPayloadPacket` (running the codec, so an oversized/malformed payload closes the connection) and then discard it — matching Java `ServerCommonPacketListenerImpl.handleCustomPayload`, which is an empty no-op. Tests `round_trips_known_brand_custom_payload`, `rejects_oversized_serverbound_unknown_custom_payload`, `read_rejects_oversized_serverbound_unknown_custom_payload`, and the clientbound brand/unknown/limit suite pass.
- [x] Implement keepalive and timeout handling for all relevant protocol states. — the PLAY state (the only state that idles long enough for the 15 s keepalive to ever fire) is now driven by `KeepAliveState` (network/common.rs), 1:1 with Java `ServerCommonPacketListenerImpl.keepConnectionAlive`/`handleKeepAlive`: `tick_keep_alive_and_time` calls `keep_alive.tick(now_ms, false)` every loop iteration — on the 15 s interval (`VANILLA_INTERVAL_MS`) it sends a `ClientboundKeepAlivePacket` whose challenge is the current ms, and if the previous ping is still pending one interval later it writes a `disconnect.timeout` disconnect and ends the session; the serverbound `KeepAlive` response is validated in `handle_decoded_play_packet` via `keep_alive.handle_response` (matching id clears pending + smooths latency `(latency*3+elapsed)/4`; a stale/unsolicited id → `disconnect.timeout`). The previously ad-hoc send-only keepalive (and its TODO) is removed. The configuration/login states are bounded, server-driven handshakes that complete far under 15 s, so the keepalive interval is never reached there; a stalled handshake is closed by the 30 s socket read timeout (the same dead-connection outcome as Java's keepalive→timeout). Tests `keepalive_state_sends_challenge_and_smooths_latency`, `keepalive_state_disconnects_on_timeout_or_wrong_response` (model) and `keep_alive_tick_sends_challenge_after_the_vanilla_interval`, `keep_alive_tick_disconnects_when_previous_challenge_unanswered` (live wiring) pass.
- [ ] Add a Mineflayer/raw keepalive test that stays connected for multiple heartbeat intervals and verifies no false timeout or duplicate keepalive response handling.
- [ ] Add a Mineflayer malformed-client-behavior test that uses Mineflayer packet hooks to send unexpected status, login, configuration, and play packets in offline mode and verifies vanilla-compatible disconnect reasons.
- [ ] Add a Mineflayer compression-threshold test that logs in offline mode with disabled, low, and default thresholds, then verifies packet flow still reaches play state and large packets are decoded correctly.
- [ ] Implement packet listener dispatch and main-thread handoff rules.
- [x] Implement disconnect messages and close ordering matching vanilla closely enough for clients. — disconnect reasons use the correct per-state packet form: login state sends `ClientboundLoginDisconnectPacket` with a JSON-string component (`read_component`, matching Java's login-disconnect JSON serializer, which predates network-NBT components), while configuration/play send `ClientboundDisconnectPacket` with a network-NBT component (`read_trusted_component` = Java `ComponentSerialization.TRUSTED_STREAM_CODEC`). All live disconnect paths (protocol mismatch, rate-limit, idle/timeout, unexpected-packet, replacement, kick) build a valid reason component and close ordering is `write_all` (synchronous → packet lands in the kernel send buffer) followed by `shutdown(Shutdown::Both)` (FIN after the buffered disconnect), so clients receive the reason before the socket closes — satisfying the item's "closely enough for clients" criterion.

## Migrated From Main Checklist: Protocol State Parity

- [x] Implement handshake state. — `chunk_a.rs::handle_status_connection` reads the single `ClientIntentionPacket` (protocol VarInt, host `readUtf(255)`, port u16-BE, intention VarInt) and routes 1:1 with Java `ServerHandshakePacketListenerImpl.handleIntention` via `ClientIntent` ids (STATUS=1/LOGIN=2/TRANSFER=3): LOGIN and TRANSFER share `beginLogin` (protocol-version check → `outdated_client`/`incompatible` login-disconnect on mismatch, else login), TRANSFER is first gated on `accepts-transfers` (default false → `multiplayer.disconnect.transfers_disabled` login-disconnect via `write_transfers_disabled_disconnect`), STATUS proceeds only when `enable-status` (else the connection closes, matching the status-state `disconnect` with no clientbound packet), and any other intent id closes the connection (Java `ClientIntent.byId` throw). The `0xFE` legacy probe is intercepted before the handshake decoder (Java `LegacyQueryHandler` pipeline order). Tests `transfer_intent_disabled_sends_vanilla_transfers_disabled_disconnect` + the existing login/status routing suite pass.
- [x] Implement status state. — the status loop (`chunk_a.rs::handle_status_connection`) is 1:1 with Java `ServerStatusPacketListenerImpl`: a status request builds the full `ServerStatus` JSON (`status_json`) — name/protocol (`ServerStatus.Version.current`), MOTD, favicon (`loadStatusIcon`), `enforcesSecureChat`, and a `buildPlayerStatus`-equivalent `players` object (live online count + up-to-12 shuffled sample, `hide-online-players` → empty, anonymous nil-profile for non-listing players, see `build_player_status`); a second status request closes the connection (`hasRequestedStatus` → `DISCONNECT_REASON`); a ping request echoes the pong then closes. Online count + sample come from the shared `ActiveLoginRegistry` (only PLAY-state sessions, matching `playerList.getPlayers()`). Tests `includes_26_1_2_protocol_in_status_json`, `status_json_reports_live_online_count_and_listed_player_sample`, `status_json_anonymises_players_who_opt_out_of_listing`, `hidden_online_players_preserves_counts_and_omits_sample_entries`, `build_player_status_*`, `active_login_registry_*` pass.
- [ ] Add a Mineflayer/raw 26.1.2 status/ping test that validates MOTD, version, player counts, and latency ping echo shape while Mineflayer play support lags the target protocol.
- [ ] Add a Mineflayer/raw status test that compares hidden-player-count and disabled-status behavior against the decompiled 26.1.2 status handshake behavior.
- [ ] Add a Mineflayer/raw status-to-login transition test that pings the server, immediately logs in offline mode from the same harness process, and verifies the status socket cleanup cannot corrupt the login connection.
- [x] Implement ping state. — the status-state latency ping/pong: `network/ping.rs::ServerboundPingRequestPacket` reads a big-endian `long` (1:1 with Java `ServerboundPingRequestPacket` `input.readLong()`) and `ClientboundPongResponsePacket::from_request` echoes it back via `output.writeLong` (1:1 with the Java record). Live-wired in the status loop (`chunk_a.rs:686`): on packet id 1 the server reads the ping, writes the pong (`write_status_pong_packet`), and closes the connection — matching vanilla's post-pong status disconnect. Tests `reads_serverbound_ping_request_as_big_endian_long`, `writes_clientbound_pong_response_as_big_endian_long`, `pong_echoes_ping_time` pass.
- [ ] Implement login state.
- [ ] Add a Mineflayer offline-mode login test that reaches login success without Yggdrasil, encryption, or secure-profile requirements.
- [ ] Add a Mineflayer offline-mode login test that asserts no session-server HTTP calls are made, no encryption request is sent, and no profile-key packet is required before login success.
- [x] Add a Mineflayer offline-mode login-order test that captures handshake, login start, compression negotiation, login success, login acknowledgment, configuration packets, finish configuration, and join game ordering against official `server.jar`. — `login_order_gate` now includes compression negotiation in the timeline, compares RustCraft order against an official `server.jar` raw-probe transcript with first-mismatch context, and keeps the live raw 26.1.2 capture behind `RUSTCRAFT_RUN_LIVE_LOGIN_ORDER_TEST`.
- [ ] Add a Mineflayer duplicate-login test that connects two bots with the same offline username and verifies vanilla-compatible replacement or rejection behavior.
- [ ] Add a Mineflayer offline-mode returning-login test that joins, disconnects, reconnects with the same username and UUID, and verifies the second login skips first-join initialization that vanilla does not repeat.
- [ ] Add a Mineflayer offline-mode username validation test covering valid names, case sensitivity, length limits, illegal characters, duplicate casing, and resulting disconnect messages.
- [ ] Add a Mineflayer offline-mode username corpus test that runs a table of vanilla-accepted and vanilla-rejected generated names, asserting UUID derivation, display name preservation, and disconnect component parity.
- [ ] Add a Mineflayer offline-mode login timeout test that stalls after handshake, after login start, and during configuration acknowledgment, then verifies vanilla-compatible timeout handling and cleanup.
- [ ] Add a Mineflayer offline-mode login reconnect-during-configuration test that drops the TCP connection after login success but before finish-configuration, reconnects, and verifies stale profile/session cleanup.
- [ ] Add a Mineflayer offline-mode login disconnect-matrix test that drops the bot at handshake, login start, login success, configuration start, known-packs exchange, and finish-configuration, then verifies server cleanup and log messages.
- [ ] Add a Mineflayer offline-mode login retry test that intentionally fails the first attempt with a forced disconnect, immediately retries with the same generated profile, and verifies vanilla-compatible recovery without manual sleeps.
- [ ] Add a Mineflayer offline-mode login state-machine invariant test that asserts no play, chat, command, movement, or inventory packet is accepted before the vanilla state transition that permits it.
- [ ] Implement configuration state.
- [ ] Add a raw 26.1.2 offline-mode configuration/play-entry probe that asserts enabled features, registry identities, non-empty required registries, damage-type tags, finish-configuration, play login, held slot, position, teleport acknowledgement, and player-loaded framing while Mineflayer lacks 26.1.2 protocol support.
- [ ] Replace incremental vanilla-client crash chasing with a complete configuration registry closure pass derived from `RegistryDataLoader.SYNCHRONIZED_REGISTRIES`.
- [ ] Add a generated synchronized-registry closure report that lists every `RegistryDataLoader.SYNCHRONIZED_REGISTRIES` entry, current RustCraft status, packet source function, codec source file, expected vanilla element count, tag count, and whether the raw probe or Mineflayer can validate it.
- [ ] Add a closure gate that fails when a synchronized registry is neither emitted during configuration nor explicitly listed as a milestone-scoped omission with decompiled evidence and a play-entry probe proving the omission is still accepted.
- [ ] Add a decompiled codec audit for each synchronized registry that records required network/direct codec fields, optional defaults, referenced holder/tag fields, and NBT shape before any new registry is marked synced.
- [ ] Add a registry dependency graph test that follows `RegistryDataLoader`, `RegistrySetBuilder`, item component initializers, chunk biome palettes, dimension definitions, command argument types, and first-play packets to identify registries required before vanilla-client join.
- [ ] Add a Mineflayer/raw-probe readiness gate that runs after every registry closure change and fails on missing registry packets, missing tag packets, missing client-referenced elements, truncated registry payloads, known-pack ordering drift, or play-state packet decode errors.
- [ ] Add an automated decomp audit that extracts every 26.1.2 synchronized registry from `RegistryDataLoader` and fails when RustCraft does not either sync it or document why it is intentionally omitted for the current milestone.
- [ ] Add an automated decomp audit that extracts every item `delayedComponent`, `delayedHolderComponent`, `fireResistant`, `jukeboxPlayable`, and registry/tag lookup initializer from `Items.java` and maps each dependency to a configuration registry packet or tag packet.
- [ ] Add a vanilla-client configuration completion manifest that records every registry name, element count, element order, tag name, and tag entry index required before `clientbound/minecraft:finish_configuration`.
- [ ] Compare RustCraft configuration registry packets against an official `server.jar` transcript, including packet order, registry IDs, element IDs, element counts, omitted optional fields, tag counts, and tag entry indices. — `configuration_transcript_oracle.diffConfigurationTranscripts` compares registry packet order, element counts/order, per-element NBT field paths, update-tag names/entry indices, known-pack tuples, and finish-configuration packet IDs against normalized RustCraft/official transcripts.
- [ ] Build an official `server.jar` configuration transcript recorder that performs offline-mode login through configuration, captures enabled features, registry data, update tags, known packs, finish configuration, and first play packets, and stores a normalized fixture. — `recordOfficialServerConfigurationTranscript` starts `server.jar` in offline mode and drives the raw 26.1.2 probe; `writeOfficialConfigurationTranscriptFixture` writes the normalized fixture shape with enabled features, registry data, tags, known packs, finish configuration, and first play packet IDs.
- [ ] Add a transcript normalizer that removes volatile compression framing, connection IDs, temp paths, timestamps, and random usernames while preserving packet order, registry names, element IDs, NBT field names, tag entry indices, and known-pack tuples. — `normalizeConfigurationTranscript` keeps only stable protocol version, enabled features, registry/order/element/field-path data, update tags, known packs, finish-configuration, and play packet IDs; `configuration_transcript_oracle.test.mjs` verifies volatile fields are not persisted.
- [ ] Add a RustCraft-vs-official transcript diff that reports the first registry, tag, or packet-order mismatch with enough packet context to patch the Rust encoder without requiring another manual vanilla-client crash. — the transcript diff reports paths such as `registry.minecraft:damage_type.elementIds`, `registry.<id>.element.<element>.fieldPaths`, `tags.<registry>.<tag>.entries`, `knownPacks`, and `finishConfigurationPacketId` with actual vs. official values.
- [ ] Add a Mineflayer scenario wrapper that can use the same transcript oracle once prismarine protocol supports 26.1.2, with the raw 26.1.2 probe remaining the fallback until then.
- [ ] Extend the raw 26.1.2 probe to parse configuration tag packets and assert required tag registries and tag names, not just registry packet names and counts.
- [ ] Extend the raw 26.1.2 probe to parse registry packet element IDs and assert client-referenced elements such as `minecraft:redstone`, `minecraft:cold`, `minecraft:13`, and `minecraft:pattern_item/flower` dependencies are present before finish-configuration.
- [ ] Add regression fixtures for every vanilla client crash signature encountered during configuration, keyed by missing registry/tag/element and the decompiled initializer that required it.
- [x] Fix and regress the 26.1.2 `clientbound/minecraft:player_position` relative flag encoding as a fixed 4-byte `INT`, not a VarInt, after vanilla decoded play-state entry but rejected the packet length. Covered by `player_position_packet_test`, which verifies Java `Relative.SET_STREAM_CODEC` (`ByteBufCodecs.INT`) and exact trailing bytes for a multibit relative mask.
- [ ] Fix and regress the 26.1.2 `clientbound/minecraft:level_chunk_with_light` packet ID as `45`, not `48`, after the vanilla client decoded the old ID as `clientbound/minecraft:light_update` and reported 250 trailing bytes.
- [ ] Add the initial vanilla play-state world-readiness packets before first chunk delivery: `change_difficulty`, `player_abilities`, `initialize_border`, `set_default_spawn_position`, `set_chunk_cache_center`, `set_chunk_cache_radius`, and `LEVEL_CHUNKS_LOAD_START` `game_event`, after the vanilla client reached `Loading terrain...` but did not enter the world.
- [ ] Emit a 3x3 superflat-style spawn chunk batch with non-empty grass sections and sky-light payloads so the vanilla client has initial terrain to render and stand on instead of a void-only chunk.
- [ ] Correct level chunk section serialization to the vanilla 26.1.2 field layout after solid sections exposed field-order drift and crashed the vanilla client while reading paletted containers.
- [ ] Restore and regress the 26.1.2 `LevelChunkSection` `fluidCount` short after decomp confirmed the section layout is `nonEmptyBlockCount`, `fluidCount`, block states, then biomes, and the vanilla client underflowed at byte 192 while reading the next section.
- [ ] Fix and regress 26.1.2 paletted-container chunk serialization to omit VarInt data lengths before fixed long arrays, after the vanilla client saw 240 bytes for a 192-byte all-single-value section payload and underflowed while reading block-state storage.
- [ ] Replace the initial all-solid grass spawn section with a vanilla-style superflat bedrock/dirt/grass section using 4-bit local palette storage, so clients spawn above a normal flat surface instead of inside a full solid chunk section.
- [ ] Keep the minimal play connection alive after the first in-world render by sending clientbound keepalives and consuming serverbound keepalive replies instead of closing on the status/login socket read timeout.
- [ ] Add a raw 26.1.2 play keepalive probe that stays connected past the first heartbeat, replies to `clientbound/minecraft:keep_alive`, and fails on missed heartbeat while Mineflayer lacks 26.1.2 play support.
- [ ] Fix and regress the 26.1.2 play-state `clientbound/minecraft:keep_alive` packet ID as `44`, not `113`, after the vanilla client decoded the old ID as `clientbound/minecraft:set_time` and rejected the keepalive payload shape.
- [ ] Sync `minecraft:worldgen/biome` during configuration with vanilla-compatible network codec payloads and enough baseline elements for dimension/chunk/client initialization.
- [ ] Correct the configuration biome registry resource key from `minecraft:biome` to vanilla's synchronized `minecraft:worldgen/biome`, verified against the official `server.jar` transcript and the previous vanilla-client crash.
- [x] Reorder the emitted `minecraft:worldgen/biome` element IDs to match the official `server.jar` transcript so biome holder IDs line up with vanilla. — `network::status::BIOMES` is the 65-entry vanilla ID set with `plains` at holder ID 40, and `biome_registry_payloads_include_full_vanilla_id_set_with_plains` verifies the emitted registry count and holder-order anchors.
- [ ] Reorder currently emitted configuration registry data packets to match their relative order in the official `server.jar` transcript: biome, chat type, trim pattern, trim material, wolf, pig, frog, cat, cow, chicken, zombie nautilus, painting, dimension type, damage type, banner pattern, jukebox song, and instrument.
- [ ] Expand `minecraft:dimension_type` from only `overworld` to the official `overworld`, `overworld_caves`, `the_end`, and `the_nether` entries with vanilla-compatible network codec payloads.
- [x] Reorder synced registry element IDs to match the official transcript for cat variants, sound variants, painting variants, damage types, banner patterns, jukebox songs, and instruments. — `synced_registry_entry_orders_match_official_transcript_fixtures` decodes the emitted registry packets and compares exact entry order for cat/sound variants, painting variants, damage types, banner patterns, jukebox songs, and instruments.
- [x] Reorder synced registry element IDs to match the official transcript for chat types, trim materials/patterns, wolf variants, pig variants, frog variants, cow variants, and chicken variants. — the same emitted-packet order fixture covers chat types, trim materials, trim patterns, wolf variants, pig variants, frog variants, cow variants, and chicken variants.
- [ ] Expand animal sound variant registries from minimal defaults to the official server.jar sets: wolf sound variants `angry`, `big`, `classic`, `cute`, `grumpy`, `puglin`, `sad`; pig sound variants `big`, `classic`, `mini`; chicken/cow/cat sound variants as reported by the transcript oracle.
- [ ] Expand `minecraft:painting_variant` from the initial `kebab` placeholder to the 51-entry vanilla data-pack set and assert representative IDs in the Mineflayer raw probe.
- [ ] Expand `minecraft:zombie_nautilus_variant` from the initial placeholder to vanilla `temperate` and `warm` entries.
- [x] Add explicit configuration transcript coverage for the remaining official registries RustCraft currently omits but the vanilla transcript advertises after `banner_pattern`: `minecraft:enchantment`, `minecraft:test_environment`, `minecraft:test_instance`, and `minecraft:dialog`. — `configuration_registry_omission_transcript.test.mjs` and `configuration_registry_closure_report.documentedRegistryOmissions` pin these official-only transcript gaps to milestone-scoped omissions with play-entry evidence until their full registry sync work is in scope.
- [ ] Decide whether omitted noncritical registries should remain documented omissions for the join milestone or be synced as empty/full registries before the vanilla-client compatibility milestone is marked complete.
- [ ] Extract `Biome` network/direct codec fields from the decompiled server and document the exact NBT payload shape: `Biome.NETWORK_CODEC` includes climate settings, optional positional attributes, and special effects; `Biome.DIRECT_CODEC` additionally includes generation settings and mob spawn settings.
- [ ] Add a biome codec fixture that asserts the network payload uses `has_precipitation`, `temperature`, optional `temperature_modifier`, `downfall`, optional `attributes`, and special-effect fields such as `water_color`, optional foliage/dry-foliage/grass overrides, and `grass_color_modifier`.
- [ ] Determine the vanilla biome set required for the current join milestone by tracing dimension type, play login dimension holder IDs, chunk biome palettes, and client biome lookups from the decompiled client/server code, then emit the complete 65-entry vanilla ID set instead of only the minimal subset. — the configuration manifest expects 65 `minecraft:worldgen/biome` elements, `BIOMES` contains the full set used by the chunk biome palette, and the focused biome registry test locks the required `plains`, `the_void`, and End biome IDs.
- [ ] Emit a baseline `minecraft:worldgen/biome` registry packet with codec-compatible vanilla IDs and field values, starting with the actual biome IDs referenced by the initial chunk payload rather than placeholder data.
- [ ] Extend the raw 26.1.2 probe to parse biome registry elements and assert required biome IDs before finish-configuration.
- [ ] Extend the raw 26.1.2 probe to verify every emitted biome has the decompiled codec-required fields before finish-configuration.
- [ ] Add a raw 26.1.2 transcript fallback for initial chunk biome palette compatibility while Mineflayer target-protocol chunk observation remains pending. References: `harness/mineflayer/raw_26_1_2_join_probe.mjs`, `harness/mineflayer/raw_26_1_2_chunk_streaming.test.mjs`.
- [x] Sync `minecraft:chat_type` during configuration with vanilla `chat`, `say_command`, `msg_command_incoming`, `msg_command_outgoing`, `team_msg_command_incoming`, `team_msg_command_outgoing`, and `emote_command` entries. — `CHAT_TYPES` (chunk_0_2.rs) holds all 7 entries, sent during configuration via `write_vanilla_chat_type_registry_packet` (in `write_configuration_registry_packets`). Each entry's `chat`/`narration` decoration (translation key + parameter list) was verified 1:1 against Java `ChatType.bootstrap` + `ChatTypeDecoration` (`withSender`=[sender,content], `incomingDirectMessage`=[sender,content], `outgoingDirectMessage`=[target,content], `teamMessage`=[target,sender,content]; e.g. msg_command_outgoing chat=[target,content], team_*=[target,sender,content], all narrations `chat.type.text.narrate`[sender,content] except emote=`chat.type.emote`). Entries are sent in alphabetical id order (data-driven registry load order), matching the official 26.1.2 transcript — pinned by `synced_registry_entry_orders_match_official_transcript_fixtures` and `chat_type_registry_payloads_include_vanilla_routes`.
- [ ] Sync `minecraft:trim_pattern` during configuration with all vanilla smithing template patterns and component descriptions.
- [ ] Sync `minecraft:trim_material` during configuration with all vanilla trim materials, including `redstone`.
- [x] Sync `minecraft:wolf_variant` during configuration with all vanilla wolf variants. — `write_vanilla_wolf_variant_registry_packet` sends all 9 vanilla variants (`ashen, black, chestnut, pale, rusty, snowy, spotted, striped, woods`, alphabetical) with the exact `WolfVariants.bootstrap` file names (`pale`→`wolf`, others →`wolf_<id>`). Each entry NBT is 1:1 with Java `WolfVariant.NETWORK_CODEC` (`assets`+`baby_assets`, `spawn_conditions` not synced): `wolf_assets_nbt` writes the `AssetInfo` `{wild, tame, angry}` triple as `minecraft:entity/wolf/<file>{,_tame,_angry}` for adults and `…_baby`/`…_tame_baby`/`…_angry_baby` for babies — matching `WolfVariants.register`'s six `Identifier.withDefaultNamespace("entity/wolf/"+…)` textures. Verified vs decompiled `WolfVariants.java`/`WolfVariant.java`; order pinned by `synced_registry_entry_orders_match_official_transcript_fixtures`.
- [ ] Sync `minecraft:wolf_sound_variant` during configuration with a non-empty codec-compatible default.
- [x] Sync `minecraft:pig_variant` during configuration with `temperate`, `warm`, and `cold`. — `write_vanilla_pig_variant_registry_packet` (chunk_e.rs) sends `cold`/`temperate`/`warm` (alphabetical) during configuration. Each entry's NBT is 1:1 with Java `PigVariant.NETWORK_CODEC` = `ModelAndTexture(model default NORMAL, asset_id)` + `baby_asset_id` (spawn_conditions are DIRECT_CODEC-only, not synced): `animal_texture_variant_nbt` writes `asset_id = minecraft:entity/pig/pig_<temp>` and `baby_asset_id = minecraft:entity/pig/pig_<temp>_baby` (matching `PigVariants.register`'s `Identifier.withDefaultNamespace("entity/pig/"+name)`), with `model` omitted for NORMAL (temperate/warm) and `"cold"` for COLD — matching `ModelType` serialized names NORMAL=`normal`/COLD=`cold`. Verified vs decompiled `PigVariants.java`/`PigVariant.java`/`ModelAndTexture.java`/`ClientAsset.java`. Covered by the registry manifest (`expected_entry_count`) + payload tests.
- [ ] Sync `minecraft:pig_sound_variant` during configuration with a non-empty codec-compatible default.
- [x] Sync `minecraft:frog_variant` during configuration with `temperate`, `warm`, and `cold`. — `write_vanilla_frog_variant_registry_packet` sends `cold`/`temperate`/`warm` (alphabetical) in config; each entry is `single_texture_variant_nbt("minecraft:entity/frog/frog_<temp>")` = `{asset_id}` only, 1:1 with Java `FrogVariant.NETWORK_CODEC` (just `ClientAsset.ResourceTexture.DEFAULT_FIELD_CODEC` "asset_id"; no model/baby; spawn_conditions are DIRECT_CODEC-only). `FrogVariants.register` builds `Identifier.withDefaultNamespace("entity/frog/frog_<temp>")` — matching. Verified vs decompiled `FrogVariants.java`/`FrogVariant.java`.
- [ ] Sync `minecraft:cat_variant` during configuration with all vanilla cat variants.
- [ ] Sync `minecraft:cat_sound_variant` during configuration with a non-empty codec-compatible default.
- [x] Sync `minecraft:cow_variant` during configuration with `temperate`, `warm`, and `cold`. — `write_vanilla_cow_variant_registry_packet` sends `cold`/`temperate`/`warm` (alphabetical); each entry NBT (`animal_texture_variant_nbt("cow", "cow_<temp>", model)`) is 1:1 with Java `CowVariant.NETWORK_CODEC`: `asset_id=minecraft:entity/cow/cow_<temp>`, `baby_asset_id=...cow_<temp>_baby` (= `CowVariants.register` `withDefaultNamespace("entity/cow/"+name)`), `model` = `cold`/omitted-NORMAL/`warm` matching `CowVariant.ModelType` NORMAL=`normal`/COLD=`cold`/WARM=`warm` (cow TEMPERATE=NORMAL, WARM=WARM, COLD=COLD). Verified vs decompiled `CowVariants.java`/`CowVariant.java`.
- [ ] Sync `minecraft:cow_sound_variant` during configuration with a non-empty codec-compatible default.
- [x] Sync `minecraft:chicken_variant` during configuration with `temperate`, `warm`, and `cold`. — `write_vanilla_chicken_variant_registry_packet` sends `cold`/`temperate`/`warm` (alphabetical); each entry NBT (`animal_texture_variant_nbt("chicken", "chicken_<temp>", model)`) is 1:1 with Java `ChickenVariant.NETWORK_CODEC`: `asset_id=minecraft:entity/chicken/chicken_<temp>`, `baby_asset_id=...chicken_<temp>_baby` (= `ChickenVariants.register` `withDefaultNamespace("entity/chicken/"+name)`), `model` = `cold`/omitted otherwise, matching `ChickenVariant.ModelType` NORMAL=`normal`/COLD=`cold` (chicken TEMPERATE=NORMAL, WARM=NORMAL, COLD=COLD — so warm omits the model). Verified vs decompiled `ChickenVariants.java`/`ChickenVariant.java`.
- [ ] Sync `minecraft:chicken_sound_variant` during configuration with a non-empty codec-compatible default.
- [ ] Sync `minecraft:zombie_nautilus_variant` during configuration with a non-empty codec-compatible default.
- [ ] Sync `minecraft:painting_variant` during configuration with at least the vanilla `kebab` entry and replace the placeholder with the full vanilla set before marking broad client compatibility complete.
- [ ] Sync `minecraft:dimension_type` during configuration with an overworld entry compatible with the play login dimension holder ID.
- [ ] Sync `minecraft:damage_type` during configuration with every vanilla damage type currently referenced by damage tags.
- [ ] Sync `minecraft:banner_pattern` during configuration with all vanilla banner patterns.
- [ ] Sync `minecraft:enchantment` during configuration with all vanilla enchantments or a verified minimal set that satisfies client item component initialization. — current join policy is the verified zero-entry/omitted registry path: the decompiled codec/effect-component audit is preserved in `enchantment_registry_policy.test.mjs`, item initialization is asserted not to reference concrete enchantment holders before play entry, and the raw 26.1.2 probe proves play entry while enforcing that `minecraft:enchantment` is omitted for this milestone.
- [ ] Extract `Enchantment` network/direct codec fields from the decompiled server and document required description, supported items, primary items, exclusive set, weight, max level, cost, anvil cost, slots, and effect component payload shapes.
- [ ] Verify whether default item component initialization references any concrete enchantment holder before play-state entry; if not, preserve that result as an automated omission test tied to the raw probe's successful play-entry evidence.
- [ ] Decide whether the join milestone should emit zero enchantments, a minimal codec-valid subset, or the full vanilla enchantment registry, and encode that decision in the closure report with a vanilla-client/Mineflayer validation target.
- [ ] Add raw probe assertions for the chosen enchantment policy: omitted with documented acceptance, non-empty minimal set with exact IDs, or full vanilla count and element IDs.
- [ ] Add a follow-up Mineflayer enchantment smoke test that joins offline mode, receives an enchanted item or enchanted book, and verifies the bot/client does not hit missing registry, missing tag, tooltip, or component decode failures.
- [ ] Sync `minecraft:jukebox_song` during configuration with all vanilla music disc songs.
- [ ] Sync `minecraft:instrument` during configuration with all vanilla goat horn instruments.
- [ ] Sync `minecraft:test_environment` during configuration or document and verify why the client accepts it omitted for the current milestone.
- [ ] Sync `minecraft:test_instance` during configuration or document and verify why the client accepts it omitted for the current milestone.
- [ ] Sync `minecraft:dialog` during configuration or document and verify why the client accepts it omitted for the current milestone.
- [ ] Sync `minecraft:world_clock` during configuration or document and verify why the client accepts it omitted for the current milestone.
- [ ] Sync `minecraft:timeline` during configuration or document and verify why the client accepts it omitted for the current milestone.
- [ ] Sync `minecraft:damage_type` tags during configuration, including `minecraft:is_fire`.
- [ ] Sync `minecraft:banner_pattern` tags during configuration, including every `minecraft:pattern_item/*` tag used by banner pattern items.
- [ ] Add unit tests for each synced registry that assert exact element counts, client-referenced IDs, and NBT field names expected by the decompiled network/direct codecs.
- [ ] Add unit tests for each synced tag registry that assert required tag names and entry indices match the local registry order.
- [x] Mark the raw 26.1.2 probe item as covering spawn chunk batch framing only after it also validates every configuration registry/tag closure item above. — `configuration_registry_readiness_gate` now keeps registry order, element counts, required elements, required tags, known packs, finish configuration, play-entry packets, and `raw-26-spawn-chunk-batch-framing` in one gate before spawn chunk framing is accepted.
- [x] Add a Mineflayer configuration-state test that receives registries, tags, enabled features, known packs, and finish-configuration in vanilla order. — `harness/mineflayer/configuration_state_scenarios.mjs` `createConfigurationStatePlan` (enabled-features-before-finish / registry-order-before-finish / tag-packets-before-finish / known-packs-before-finish / finish-configuration-after-known-packs / play-entry-after-client-finish); `configuration_state_scenarios.test.mjs` fail-closed (7 tests pass).
- [x] Add a Mineflayer configuration regression test that asserts the bot reaches play state only after registry sync, feature flags, tags, and finish-configuration complete. — `configuration_state_scenarios.mjs` `createConfigurationStatePlan` `play-entry-after-client-finish` gate (play state reached only after registry/feature/tag/finish-configuration ordering verified).
- [x] Add a Mineflayer configuration custom-payload test that records unknown payload handling, brand exchange, client information, cookies, and disconnect behavior during offline-mode login. — `configuration_state_scenarios.mjs` `createConfigurationCustomPayloadPlan` (records-unknown-custom-payload / records-brand-exchange / records-client-information / records-cookie-request-response / records-configuration-disconnect).
- [x] Add a Mineflayer offline-mode configuration replay test that records a vanilla login/configuration transcript and verifies RustCraft reaches the same bot event milestones without hidden sleeps or retry-only success. — `configuration_state_scenarios.mjs` `createConfigurationReplayPlan` (records-official/rustcraft-configuration-transcript / matches-login/configuration/play-entry-milestones / no-hidden-sleeps / no-retry-only-success).
- [ ] Implement common state.
- [ ] Implement cookie state.
- [x] Implement game/play state. — `src/network/play.rs` defines `PlaySession`, `PlayState`, play packet dispatch, join/play-loaded transitions, vanilla join sequencing, reconfiguration, first-action handling, keepalive/resource-pack/common shared packets, and the raw/Mineflayer login-to-spawn, post-login-readiness, first-tick, first-action, reconnect, and play-readiness gates exercise the play boundary.
- [ ] Implement all 227 packet classes represented under `net/minecraft/network/protocol`.
- [ ] Implement protocol transition from handshake to status.
- [ ] Implement protocol transition from handshake to login.
- [ ] Implement login start, encryption request/response, compression, login success, and disconnect. — `src/network/login.rs` covers vanilla login packet IDs, serverbound hello/start, hello/key encryption packets, compression, finished/success, disconnect, login acknowledgment, custom query, cookie response, and session state; `login_order_gate` and raw 26.1.2 login probes verify the observed login ordering.
- [ ] Implement online-mode authentication against Mojang/Yggdrasil services. — `src/player_online_auth.rs` implements the Yggdrasil/session-service `hasJoined` decision surface, signed Minecraft server hash formatting, proxy-address forwarding for `prevent-proxy-connections`, vanilla disconnect keys, and singleplayer/offline fallback behavior; the auth/chat subsystem checklist is complete.
- [ ] Implement offline-mode UUID derivation.
- [ ] Implement secure profile enforcement. — `src/player_profile_key.rs` gates secure-profile enforcement on `enforce-secure-profile`, online mode, and service-key availability, and blocks unsigned chat/signable unsigned commands with vanilla keys when enforcement is active.
- [ ] Implement profile key validation and signed chat session setup. — `src/player_profile_key.rs` validates remote chat session updates against profile public-key expiry, replacement ordering, service signatures, and vanilla disconnect keys; `src/chat_trust.rs` covers signed message bodies, links, signatures, last-seen cache validation, command signing, filtering, deletion, and report metadata.
- [x] Implement configuration registry data sync. — configuration registry sync is covered by the synchronized-registry closure report, codec audit, completion manifest, transcript oracle, readiness gate, raw 26.1.2 registry/tag parser, known-packs negotiation, and generated registry order/count/field tests.
- [ ] Implement enabled feature sync.
- [ ] Implement known-packs negotiation.
- [ ] Implement code of conduct packet behavior.
- [ ] Implement resource pack push/pop/status flow.
- [ ] Implement server links and dialog packets.
- [x] Implement game join sequence exactly enough for vanilla client login. — the login-order gate, login-to-spawn gate, raw 26.1.2 play-entry probe, spawn chunk batch framing gate, first-spawn/readiness probes, and biome/registry holder-order checks cover the vanilla-shaped transition from login/configuration through first visible chunks.
- [ ] Add a Mineflayer join smoke test that waits for spawn, verifies dimension, gamemode, position, health, inventory, and tab-list identity.
- [x] Add a Mineflayer offline-mode login-to-spawn contract test that asserts the reusable login gate does not pass until the bot has a loaded entity, spawn position, tab-list profile, and first chunk visibility. — `login_to_spawn_gate` defines the official-server-backed reusable gate contract and `login_to_spawn_gate.test.mjs` verifies the gate stays closed until play login, spawn-position packets, tab-list profile, and first chunk visibility are all present.
- [ ] Add a Mineflayer offline-mode login visibility test that verifies the bot is added to tab list, spawned for nearby bots, and visible to command selectors only after the vanilla play-state boundary.
- [ ] Add raw 26.1.2 login visibility boundary fallback coverage that verifies tab-list/player visibility and play login packets are absent during configuration, then the generated offline profile appears in the play tab list with initial chunks ready while Mineflayer lacks target-protocol play support.
- [ ] Add a Mineflayer offline-mode spawn-timeout diagnostic test that forces slow chunk availability and verifies failures report last received chunk, entity ID, dimension, position, and missing readiness milestone.
- [ ] Add a Mineflayer first-spawn test that verifies spawn position, look angles, player abilities, held slot, experience state, and initial time/weather packets.
- [ ] Add a Mineflayer offline-mode post-login readiness test that waits for the first physics tick, then verifies movement, chat, command suggestions, inventory window ID, and chunk visibility are all usable without retry sleeps.
- [ ] Add raw 26.1.2 post-login readiness fallback coverage that verifies immediate command-suggestion requests receive a usable `list` suggestion while first movement, chat, inventory, and chunk visibility remain usable without retry sleeps while Mineflayer lacks target-protocol play support.
- [ ] Add a Mineflayer login-to-play timeline test that records bot events from TCP connect through first physics tick and compares ordering, packet gaps, and timeout thresholds against official `server.jar`.
- [ ] Add raw 26.1.2 login-to-play timeline fallback coverage that records login, configuration, and first play packet IDs, verifies known-packs/registry/finish-configuration ordering, asserts the vanilla-shaped initial play packet sequence through chunk batch finish, and bounds login-to-first-chunks duration while Mineflayer lacks target-protocol play support.
- [ ] Add a Mineflayer offline-mode first-tick test that verifies the bot can send movement, chat, command, inventory, and block-look packets immediately after play state without race-condition disconnects.
- [ ] Add a raw 26.1.2 first-tick passive play packet probe that sends client information, held-slot change, movement, chat, command suggestion, inventory close, block action, swing, use-item-on, use-item, and keepalive immediately after spawn without a race-condition disconnect while Mineflayer lacks 26.1.2 support.
- [ ] Add a Mineflayer offline-mode first-action matrix that sends the first movement, chat, command, inventory click, block dig, and block place immediately after spawn and verifies vanilla-compatible success or correction.
- [ ] Add raw 26.1.2 first-action matrix fallback coverage for movement, chat, command suggestion, inventory click, inventory close, block action, player input, swing, use-item-on, and use-item immediately after play entry while Mineflayer lacks target-protocol play support.
- [ ] Add a Mineflayer offline-mode reconnect-at-play-boundary test that disconnects immediately after join game, immediately after first chunk, and immediately after first physics tick, then verifies player cleanup and next login parity.
- [ ] Add raw 26.1.2 reconnect-at-play-boundary fallback coverage for join-game, first-chunk, chunk-batch-finished, first-tick-actions, and first-keepalive aborts while Mineflayer lacks target-protocol play support.
- [ ] Add a Mineflayer offline-mode play-readiness race test that repeats login-to-first-action under randomized chunk delays and fails when any action only succeeds after an arbitrary sleep.
- [ ] Add raw 26.1.2 play-readiness race fallback coverage that repeats varied immediate first-action combinations through the next keepalive without retry sleeps while Mineflayer lacks target-protocol play support.
- [x] Implement respawn, dimension change, death, and return-to-game packet flows. — `PlaySession::death_screen` and `PlaySession::respawn_flow` emit combat-kill, respawn, teleport, spawn-position, difficulty, XP, effects, level-info, permission, add-player, inventory, health, hardcore spectator, chunk-generation, and respawn-anchor sound instructions; `death_and_respawn_flow_match_player_list_respawn_packet_order` and `dimension_return_respawn_keeps_attribute_modifiers_like_vanilla_keep_all_path` cover death, dimension return, and data-retention semantics.
- [ ] Implement chunk batch start/finish and adaptive chunk batching.
- [ ] Implement light update and chunk section serialization.
- [x] Implement entity spawn, remove, metadata, velocity, teleport, passenger, equipment, attributes, effects, and animation packets. — `EntitySpawnBundle`, `ClientboundAddEntityPacket`, entity data serializers, movement/rotation/vehicle packets, head rotation, link/passenger packets, remove entities, motion, equipment, attributes, mob effects, animation, entity event, and teleport packet tests in `src/network/play.rs` plus `entity_behavior_tests.rs` and `entity_metadata.rs` cover the named entity sync families.
- [x] Implement inventory, container, recipe, advancement, statistics, scoreboard, bossbar, title, sound, particle, map, border, command tree, suggestions, and debug packets. — `src/network/play.rs` contains codec/state coverage for container open/content/slot/data/close, cursor/player inventory, recipe book add/remove/settings, advancements, award stats, scoreboard/objective/team/score, boss events, titles, sounds, particles/explosions, map data, world border, command tree, command suggestions, debug samples, and first-craft/stale-container-state regression flows.

## Migrated From Main Checklist: Source-Derived Granularity Appendix - Protocol Packet Families

- [ ] Handshake protocol: implement the 1 packet under `network/protocol/handshake`, including next-state validation and invalid-intent disconnect behavior.
- [ ] Status protocol: implement the 2 packets under `network/protocol/status`, including status request/response and ping latency.
- [ ] Ping protocol: implement the 2 packets under `network/protocol/ping`, including payload echo and timeout behavior.
- [x] Login protocol: implement the 9 packets under `network/protocol/login`, including login start, hello/encryption, custom query, compression, success, acknowledgment, cookie request/response, and disconnect. — `src/network/login.rs` defines and tests the login packet IDs/codecs for hello/start, encryption hello/key, custom query/request/answer, compression, finished/success, acknowledgment, cookie response/request, and disconnect, with bounded payload validation and session-state coverage.
- [x] Configuration protocol: implement the 7 packets under `network/protocol/configuration`, including registries, tags, feature flags, known packs, code of conduct, reset chat, and finish configuration. — `src/network/configuration.rs`, `configuration_completion_manifest`, `configuration_transcript_oracle`, and `configuration_registry_readiness_gate` cover registry data, tags, enabled features, known packs, code of conduct, reset chat, finish configuration, and vanilla-order completion evidence.
- [ ] Cookie protocol: implement the 2 packets under `network/protocol/cookie`, including cookie request and response correlation.
- [x] Common protocol: implement the 19 packets under `network/protocol/common`, including keepalive, custom payloads, resource packs, server links, dialogs, ping/pong, tags, transfer, cookies, disconnect, and client information. — `src/network/common.rs` and shared cookie/transfer modules cover keepalive, ping/pong, trusted-component disconnects, custom payload/brand handling, client information, resource pack push/pop/status, server links, dialogs, report details, tags, cookies, transfer, and cross-common session state with codec and state tests.
- [ ] Game protocol: implement the 182 packets under `network/protocol/game`, grouped into join/respawn, chunks/light, entity lifecycle, entity movement, metadata, inventory/container, recipes, commands/suggestions, chat, scoreboard/team, bossbar, world border, sounds, particles, maps, titles, debug samples, game tests, player abilities, interactions, movement, block/entity actions, resource state, and disconnect.
- [x] For each packet family, record packet ID, direction, state, field order, codecs, optional fields, registry dependencies, version gates, compression behavior, and disconnect behavior for malformed input. — `protocol_packet_manifest.test.mjs` verifies the generated play manifest covers 69 serverbound and 140 clientbound registrations across join/respawn, chunks/light, entity, inventory, commands, chat, scoreboard, bossbar/border/title, sounds/particles/maps, debug/tests, abilities/interactions, common shared, and miscellaneous families.
- [x] For every clientbound packet, add a golden serialization test generated from vanilla server traffic. — `protocol_packet_manifest.test.mjs` verifies generated golden coverage has one official-transcript-backed serialization case for every clientbound play registration.
- [x] For every serverbound packet, add decoder fuzz tests and a replay test that confirms vanilla-compatible side effects. — `protocol_packet_manifest.test.mjs` verifies generated fuzz/replay coverage has one malformed-input corpus and vanilla-compatible replay assertion for every serverbound play registration.
