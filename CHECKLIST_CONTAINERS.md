# Container Menu Checklist

## Relevant Java Source Files

- `decompiled-server-26.1.2/net/minecraft/world/inventory/AbstractContainerMenu.java` — base menu
- `decompiled-server-26.1.2/net/minecraft/world/inventory/Slot.java` — slot base
- `decompiled-server-26.1.2/net/minecraft/world/inventory/InventoryMenu.java` — player inventory
- `decompiled-server-26.1.2/net/minecraft/world/inventory/CraftingMenu.java` — 3×3 crafting
- `decompiled-server-26.1.2/net/minecraft/world/inventory/AbstractCraftingMenu.java` — crafting base
- `decompiled-server-26.1.2/net/minecraft/world/inventory/AbstractFurnaceMenu.java` — furnace base
- `decompiled-server-26.1.2/net/minecraft/world/inventory/FurnaceMenu.java` — furnace
- `decompiled-server-26.1.2/net/minecraft/world/inventory/BlastFurnaceMenu.java` — blast furnace
- `decompiled-server-26.1.2/net/minecraft/world/inventory/SmokerMenu.java` — smoker
- `decompiled-server-26.1.2/net/minecraft/world/inventory/AnvilMenu.java` — anvil
- `decompiled-server-26.1.2/net/minecraft/world/inventory/BeaconMenu.java` — beacon
- `decompiled-server-26.1.2/net/minecraft/world/inventory/BrewingStandMenu.java` — brewing stand
- `decompiled-server-26.1.2/net/minecraft/world/inventory/CartographyTableMenu.java` — cartography table
- `decompiled-server-26.1.2/net/minecraft/world/inventory/CrafterMenu.java` — crafter block
- `decompiled-server-26.1.2/net/minecraft/world/inventory/EnchantmentMenu.java` — enchanting table
- `decompiled-server-26.1.2/net/minecraft/world/inventory/GrindstoneMenu.java` — grindstone
- `decompiled-server-26.1.2/net/minecraft/world/inventory/LecternMenu.java` — lectern
- `decompiled-server-26.1.2/net/minecraft/world/inventory/LoomMenu.java` — loom
- `decompiled-server-26.1.2/net/minecraft/world/inventory/SmithingMenu.java` — smithing table
- `decompiled-server-26.1.2/net/minecraft/world/inventory/StonecutterMenu.java` — stonecutter
- `decompiled-server-26.1.2/net/minecraft/world/inventory/ChestMenu.java` — chest / double-chest
- `decompiled-server-26.1.2/net/minecraft/world/inventory/DispenserMenu.java` — dispenser/dropper
- `decompiled-server-26.1.2/net/minecraft/world/inventory/HopperMenu.java` — hopper
- `decompiled-server-26.1.2/net/minecraft/world/inventory/ShulkerBoxMenu.java` — shulker box
- `decompiled-server-26.1.2/net/minecraft/world/inventory/HorseInventoryMenu.java` — horse/llama
- `decompiled-server-26.1.2/net/minecraft/world/inventory/NautilusInventoryMenu.java` — happy ghast harness
- `decompiled-server-26.1.2/net/minecraft/world/inventory/MerchantMenu.java` — villager trading
- `decompiled-server-26.1.2/net/minecraft/world/inventory/MerchantContainer.java` — merchant container
- `decompiled-server-26.1.2/net/minecraft/world/inventory/MenuType.java` — menu type registry
- `RustCraft/src/inventory.rs` — RustCraft inventory
- `RustCraft/src/inventory_transactions.rs` — RustCraft inventory click handling
- `RustCraft/src/player_inventory.rs` — RustCraft player inventory

## AbstractContainerMenu Infrastructure

- [x] Track per-player state-ID counter: Java 26.1.2 `AbstractContainerMenu` owns `stateId`, exposes `getStateId()`/`incrementStateId()`, and `ServerGamePacketListenerImpl.handleContainerClick()` validates `ServerboundContainerClickPacket.stateId()` against the current menu state; Rust `PlaySession` owns `container_state_id`, resets it on login/join setup, and exposes `apply_scripted_container_click()` to validate against `apply_scripted_packet` and advance only accepted transactions; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 play_session_container_state_id_advances_only_after_accepted_click`.
- [x] Implement `Slot` list: Java 26.1.2 `AbstractContainerMenu` owns an ordered `NonNullList<Slot>`, and `Slot` exposes `mayPlace`, `mayPickup`, `getMaxStackSize`, `hasItem`, `tryRemove`/`remove`, and set/insert paths used by click handling; Rust `inventory.rs` models the shared slot surface with `Slot { may_place, may_pickup, max_stack_size }`, `has_item()`, `safe_take()`, `safe_insert()`, and ordered `Menu.slots: Vec<Slot>` used by pickup, quick-move, hotbar-swap, clone, throw, quick-craft, and pickup-all handlers; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 slot`.
- [x] Implement carried-item stack (cursor): Java 26.1.2 `AbstractContainerMenu` owns `carried`, exposes `getCarried()`/`setCarried()`, and routes it through pickup, outside-drop, clone, quick-craft, throw, and pickup-all handling; Rust `Menu.carried` tracks the cursor item through the same modeled click modes, `apply_scripted_packet()` validates client-reported carried state, and `remote_carried` drives correction/synchronizer output; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 carried`.
- [x] Implement remote-slot shadow copies: Java 26.1.2 `AbstractContainerMenu` keeps `remoteSlots` and `remoteCarried`, forces them during `sendAllDataToRemote()`, and only calls `sendSlotChange()`/`sendCarriedChange()` when the remote shadow no longer matches the current slot or carried stack; Rust `Menu.remote_slots` and `remote_carried` mirror that client-view shadow, with `send_all_data_to_remote()`, `send_slot_change()`, `send_carried_change()`, and `broadcast_changes()` emitting only changed slot/carried stacks; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 remote_slot_shadow_copies_only_report_changed_slots_and_carried`.
- [x] Implement `DataSlot` / `ContainerData` sync: Java 26.1.2 `DataSlot.forContainer()` keeps live access to `ContainerData`, `prevValue` starts at zero, `AbstractContainerMenu.addDataSlots()` adds one `DataSlot` per container value, `sendAllDataToRemote()` seeds `remoteDataSlots`, and `broadcastChanges()` only sends data changes when the remote integer shadow differs; Rust `DataSlot::for_container()`, shared `ContainerData` backing storage, zero-initialized `previous_value`, `Menu.remote_data_slots`, `send_all_data_to_remote()`, `send_data_change()`, and `broadcast_changes()` now match that model; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 data_slot`.
- [x] Implement click validation: Java 26.1.2 `ServerGamePacketListenerImpl.handleContainerClick()` validates the active container ID and slot index, routes through `AbstractContainerMenu.clicked()` for PICKUP, QUICK_MOVE, SWAP, CLONE, THROW, QUICK_CRAFT, and PICKUP_ALL, mirrors client changed slots/carried into remote shadows, then broadcasts either full state or deltas after stale state detection; Rust `inventory_transactions.rs::apply_scripted_packet()` validates state IDs, dispatches all seven `ContainerInput` modes, treats invalid PICKUP slots as no-ops instead of outside drops/panics, and collects slot/carried corrections against the server menu state; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 scripted_invalid_pickup_slot_is_noop_without_treating_it_as_outside_drop` and `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 scripted_throw_clone_quickcraft_and_pickup_all_modes_are_mapped`. Full Java quick-craft drag-stage parity remains tracked separately below.
- [x] Implement quick-craft (drag-split): Java 26.1.2 `AbstractContainerMenu.clicked()` decodes `getQuickcraftHeader(button)`/`getQuickcraftType(button)`, tracks START/CONTINUE/END drag stages, only allows middle-drag clone for creative players, computes per-slot counts with `getQuickCraftPlaceCount()`, and falls back to normal pickup when a drag ends with one slot; Rust `Menu.click_quick_craft()` mirrors the packet-stage state machine instead of trusting client `changed_slots`, keeps the compatibility `Menu.quick_craft()` helper on the same placement path, and `apply_scripted_packet()` now routes QUICK_CRAFT packets by slot/button mask before correction validation; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 quick_craft` and `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 scripted_throw_clone_quickcraft_and_pickup_all_modes_are_mapped`.
- [x] Implement `ContainerSynchronizer` and `ContainerListener` interfaces used to push updates back to client: Java 26.1.2 `ContainerListener` exposes `slotChanged`/`dataChanged`, `ContainerSynchronizer` exposes initial, slot, carried, and data sync methods, `setSynchronizer()` immediately sends full menu state, and `broadcastChanges()` routes local listener events plus remote synchronizer deltas; Rust `Menu` tracks listener/synchronizer event sinks, `set_synchronizer()` sends Java-style initial data, and `broadcast_changes()` emits slot, carried, and data changes from the same local/remote shadows; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 changes`.
- [x] Implement `stillValid(player)` check on every tick: Java 26.1.2 `AbstractContainerMenu.stillValid(access, player, block)` requires the access block to still match and delegates to `Player.isWithinBlockInteractionRange(pos, 4.0)`, which measures the player's eye position against the block AABB with `block_interaction_range + buffer` and a strict less-than boundary; Rust `MenuValidity` models always-valid, block-backed, and block-entity-backed menus, and `tick_validity()` returns `CloseMenu` when the expected access target is gone/replaced or the player is outside the Java eye-to-AABB range; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 still_valid` and `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 block_interaction_range_uses_java_eye_to_aabb_distance`.
- [x] Add parity test: stateId desync detection - Java 26.1.2 `ServerboundContainerClickPacket` carries `stateId`, `ServerGamePacketListenerImpl.handleContainerClick()` compares it with `AbstractContainerMenu.getStateId()`, then stale clients are corrected through the menu resync path; Rust `network::play::tests::stale_container_state_id_corrections_become_set_slot_packets` verifies stale client state leaves the server state unchanged and converts `SlotCorrection`s into `ContainerSetSlot` packets, including slot `-1` for carried-item correction; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 stale_container_state_id_corrections_become_set_slot_packets`.

## Player and Crafting Menus

### In-inventory 2×2 crafting — prerequisite chain (must be done in order)

- [x] **Connect `CraftingGrid` to `RecipeMap`**: Java 26.1.2 `InventoryMenu.slotsChanged()` routes its 2x2 `CraftingContainer` through `CraftingMenu.slotChangedCraftingGrid()`, which calls `RecipeManager.getRecipeFor(RecipeType.CRAFTING, input, level, hint)`, stores the recipe on the result container, and assembles the output; Rust `player_inventory.rs` builds `Vec<Option<&'static str>>` from grid slots, calls `RecipeMap::get_recipe_for("crafting", width, height, items)`, stores the matched recipe holder ID, and assembles the result via `holder.recipe.assemble()`; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 crafting_grid_updates_result_and_consumes_inputs_after_take`.

- [x] **Implement `InventoryMenu` slot layout**: Java 26.1.2 `InventoryMenu` defines result=0, crafting grid=1-4, armor=5-8 backed by inventory slots 39..36 in head/chest/legs/feet order, main storage=9-35, hotbar=36-44, and offhand=45; Rust `player_inventory::InventoryMenu` owns a `PlayerInventory` + `CraftingGrid`, exposes the same slot mapping, delegates reads/writes to the backing inventory or grid, and rejects placement into slot 0; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 inventory_menu_maps_vanilla_slots_to_backing_inventory_and_crafting_grid`.

- [x] **Implement `ResultSlot` on-take side effects**: Java 26.1.2 `ResultSlot.onTake()` checks crafting achievements/used recipes, removes one item from each active crafting input, applies `CraftingRecipe.getRemainingItems()` / default crafting reminders, and refills matching remainder stacks; Rust `InventoryMenu::take_result()` returns the assembled stack, shrinks each non-empty crafting-grid slot, applies `RecipeKind::get_remaining_items()` / default crafting remainders such as bucket returns, refreshes the result, and records a first-craft recipe-book unlock event for the matched recipe holder ID; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 inventory_menu_result_take_consumes_inputs_remainders_and_unlocks_recipe_once`.

- [x] **Thread per-player state ID through the click handler**: Java 26.1.2 stores `AbstractContainerMenu.stateId` as an `int`, sends it as a VarInt, and advances it for accepted menu updates; Rust tracks `container_state_id: i32` on `PlaySession`/play state, seeds it to 0 on login/join, threads it through `process_pending_container_click()` / `handle_container_click()`, and increments it after accepted player-inventory click transactions; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 pending_container_click_updates_inventory_menu_result_and_unlocks_recipe`.

- [x] **Route `ServerboundContainerClickPacket` → `apply_scripted_packet`**: Java 26.1.2 `ServerGamePacketListenerImpl.handleContainerClick()` verifies the active container ID, checks stale `stateId`, calls `AbstractContainerMenu.clicked()`, mirrors the client's changed remote slots/carried item, then broadcasts either a full state or changed slots; Rust play dispatch decodes `SERVERBOUND_CONTAINER_CLICK_PACKET_ID` into `last_container_click`, `process_pending_container_click()` routes container-id 0 to `handle_container_click()`, converts the click to `ScriptedContainerClickPacket`, applies it to the active `InventoryMenu`, emits `ContainerSetSlot` for changed menu slots (including slot 0 when crafting-grid changes refresh the result), and emits `SetCursorItem` for carried-stack changes; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 small_play_packets_round_trip_vanilla_codecs`, `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 pending_container_click_updates_inventory_menu_result_and_unlocks_recipe`, and `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 crafting_grid_log_to_planks_full_round_trip`.

- [x] **Send `ContainerSetContent` on login**: Java 26.1.2 `PlayerList.sendAllPlayerInfo()` calls `player.inventoryMenu.sendAllDataToRemote()`, and `ServerPlayer.containerSynchronizer.sendInitialData()` sends `ClientboundContainerSetContentPacket(containerId, incrementStateId(), slotItems, carriedItem)`; Rust `write_minimal_play_join()` sends `CLIENTBOUND_CONTAINER_SET_CONTENT_PACKET_ID` for container-id 0 after the login/recipe-book setup, serializing all 46 `InventoryMenu` slots plus the carried cursor item, and `write_inventory_menu_full_sync()` shares the same 46-slot content shape for later full resyncs; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 inventory_menu_full_sync_writes_all_slots_and_carried_item`.

- [x] **Zone-aware `quick_move` for `InventoryMenu`**: Java 26.1.2 `InventoryMenu.quickMoveStack()` routes result-slot stacks through `moveItemStackTo(stack, 9, 45, true)`, crafting/armor/offhand source slots through `9..45` forward, storage slots into an empty matching equipment slot (including shield/offhand) before hotbar `36..45`, and hotbar slots into storage `9..36`; Rust `InventoryMenu::quick_move()` now preserves failed result moves, uses the same forward/backward slot scan order, and derives equipment targets from the item equippable component with armor suffix fallback; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 inventory_menu_quick_move_uses_vanilla_inventory_zones`.

- [x] **Recipe-book unlock on first craft**: Java 26.1.2 `ResultSlot.checkTakeAchievements()` calls `RecipeCraftingHolder.awardUsedRecipes()`, which calls `ServerPlayer.awardRecipes()`, and `ServerRecipeBook.addRecipes()` adds only unknown non-special recipes, highlights them, and sends `ClientboundRecipeBookAddPacket(..., false)` with show-notification and highlight flags; Rust `InventoryMenu::take_result()` records first-craft recipe IDs in `unlocked_recipes`/`highlighted_recipes`, emits each unlock only once through `RecipesUnlocked`, and the runtime converts those IDs to `ClientboundRecipeBookAddPacket` entries with `notification=true`, `highlight=true`, `replace=false`; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 crafting_grid_result_updates_per_slot_change_stale_id_corrected_no_double_unlock` and `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 first_craft_recipe_unlock_builds_highlighted_notification_packet`.

### Remaining menu types (not needed for 2×2 crafting, implement after above is working)

- [x] Implement `CraftingMenu`: Java 26.1.2 `CraftingMenu` lays out result slot 0, 3×3 grid slots 1..9, player storage 10..36, and hotbar 37..45; `slotsChanged()` calls `slotChangedCraftingGrid()` to match a `RecipeHolder<CraftingRecipe>` and update the result slot; `quickMoveStack()` routes result stacks to player slots 10..46 in reverse and only consumes inputs after the move succeeds; `ResultSlot.onTake()` consumes ingredients, applies remainder items, and awards the used recipe once. Rust `container_menus::CraftingMenu` mirrors the slot layout, recipe matching, quick-move zones, result remainder handling, and first-craft unlock/highlight event tracking; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 crafting_menu_layout_and_recipe_matching_match_vanilla`, `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 crafting_menu_quick_move_result_goes_to_player_inventory`, `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 crafting_menu_result_take_applies_remainders_and_unlocks_recipe_once`, and `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 crafting_menu_failed_result_quick_move_preserves_inputs_and_result`.
- [x] Implement `AbstractCraftingMenu`: Java 26.1.2 owns the shared crafting grid/result containers, exposes result/input grid slots to `RecipeBookMenu`, and delegates result recomputation plus remainder handling through `CraftingMenu.slotChangedCraftingGrid()` and `ResultSlot.onTake()`; Rust shares the same behavior inside `CraftingMenu::slots_changed()`, `consume_inputs_and_refresh()`, and the 3×3 slot accessors; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 crafting_menu_result_take_applies_remainders_and_unlocks_recipe_once`.
- [x] Implement `RecipeBookMenu`: Java 26.1.2 `RecipeBookMenu` exposes `getRecipeBookType()` and `handlePlacement(...)`, `InventoryMenu`/`CraftingMenu` report `RecipeBookType.CRAFTING`, furnace wrappers report FURNACE/BLAST_FURNACE/SMOKER, and `ServerGamePacketListenerImpl.handlePlaceRecipe()` requires the active container id plus an unlocked recipe before placing ingredients; Rust exposes matching recipe-book types for `InventoryMenu`, `CraftingMenu`, and furnace menus, persists/syncs recipe-book settings and highlighted recipes, routes `ServerboundPlaceRecipePacket` through unlocked-recipe placement into the 2x2 inventory grid, advances state only on accepted placement, and now rejects wrong container ids before mutating the grid; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 furnace_wrappers_use_vanilla_recipe_book_types` and `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 place_recipe_packet`.

### Tests

- [x] Unit test: `CraftingGrid::update_result` - `crafting_grid_updates_result_and_consumes_inputs_after_take` covers one oak log producing 4 oak planks, empty result after taking output, two-stick shapeless matching, and an incomplete shaped crafting-table pattern producing no result; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 crafting_grid_updates_result_and_consumes_inputs_after_take`.
- [x] Unit test: `InventoryMenu` slot 0 rejects insert/take side effects - `inventory_menu_maps_vanilla_slots_to_backing_inventory_and_crafting_grid` verifies result-slot placement rejection, and `inventory_menu_result_take_consumes_inputs_remainders_and_unlocks_recipe_once` verifies result take shrinks inputs, returns bucket remainders, refreshes the result, and emits the recipe unlock only once; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 inventory_menu_maps_vanilla_slots_to_backing_inventory_and_crafting_grid` and `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 inventory_menu_result_take_consumes_inputs_remainders_and_unlocks_recipe_once`.
- [x] Unit test: `InventoryMenu` zone-aware `quick_move` places crafting result into hotbar before storage, shifts a hotbar item into storage, shifts storage into hotbar, prefers matching equipment slots for armor/offhand items, and preserves a failed result-slot shift-click; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 inventory_menu_quick_move_uses_vanilla_inventory_zones`.
- [x] Parity test (full network round-trip): place one log into grid slot 1 → server sends `ContainerSetSlot` slot 0 with 4 planks → client takes result → server sends `ContainerSetSlot` slot 0 empty and slot 1 empty; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 crafting_grid_log_to_planks_full_round_trip`.
- [x] Add parity test: 2×2 crafting grid result update on each slot change, recipe unlocking, no double-unlock on second craft of same recipe; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 crafting_grid_result_updates_per_slot_change_stale_id_corrected_no_double_unlock`.

## Furnace Menus

- [x] Implement `AbstractFurnaceMenu`: input slot (0), fuel slot (1), result slot (2); `ContainerData` sync for `litTime`, `litDuration`, `cookingProgress`, `cookingTotalTime`; `FurnaceFuelSlot` slot type restriction; `FurnaceResultSlot` on-take XP release — `container_menus::AbstractFurnaceMenu` (39 slots, `data: [i16; 4]`, fuel slot accepts fuel-tag items + buckets at max-stack 1, result slot rejects placement, `burn_progress`/`lit_progress` mirror Java `AbstractFurnaceMenu`, and result extraction queues the recipe/XP award side effect from Java `FurnaceResultSlot.checkTakeAchievements`); covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 furnace_menu_layout_and_fuel_restrictions_match_vanilla`, `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 furnace_quick_move_result_to_player_and_storage_to_input`, and `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 furnace_result_extraction_releases_xp_and_clears_recipe_ledger`.
- [x] Implement `FurnaceMenu`: Java 26.1.2 `FurnaceMenu` passes `RecipeType.SMELTING`, `RecipePropertySet.FURNACE_INPUT`, and `RecipeBookType.FURNACE` into `AbstractFurnaceMenu`; Rust `container_menus::FurnaceMenu` wraps `AbstractFurnaceMenu::new(FurnaceKind::Furnace, ...)`, which reports `RecipeBookType::Furnace`; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 furnace_wrappers_use_vanilla_recipe_book_types`.
- [x] Implement `BlastFurnaceMenu`: Java 26.1.2 `BlastFurnaceMenu` passes `RecipeType.BLASTING`, `RecipePropertySet.BLAST_FURNACE_INPUT`, and `RecipeBookType.BLAST_FURNACE`; Rust `container_menus::BlastFurnaceMenu` wraps `FurnaceKind::BlastFurnace`, which reports `RecipeBookType::BlastFurnace`; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 furnace_wrappers_use_vanilla_recipe_book_types`.
- [x] Implement `SmokerMenu`: Java 26.1.2 `SmokerMenu` passes `RecipeType.SMOKING`, `RecipePropertySet.SMOKER_INPUT`, and `RecipeBookType.SMOKER`; Rust `container_menus::SmokerMenu` wraps `FurnaceKind::Smoker`, which reports `RecipeBookType::Smoker`; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 furnace_wrappers_use_vanilla_recipe_book_types`.
- [x] Add parity test: fuel slot accepts only fuel items, result slot releases XP on extraction, progress data sync timing — `furnace_menu_layout_and_fuel_restrictions_match_vanilla` covers fuel restrictions, bucket max-stack, result placement rejection, and `ContainerData` progress helpers; `furnace_result_extraction_releases_xp_and_clears_recipe_ledger` covers direct/shift-click result extraction awarding and clearing the recipe ledger; `furnace_quick_move_result_to_player_and_storage_to_input` covers Java quick-move zones; covered by the focused `cargo test` filters listed on the `AbstractFurnaceMenu` row.

## Workstation Menus

- [ ] Implement `AnvilMenu` (`ItemCombinerMenu`): input-left (0), input-right (1), result (2); rename-text input via custom payload; cost calculation (XP cost, `too expensive` threshold at cost ≥ 40 in survival); prior-work cost accumulation — `container_menus::AnvilMenu` with `ANVIL_MAX_COST = 40`, `set_item_name`, `set_result_from_inputs` (renaming path complete; full enchantment-combine path TODO once enchantment data is on `ItemStack`)
- [ ] Implement `BeaconMenu`: payment slot, primary effect selector, secondary effect selector; `ContainerData` for beacon level; cost validation (accepts emerald/diamond/gold/iron/netherite/amethyst) — `container_menus::BeaconMenu` with `BEACON_PAYMENT_ITEMS`
- [ ] Implement `BrewingStandMenu`: ingredient (3), fuel (4), potion slots (0-2); `ContainerData` for `brewTime` and `fuelLevel` — `container_menus::BrewingStandMenu`
- [ ] Implement `CartographyTableMenu`: map input (0), paper/map (1), result (2); extend vs. clone vs. lock behavior based on input combination — `container_menus::CartographyTableMenu` (slot restrictions enforced, result computation deferred to consumer with the `MapItemSavedData` plumbing)
- [ ] Implement `CrafterMenu`: 9 craftable grid slots + 1 result slot; per-slot enabled/disabled toggle via interaction; comparator output integration — `container_menus::CrafterMenu` with `container_data: [i32; 10]` (9 enable flags + 1 powered)
- [ ] Implement `EnchantmentMenu`: item input (0), lapis input (1); `ContainerData` for 3 enchantment costs and 3 enchantment hints; require lapis and min-level check — `container_menus::EnchantmentMenu` with `data: [i32; 6]`
- [ ] Implement `GrindstoneMenu`: input-left (0), input-right (1), result (2); disenchant + repair behavior; XP release on take proportional to removed enchantments — `container_menus::GrindstoneMenu` (slot restrictions + result-slot rejection of placement; XP/disenchant on take deferred to consumer)
- [ ] Implement `LecternMenu`: book display, page-turn packet handling — `container_menus::LecternMenu::click_button`
- [ ] Implement `LoomMenu`: banner input (0), dye input (1), pattern item (2), result (3); available patterns filtered by banner patterns in registry + held pattern item — `container_menus::LoomMenu`
- [ ] Implement `SmithingMenu` (`ItemCombinerMenu`): template (0), base (1), addition (2), result (3); template validation, trim vs. transform recipe matching — `container_menus::SmithingMenu`
- [ ] Implement `StonecutterMenu`: input (0), result (1); recipe list from `minecraft:stonecutting` type; selected-recipe index tracked — `container_menus::StonecutterMenu::selected_recipe_index`
- [ ] Add parity test: anvil cost calculation for rename-only, repair, enchant-combine, and prior-work penalty — `anvil_result_slot_rejects_placement_and_renaming_costs_one`
- [ ] Add parity test: enchanting table slot costs and enchantment hints for seeded items — `enchant_menu_lapis_only_in_lapis_slot_and_item_slot_max_1`
- [ ] Add parity test: grindstone XP release proportional to enchantment cost removed — `grindstone_input_accepts_damageable_only_and_result_rejects_placement`

## Storage and Transfer Menus

- [x] Implement `ChestMenu`: Java 26.1.2 `ChestMenu` supports one through six 9-wide rows, adds chest slots before the standard 27-slot player storage plus 9-slot hotbar, and uses six rows for the 54-slot double-chest menu; Rust `container_menus::ChestMenu::new(rows)` enforces rows `1..=6`, exposes `rows * 9` chest slots followed by the standard 36 player slots, and mirrors Java quick-move chest↔player directions; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 chest_menu_supports_one_through_six_rows_and_player_inventory_append` and `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 chest_menu_quick_move_shifts_between_chest_and_inventory`.
- [x] Implement `DispenserMenu`: Java 26.1.2 `DispenserMenu` checks a 9-slot container, adds a 3×3 grid before standard player inventory slots, and shift-clicks between grid `0..9` and player `9..45`; Rust `container_menus::DispenserMenu` exposes the same 45-slot layout and transfer direction; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 dispenser_menu_layout_and_quick_move`.
- [x] Implement `HopperMenu`: Java 26.1.2 `HopperMenu` checks a 5-slot container, adds five horizontal hopper slots before standard player inventory slots, and shift-clicks between hopper `0..5` and player `5..41`; Rust `container_menus::HopperMenu` exposes the same 41-slot layout and transfer direction; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 hopper_menu_5_slots_and_quick_move_to_player`.
- [x] Implement `ShulkerBoxMenu`: Java 26.1.2 `ShulkerBoxMenu` checks a 27-slot container, uses `ShulkerBoxSlot` before standard player inventory slots, and `ShulkerBoxSlot.mayPlace()` rejects block items whose block is a `ShulkerBoxBlock`; Rust `container_menus::ShulkerBoxMenu` exposes 27 shulker slots plus 36 player slots, rejects base and dyed shulker boxes from shulker slots, and mirrors Java quick-move directions; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 shulker_box_rejects_nested_shulker_boxes`.
- [x] Implement `HorseInventoryMenu`: Java 26.1.2 `HorseInventoryMenu` adds saddle slot `0`, body armor slot `1`, optional `inventoryColumns * 3` mount storage slots starting at `2`, then standard player inventory; Rust `container_menus::HorseInventoryMenu` mirrors that layout through `HorseLayout`, respects inactive saddle/body slots, accepts Java horse armor IDs plus llama carpets, and uses the shared mount quick-move priority; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 horse_inventory_saddle_and_armor_slot_restrictions` and `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 horse_inventory_quick_move_prioritizes_mount_slots_then_storage`.
- [x] Implement `AbstractMountInventoryMenu`: Java 26.1.2 `AbstractMountInventoryMenu.quickMoveStack` moves mount slots to player inventory, then prioritizes body armor, saddle, mount storage, and main/hotbar fallback zones; Rust encapsulates the same transfer ordering in `HorseInventoryMenu` and `NautilusInventoryMenu`; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 horse_inventory_quick_move_prioritizes_mount_slots_then_storage` and `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 nautilus_inventory_quick_move_prioritizes_equipment_slots`.
- [x] Implement `NautilusInventoryMenu` (new in 26.1.2): Java 26.1.2 `NautilusInventoryMenu` adds saddle slot `0`, body armor slot `1`, then standard player inventory; Rust `container_menus::NautilusInventoryMenu` mirrors the 38-slot layout, accepts Java nautilus armor IDs, and uses the shared mount quick-move priority; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 nautilus_inventory_saddle_and_armor_restrictions` and `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 nautilus_inventory_quick_move_prioritizes_equipment_slots`.
- [x] Add parity test: double-chest row count = 6, slot offset between chest and player inventory — `chest_menu_supports_one_through_six_rows_and_player_inventory_append` now verifies row counts 1..=6, total slot count `rows * 9 + 36`, player inventory start `rows * 9`, and hotbar start `rows * 9 + 27`.
- [x] Add parity test: horse inventory saddle slot restriction (accepts only saddle item), armor slot restriction — `horse_inventory_saddle_and_armor_slot_restrictions` now verifies saddle-only placement, Java horse armor IDs, llama carpet armor, inactive saddle/body slots, and rejection of non-horse body armor.

## Merchant Menu

- [x] Implement `MerchantMenu`: Java 26.1.2 `MerchantMenu` exposes a 3-slot `MerchantContainer` (input-left, input-right, result) plus appended player inventory, `setSelectionHint()` / `tryMoveItems()` track and refill the selected offer, `quickMoveStack()` moves result/payment/player stacks through the vanilla zones, `MerchantResultSlot.onTake()` consumes payment, increments offer uses through `notifyTrade()`, and adds `offer.getXp()` to villager XP; Rust `container_menus::MerchantMenu` mirrors the 3-slot slot view, appended player inventory, quick-move zones, and select-offer refill path, while `MerchantContainer::take_result()` consumes either payment order, increments uses, refreshes the result, and now tracks villager XP; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 merchant_result_take_increments_uses_and_villager_xp` and `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 merchant_`.
- [x] Implement `MerchantContainer`: Java 26.1.2 `MerchantContainer.updateSellItem()` selects an active offer from payment slots, exposes `canTrade()` via the active offer/result state, and refreshes the result/future XP when payment or selection hint changes; Rust `MerchantContainer` mirrors the 3-slot payment/result container, selection hint, `prepare_trade()`, `can_trade()`, `future_xp()`, and result refresh behavior; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 merchant_container_selects_active_offer_and_clears_out_of_stock_results` and `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 merchant_container_respects_selection_hint_and_payment_slots`.
- [x] Implement offer selection: Java 26.1.2 `MerchantMenu.tryMoveItems(newTradeIndex)` pushes active payment slots back into player inventory and re-fills cost A/B from inventory; Rust `container_menus::MerchantMenu::try_move_items` mirrors out-of-bounds ignore, payment push-back, cost-A refill, and optional cost-B refill; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 merchant_menu_try_move_items`.
- [x] Implement demand mechanics: Java 26.1.2 `MerchantOffer.updateDemand()` applies `demand = demand + uses - (maxUses - uses)`, modified cost clamps to the item max stack size, and `Villager.updateSpecialPrices()` applies hero-of-the-village reductions; Rust `MerchantOffer::cost_a_count`, `MerchantContainer::update_demand`, `apply_hero_discount(amplifier)`, and `reset_special_prices()` mirror those formulas; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 merchant_`.
- [x] Implement trade-use counting: Java 26.1.2 `MerchantResultSlot.onTake()` consumes payment in either slot order, calls `MerchantOffer.take(...)`, and increments uses through the merchant notification path; Rust `MerchantContainer::take_result` now consumes payment in both slot orders and increments `uses` on successful trades, with `MerchantOffer::{increase_uses,is_out_of_stock,reset_uses}` and `reward_exp`; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 merchant_container_take_result_consumes_swapped_payment_slot` and `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 merchant_`.
- [x] Implement restock: Java 26.1.2 `Villager.restock()` updates demand before clearing uses and caps restocks to twice per day with a 2400-tick cooldown; Rust `MerchantContainer::restock()` runs `update_demand()` then clears uses, and `MerchantRestockTracker` mirrors `numberOfRestocksToday` / `lastRestockGameTime`; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 merchant_`.
- [x] Add Mineflayer merchant-menu test: Java 26.1.2 `MerchantMenu.setSelectionHint()` / `tryMoveItems()` refill payment slots, `quickMoveStack()` shift-clicks the result into the player inventory, `MerchantOffer` tracks `uses`, `maxUses`, demand, special price, and XP, `Villager.restock()` updates demand and resets offers, and `MerchantMenu.removed()` returns payment/cursor stacks on close; `harness/mineflayer/merchant_menu_scenarios.{mjs,test.mjs}` now defines fail-closed plan scenarios for open/select/result verification, shift-click trade, rejected payment, stale offer selection, XP/price updates, offer exhaustion, restock timing, close/reopen, disconnect mid-trade, and close-while-carrying; covered by `node --test merchant_menu_scenarios.test.mjs window_lifecycle_scenarios.test.mjs` from `harness/mineflayer`.
- [x] Add parity test: demand multiplier increment after each purchase, price-restore after restock — covered by `merchant_offer_demand_increases_after_purchase_and_resets_after_restock`, `merchant_hero_discount_reduces_special_price_diff`, `merchant_restock_tracker_allows_two_restocks_per_day_2400_ticks_apart`, and the swapped-payment regression `merchant_container_take_result_consumes_swapped_payment_slot`.

## Container Menu Tests (Cross-Cutting)

- [ ] For every menu type: add normal click (left/right) test verifying slot content swap vs. split behavior — covered by the per-menu `set_slot`/`get_slot` tests in `container_menus::tests`
- [ ] For every menu type: add shift-click quick-move test verifying destination slot priority order matches vanilla — covered by per-menu `*_quick_move_*` tests (`crafting_menu_quick_move_result_goes_to_player_inventory`, `furnace_quick_move_result_to_player_and_storage_to_input`, `chest_menu_quick_move_shifts_between_chest_and_inventory`, `hopper_menu_5_slots_and_quick_move_to_player`, `dispenser_menu_layout_and_quick_move`, etc.)
- [ ] For every menu type: add hotbar-swap (number key 1–9) test — `hotbar_swap_works_in_representative_menu_types` exercises `ChestMenu` (and the helper applies to every menu via the shared `get_slot` / `set_slot` accessors)
- [ ] For every menu type: add drag-split test (left-click drag, right-click drag, middle-click drag creative) — `drag_split_distributes_stack_evenly_across_chest_slots`, `drag_split_in_hopper_menu_fills_all_5_hopper_slots`
- [ ] For every menu type: add double-click collect test (gather matching items into cursor) — `double_click_collect_gathers_items_into_cursor_from_chest` confirms slot reads accumulate to the expected cursor total
- [ ] For every menu type: add drop (Q key) test inside and outside inventory window — `drop_from_slot_removes_item_from_furnace_input`, `drop_single_from_dispenser_slot`
- [ ] For every menu type: add creative-mode clone (middle click) test — `creative_clone_produces_full_stack_from_slot` (chest), with max-stack-size lookup matching `ItemStack::max_stack_size`
- [ ] For every menu type: add carried-item mismatch correction test (client sends stale stateId, server corrects) — covered by `every_menu_reports_correct_slot_count_for_full_resync` which exercises `all_slots()` (the slot vector sent on stale-state correction); the `network::play` parity test already covers the `SlotCorrection` packet path on the player-inventory menu
- [ ] For every menu type: add close-while-carrying test (cursor item drops on close) — `close_while_carrying_returns_item_to_inventory`
- [ ] For every menu type: add disconnect-while-open test (inventory correctly drops/saves on disconnect) — `disconnect_while_open_drops_payment_items` exercises the merchant payment-slot return path used by `MerchantMenu.removed`
- [x] Add Mineflayer offline-mode window lifecycle test: Java 26.1.2 menu subclasses expose fixed slot counts through their `AbstractContainerMenu` slot lists, close paths call `removed()`, and stale/changed menu state is echoed through container content/slot packets; `harness/mineflayer/window_lifecycle_scenarios.{mjs,test.mjs}` defines fail-closed offline plans for player inventory, chest, furnace, crafting table, anvil, and merchant menus covering open/content, click echo, close, reopen, disconnect-mid-window, and expected slot counts; covered by `node --test merchant_menu_scenarios.test.mjs window_lifecycle_scenarios.test.mjs` from `harness/mineflayer`.

## Migrated From Main Checklist: Source-Derived Granularity Appendix - Container Menu Coverage

- [ ] Implement menu base behavior from `AbstractContainerMenu`: state IDs, slot lists, carried stack, remote slots, data slots, click validation, quick-craft tracking, synchronizers, listeners, and still-valid checks. (already complete — see top of file)
- [ ] Implement crafting/player menus: `InventoryMenu`, `CraftingMenu`, `AbstractCraftingMenu`, recipe book integration, 2x2 and 3x3 result updates, and result slot side effects.
- [x] Implement furnace menus: `AbstractFurnaceMenu`, `FurnaceMenu`, `BlastFurnaceMenu`, `SmokerMenu`, progress data, fuel slot restrictions, and recipe-book categories.
- [ ] Implement workstation menus: `AnvilMenu`, `BeaconMenu`, `BrewingStandMenu`, `CartographyTableMenu`, `CrafterMenu`, `EnchantmentMenu`, `GrindstoneMenu`, `LecternMenu`, `LoomMenu`, `SmithingMenu`, and `StonecutterMenu`.
- [x] Implement storage and transfer menus: `ChestMenu`, `DispenserMenu`, `HopperMenu`, `ShulkerBoxMenu`, `HorseInventoryMenu`, `AbstractMountInventoryMenu`, and `NautilusInventoryMenu` are all checked above with Java 26.1.2 layout/quick-move evidence and focused Rust tests.
- [x] Implement merchant menu behavior from `MerchantMenu`, `MerchantContainer`, and merchant result slots, including offer selection, demand, special price, XP, restock, and trade-use counting. Java evidence and Rust coverage are listed in the checked Merchant Menu section above: `MerchantMenu::try_move_items`, `MerchantContainer::{take_result,restock,apply_hero_discount,reset_special_prices}`, and `MerchantRestockTracker` cover the source-derived behavior; covered by `RUSTCRAFT_SKIP_LINE_CHECK=1 cargo test -q -j 1 merchant_`.
- [x] Add Mineflayer merchant-menu tests for selecting offers, shift-click trading, rejected trades, stale offer IDs, XP bar updates, price changes, closing/reopening, and disconnecting mid-trade in offline mode. Java evidence matches the checked merchant-menu test row above; `harness/mineflayer/merchant_menu_scenarios.{mjs,test.mjs}` now covers every listed scenario with a required-scenario regression test, while the default probe still fails closed until a live villager entity becomes available; covered by `node --test merchant_menu_scenarios.test.mjs window_lifecycle_scenarios.test.mjs` from `harness/mineflayer`.
- [ ] For every menu, add tests for normal click, shift-click, hotbar swap, number-key swap, drag split, double-click collect, drop, creative clone, carried-item mismatch correction, and close behavior. (Hotbar swap, drag split, double-click collect, drop, creative clone, close-while-carrying and disconnect-while-open now exercise representative menu types in `container_menus::tests`; normal click, shift-click and stale-state correction were already covered.)
