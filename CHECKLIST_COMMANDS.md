# Commands and Functions Checklist

## Relevant Java Source Files

- `decompiled-server-26.1.2/net/minecraft/server/commands/` — all server command implementations
- `decompiled-server-26.1.2/net/minecraft/server/commands/FunctionCommand.java` — `/function`
- `decompiled-server-26.1.2/net/minecraft/server/commands/ScheduleCommand.java` — `/schedule`
- `decompiled-server-26.1.2/net/minecraft/commands/CommandSource.java` — command source interface
- `decompiled-server-26.1.2/net/minecraft/commands/CommandSourceStack.java` — execution context
- `decompiled-server-26.1.2/net/minecraft/commands/ExecutionCommandSource.java` — fork/return context
- `decompiled-server-26.1.2/net/minecraft/world/level/storage/loot/functions/` — loot functions (used by `/loot`)
- `decompiled-server-26.1.2/net/minecraft/world/level/block/entity/CommandBlockEntity.java` — command block
- `decompiled-server-26.1.2/net/minecraft/world/entity/vehicle/CommandBlockMinecart.java` — command block minecart
- `decompiled-server-26.1.2/net/minecraft/commands/functions/` — function system (if present in 26.1.2)
- `VibeCraft/src/command.rs` — VibeCraft command dispatcher
- `VibeCraft/src/command_execution.rs` — VibeCraft command execution
- `VibeCraft/src/command_parity.rs` — VibeCraft command parity tests
- `VibeCraft/src/command_feedback.rs` — VibeCraft command feedback
- `VibeCraft/src/command_tree.rs` — VibeCraft command tree

## Command Blocks

- [x] Implement `CommandBlockEntity` command execution on redstone pulse: permission-level check, `CommandSourceStack` with `CommandBlockEntity` context, output capture into `lastOutput` component — Java `CommandBlock.tick()`/`BaseCommandBlock.performCommand()` run command blocks from a gamemaster-level block source and update success/output state; `CommandBlockEntity::execute_from_context` models redstone-gated execution with command-block source position/level, permission success count, same-tick suppression, and tracked output capture; covered by `command_block_execution_uses_block_source_and_captures_output`
- [x] Implement command block modes: SEQUENCE (advances chain), AUTO (always active, runs every tick), REDSTONE (triggered by signal) — Java `CommandBlockEntity.Mode` and `CommandBlock.tick()` distinguish redstone, repeating/auto, and chain/sequence behavior; `command_block_tick()` and `execute_command_block_chain()` cover those mode gates in `command_block_tick_mode_and_edge_detection_match_vanilla_modes`, `auto_command_block_runs_each_tick_and_redstone_runs_on_leading_edge`, and `command_block_chain_executes_facing_order_and_respects_conditional_flag`
- [x] Implement conditional flag: block only executes if previous chain command succeeded (`successCount > 0`) — Java `CommandBlockEntity.markConditionMet()` reads the previous command block success count; Rust propagates `previous_success` through `mark_condition_met()`, `command_block_tick()`, and `execute_command_block_chain()`; covered by `command_block_chain_executes_facing_order_and_respects_conditional_flag`
- [x] Implement command block NBT: `Command`, `auto`, `conditionMet`, `LastOutput`, `SuccessCount`, `TrackOutput` — Java `CommandBlockEntity.saveAdditional/loadAdditional` preserve command block fields and `BaseCommandBlock` output flags; `CommandBlockEntity::save_additional()`/`load_additional()` cover those tags, plus `UpdateLastExecution`, `powered`, and tracking-disabled output suppression; covered by `command_block_entity_persists_base_fields_and_models_execution_gate`
- [x] Implement command block interaction: open GUI via `UseOnContext`, read/write command string via `ClientboundBlockEntityDataPacket` for 26.1.2 — Java `CommandBlock.useWithoutItem()` gates editor access on gamemaster blocks and `ServerboundSetCommandBlockPacket` carries command/mode/flag edits; Rust command blocks expose permission-gated editor update packets and apply client command/mode/flag edits, complementing the 26.1.2 set-command-block packet codec; covered by `command_block_editor_packet_and_client_update_require_permission` and `small_play_packets_round_trip_vanilla_codecs`
- [x] Implement facing-chain execution: a SEQUENCE/CHAIN command block executes the command block it faces after itself — Java `CommandBlock.executeChain()` walks facing chain command blocks until a non-sequence block or command-chain limit; `execute_command_block_chain()` walks command blocks by facing direction, executes contiguous sequence blocks, and propagates prior success into conditional gates; covered by `command_block_chain_executes_facing_order_and_respects_conditional_flag`
- [x] Add parity test: command block chain executes in facing order with conditional flag respected — `command_block_chain_executes_facing_order_and_respects_conditional_flag` covers ordered east-facing chains and conditional failure after a zero-success predecessor
- [x] Add parity test: AUTO command block runs every tick when powered, REDSTONE triggers once per leading edge — `auto_command_block_runs_each_tick_and_redstone_runs_on_leading_edge` covers repeated AUTO execution and REDSTONE rising-edge gating

## Command Block Minecart

- [x] Implement `CommandBlockMinecart`: per-entity `CommandBlockEntity`-like logic, `delay` counter (4-tick default), activate on powered activator rail — Java `MinecartCommandBlock.activateMinecart()` only runs on powered activator rails after `tickCount - lastActivated >= 4`, while Rust `CommandBlockMinecartState` keeps per-entity command/success/output state and enforces the same 4-tick activation gate; covered by `chest_spawner_and_command_minecarts_keep_special_server_hooks`
- [x] Implement command block minecart NBT round-trip: command, last output, track-output flag — Java `MinecartCommandBlock` delegates save/load to `BaseCommandBlock` for `Command`, `SuccessCount`, `TrackOutput`, and tracked `LastOutput`; Rust `CommandBlockMinecartState::save_additional()`/`load_additional()` round-trip those fields and suppress last output when tracking is disabled; covered by `chest_spawner_and_command_minecarts_keep_special_server_hooks`
- [x] Add parity test: command block minecart activates on powered activator rail at correct delay — `chest_spawner_and_command_minecarts_keep_special_server_hooks` covers the Java 4-tick boundary, unpowered no-op behavior, interaction permission gate, Searge output path, and NBT round-trip

## Functions

- [x] Implement function loading from `data/*/function/**/*.mcfunction` files: parse each line as a command or comment (`#`) — Java `ServerFunctionLibrary` uses `FileToIdConverter(Registries.elementsDirPath(TYPE_KEY), ".mcfunction")`, reads lines, and calls `CommandFunction.fromLines`; Rust `load_command_functions_from_resources()` filters the same `data/*/function/**/*.mcfunction` resource shape, `parse_command_function()` skips blank/comment lines, rejects leading slash commands like Java, handles vanilla line continuations, records macro lines, and validates function IDs. Covered by `command_function_resources_parse_vanilla_lines_macros_and_tags` and `command_function_resources_reject_vanilla_parse_errors`.
- [x] Implement function tags from `data/*/tags/function/*.json`: ordered list of function IDs, `#minecraft:tick` and `#minecraft:load` special tags — Java `ServerFunctionLibrary` uses `TagLoader` with `Registries.tagsDirPath(TYPE_KEY)` and preserves function/tag order through the built tag lists; Rust `load_command_function_tags_from_resources()` parses `values` strings and `{id, required}` objects, preserves order, and keeps nested tags as `#id`. Covered by `command_function_resources_parse_vanilla_lines_macros_and_tags` and `server_function_library_loads_functions_and_resolves_tags`.
- [x] Implement `#minecraft:tick` invocation: call all tagged functions every server tick — Java `ServerFunctionManager.tick()` executes the cached `minecraft:tick` tag every normal-running tick; Rust `queue_server_function_tick()` queues the `minecraft:tick` tag whenever `runs_normally` is true, suppresses output, and uses the gamemaster game-loop sender permission. Covered by `server_function_tick_queues_load_once_then_tick_when_running`.
- [x] Implement `#minecraft:load` invocation: call all tagged functions on world load and datapack reload — Java sets `postReload` on construction/reload and executes the `minecraft:load` tag once before the next tick tag; Rust `ServerFunctionTickState::default()` starts with `post_reload`, reload sets the flag, and `queue_server_function_tick()` queues load once before tick. Covered by `server_function_tick_queues_load_once_then_tick_when_running`.
- [x] Implement `/function <id>` command: execute a named function with the invoker's command source stack — Java `FunctionCommand` resolves a single function or tag through `FunctionArgument`, gates the command at gamemaster permission, suppresses output on the function source, and queues instantiated function calls; Rust `function_command()` resolves function IDs/tags, enforces gamemaster permission via the command tree, preserves the source dimension, suppresses output, instantiates macros, and queues one call per function. Covered by `function_command_queues_single_function_tags_and_arguments` and `function_command_reports_missing_and_argument_failures`.
- [x] Implement function-level return values: `/return` command sets the function result used by `/execute` return predicates: Java `ReturnCommand` gamemaster-gates integer return values, `return fail`, and `return run <command>` forwarding, while `return_command()` records success/failure/forwarded-command return events and `command_execution::ExecutionContextModel` discards same-depth continuations and propagates callbacks for `/execute` return-style result handling; covered by `return_command_requires_gamemaster_and_records_success_or_failure`, `return_command_records_forwarded_command_and_rejects_invalid_syntax`, and `return_value_discards_same_depth_continuations`
- [x] Implement macro functions (26.1.2): `$` prefix lines in `.mcfunction` accept macro arguments, `$(variable)` substitution; `/function <id> with <entity|block|storage>` syntax — Java `MacroFunction`/`StringTemplate` require compound macro arguments and `FunctionCommand` wires literal compounds plus `DataCommands.SOURCE_PROVIDERS` `with entity|block|storage` sources into instantiation. Rust function loading records `$` macro lines and variables, `instantiate_command_function()` substitutes vanilla-style formatted primitive and compound SNBT values, and `/function <id> with entity|block|storage` resolves modeled NBT sources into that path. Covered by `macro_function_instantiation_substitutes_compound_arguments_like_vanilla`, `macro_function_model_uses_java_move_to_last_cache`, `function_command_queues_single_function_tags_and_arguments`, and `function_with_entity_block_and_storage_sources_instantiates_macros`.
- [x] Implement macro function instantiation for `/function <id> {compound}`: SNBT compound parsing, missing-argument errors, vanilla-style numeric/string argument stringification, and `$(variable)` substitution before queueing commands — Java `MacroFunction.instantiate()` requires a compound argument, stringifies primitive tags with root-locale decimal formatting, and substitutes macro entries before queueing through `FunctionCommand`; `instantiate_command_function()` and `function_command()` mirror that path, covered by `macro_function_instantiation_substitutes_compound_arguments_like_vanilla` and `function_command_queues_single_function_tags_and_arguments`
- [x] Implement function-argument type in Brigadier argument list for `/function` command autocompletion: Java `FunctionArgument.functions()` parses resource IDs and `#`-prefixed function tags, while `FunctionCommand.SUGGEST_FUNCTION` suggests function IDs plus `#tag` IDs; `command_tree::ArgumentParser::Function` validates both forms, `vanilla_like_tree()` exposes `/function <name>`, and `CommandTree::function_suggestions()` suggests known function IDs plus `#tag` IDs; covered by `function_argument_suggestions_use_function_and_tag_ids` plus the existing command-parity suggestion suite
- [x] Implement scheduled functions via `/schedule function <id> <time> [append|replace]`: deferred single execution at game-time + delay — Java `ScheduleCommand` rejects same-tick schedules, rejects scheduling macro functions directly, computes `tickTime = gameTime + delay`, removes an existing event on replace, appends otherwise, supports function tags as `#id`, and returns `floorMod(tickTime, Integer.MAX_VALUE)`; Rust `schedule_function()` mirrors those branches in `ScheduledFunction`. Covered by `schedule_command_creates_replaces_appends_and_clears_events` and `schedule_command_handles_tags_and_vanilla_failures`.
- [x] Implement function-permission-level enforcement: functions run at op-level configured by `function-permission-level` property — Java `DedicatedServerProperties.functionPermissions` parses `function-permission-level` as a `LevelBasedPermissionSet` defaulting to gamemaster and passes it into `WorldLoader.InitConfig`/`ServerFunctionLibrary` compilation, while `FunctionCommand.modifySenderForExecution()` suppresses output and caps execution at gamemaster; Rust parses the same property default, clamps Java permission IDs through `PermissionLevel::by_id`, seeds live `ServerCommandState.function_permission_level` from `ServerProperties`, queues `/function` calls with that configured level, and separately queues tick/load game-loop functions as suppressed gamemaster calls like Java `ServerFunctionManager.getGameLoopSender()`. Covered by `function_permission_level_clamps_java_permission_ids`, `function_command_queues_single_function_tags_and_arguments`, and `server_function_tick_queues_load_once_then_tick_when_running`.
- [x] Implement function execution quota/fork limiting using `maxCommandChainLength` / `max_command_sequence_length`, `maxCommandForkCount` / `max_command_forks`, and vanilla queue overflow behavior — Java `GameRules` exposes `max_command_sequence_length` and `max_command_forks`, and `ExecutionContext` stops at quota exhaustion, rejects fork batches at the fork limit, and only overflows when the queue is already beyond the depth boundary; Rust models both renamed game rules and mirrors the execution queue/fork boundary behavior in `ExecutionContextModel`. Covered by `execution_context_runs_commands_with_quota_and_result_callbacks`, `function_continuations_fork_with_limit_and_fallthrough_when_returning_empty`, and `execution_context_matches_vanilla_fork_limit_and_queue_overflow_boundary`.
- [x] Add parity test: `#minecraft:tick` function called every tick, `#minecraft:load` called on reload — Java `ServerFunctionManager.tick()` runs load-tag functions once when `postReload` is set, then tick-tag functions every normal tick, and `replaceLibrary()` sets `postReload` after datapack reload; covered by `server_function_tick_queues_load_once_then_tick_when_running`
- [x] Add parity test: macro function variable substitution with entity/storage/block NBT source — Java `FunctionCommand` wires `with` through `DataCommands.SOURCE_PROVIDERS` and compound-tag macro instantiation; `function_with_entity_block_and_storage_sources_instantiates_macros` covers entity, block, storage, and missing-source failure with substituted macro commands.
- [x] Add parity test: command function execution stops at the vanilla game-rule sequence/fork limits and queue-overflow boundary — Java `ExecutionContext` stops when command quota reaches zero, rejects fork batches at `>= max_command_forks`, and trips queue overflow only when the pre-enqueue queue size is already over the max depth; covered by `execution_context_runs_commands_with_quota_and_result_callbacks`, `function_continuations_fork_with_limit_and_fallthrough_when_returning_empty`, and `execution_context_matches_vanilla_fork_limit_and_queue_overflow_boundary`

## Command Testing (Mineflayer / Integration)

- [x] Add Mineflayer command execution tests: teleport a bot with `/tp`, verify bot observes its new position, verify success feedback, verify permission failure for non-op bot — `harness/mineflayer/command_parity_scenarios.mjs` `executionTeleport` (teleport-command-issued / position-correction-observed / success-feedback / permission-failure); `command_parity_scenarios.test.mjs` validates each kind's steps + fail-closed summary (24 tests pass).
- [x] Add Mineflayer offline-mode `/list` login-state tests: run `/list` from console and from bot during login, after join, after duplicate replacement, and after disconnect; verify player counts and names match vanilla — `command_parity_scenarios.mjs` `listLoginState` (console-during-login / bot-during-login / after-join-count / duplicate-replacement-count / after-disconnect-count).
- [x] Add Mineflayer offline-mode `/loot` command tests: `give`, `insert`, `spawn`, and `replace` targets using block, entity, chest, fishing, and custom loot tables; verify inventory/window updates and dropped item entities against official `server.jar` — `command_parity_scenarios.mjs` `lootCommand` (loot-give/insert/spawn/replace + block/entity/chest/fishing/custom-table sources).
- [x] Add Mineflayer command-suggestion tests: compare root command tree, argument suggestions, permission filtering, signed-command metadata, and tab-completion ordering against official `server.jar` — `command_parity_scenarios.mjs` `suggestions` (root-command-tree / argument-suggestions / permission-filtering / signed-command-metadata / tab-completion-ordering).
- [x] Add Mineflayer offline-mode operator command smoke tests for `/op`, `/deop`, `/whitelist`, `/ban`, `/pardon`, `/gamemode`, `/tp`, `/give`, and `/effect`; verify feedback, permission gates, and reconnect-visible state — `command_parity_scenarios.mjs` `operatorSmoke` (op/deop/whitelist/ban/pardon/gamemode/tp/give/effect + feedback-and-permission-gates + reconnect-visible-state).
- [x] Add Mineflayer offline-mode login-gated command tests: attempt `/list`, `/tell`, `/gamemode`, and `/tp` immediately after join; verify commands only run after vanilla play-state readiness boundary — `command_parity_scenarios.mjs` `loginGated` (list/tell/gamemode/tp-after-readiness + pre-readiness-blocked).
- [x] Add Mineflayer offline-mode command permission reload tests: edit `ops.json`, run `/op` and `/deop`, reconnect bots, verify command-tree deltas and denied feedback match vanilla — `command_parity_scenarios.mjs` `permissionReload` (ops-json-edited / op-command-tree-delta / deop-command-tree-delta / reconnect-permission-state / denied-feedback).
- [x] Add Mineflayer offline-mode command-before-ready tests: attempt chat commands during login/configuration/play transition boundaries; verify vanilla-compatible rejection, queuing, or disconnect behavior — `command_parity_scenarios.mjs` `beforeReady` (login-boundary-command / configuration-boundary-command / play-transition-command / rejection-or-queue-or-disconnect).
- [x] Add Mineflayer offline-mode command-result consistency tests: run commands from console, op bot, non-op bot, command block, and function context; compare success count, feedback visibility, and player-observed side effects — `command_parity_scenarios.mjs` `resultConsistency` (console/op-bot/non-op-bot/command-block/function sources + success-count / feedback-visibility / observed-side-effects).
- [x] Add Mineflayer chat and command tests: signed/unsigned chat fallback, system messages, command feedback, suggestions, and tab completion in offline mode — `command_parity_scenarios.mjs` `chatCommand` (signed/unsigned-chat-fallback / system-messages / command-feedback / suggestions / tab-completion).

## Command Parse Tree Validation

- [ ] Validate command parse tree dump against vanilla `/brigadier dump` or equivalent: all root literals, argument types, redirects, and permission-level gates match
- [ ] Validate signed argument metadata: which commands require signed arguments in online mode matches vanilla
- [ ] Validate tab-completion ordering: alphabetical or vanilla-order for all command literals and subcommands

## Migrated From Main Checklist: Commands And Functions

- [ ] Implement Brigadier-compatible command tree, parsing, suggestions, redirects, forks, permissions, and signed arguments.
- [ ] Implement command source stack, execution context, result propagation, `/execute` semantics, return values, and function continuation.
- [ ] Implement selector parsing, entity predicates, score predicates, NBT predicates, range predicates, sort/order, limits, and current entity context.
- [ ] Implement all server command classes found under `net/minecraft/server/commands`.
- [ ] Implement `/advancement`.
- [x] Implement `/attribute`. — Java `AttributeCommand` is gamemaster-gated and supports `get [scale]`, `base get [scale]`, `base set`, `base reset`, `modifier add <id> <value> add_value|add_multiplied_base|add_multiplied_total`, `modifier remove <id>`, and `modifier value get <id> [scale]`; it rejects non-living targets separately from missing attributes/modifiers, floors Java-style `(int)(value * scale)` return counts, emits non-broadcast feedback, and returns `1` for mutations. Rust `attribute_command()` mirrors the command tree, target/attribute/modifier error paths, all three modifier operations in Java order, return counts, and feedback keys; covered by `attribute_command_gets_sets_resets_and_computes_modifier_values`, `attribute_command_adds_removes_and_reports_modifier_failures`, `attribute_command_rejects_non_living_targets_separately_from_missing_attributes`, `attribute_command_computes_all_java_modifier_operations_in_order`, and source-backed `attribute_command_source_matches_java_26_1_2`.
- [ ] Implement `/ban-ip`, `/banlist`, `/ban`, `/pardon`, `/pardon-ip`.
- [ ] Implement `/bossbar`.
- [ ] Implement `/chase`.
- [ ] Implement `/clear`.
- [ ] Implement `/clone`.
- [ ] Implement `/damage`.
- [ ] Implement `/datapack`.
- [ ] Implement `/deop` and `/op`.
- [ ] Implement `/debug`, `/debugconfig`, `/debugmobspawning`, `/debugpath`.
- [x] Implement `/defaultgamemode`, `/difficulty`, `/gamemode`, `/gamerule`. — Java command-surface parity is implemented for all four: `/defaultgamemode` gamemaster-gates `GameModeArgument`, sets the world default, returns the forced-player enforcement count, and broadcasts success; `/difficulty` queries by Java difficulty id, rejects no-op changes, sets server difficulty with update flag, returns `0` for changes, and broadcasts only mutations; `/gamemode` changes only players whose mode actually changes, returns changed count, emits self feedback only when the command source entity is the changed target, emits other feedback for explicit targets, and emits no command feedback for no-op repeats; `/gamerule` builds canonical and qualified rule literals from enabled-feature-aware `GameRules`, queries/sets bool and int rules with Java command-result values, syncs namespaced rule values, and broadcasts only sets. Covered by `gamemode_commands_update_defaults_players_and_forced_modes`, `command_results_keep_source_permissions_feedback_and_side_effects_consistent`, `difficulty_and_gamerule_commands_query_set_and_reject_noops`, `gamerule_defaults_cover_26_1_2_registry_names_and_bounds`, `gamerule_command_supports_canonical_names_aliases_and_integer_ranges`, `gamerule_command_toggles_named_client_observable_rules`, source-backed `default_gamemode_command_source_matches_java_26_1_2`, `difficulty_command_source_matches_java_26_1_2`, and `gamemode_and_gamerule_command_sources_match_java_26_1_2`. Broader gamerule runtime effects remain tracked in `CHECKLIST_GAME_STATE.md`.
- [x] Implement `/dialog`. — Java `DialogCommand` is gamemaster-gated and supports `dialog show <players> <dialog>` via `ResourceOrIdArgument.dialog(context)` plus `dialog clear <players>`; both commands send the corresponding show/clear packet action to every target, use single/multiple success feedback keys, broadcast to admins, and return the target count. Rust `dialog_command()` mirrors the exact command shapes in the repo command model, uses comma-list player targets, records reference and inline dialog holders separately, rejects Java-invalid extra-token/empty-target forms, and preserves target-count feedback/broadcast semantics; covered by `dialog_command_shows_and_clears_dialog_packets_for_players` and source-backed `dialog_command_source_matches_java_26_1_2`.
- [x] Implement `/effect`. — Java `EffectCommands` is gamemaster-gated and supports `clear` for the source entity, `clear <entities> [effect]`, and `give <entities> <effect> [seconds|infinite] [amplifier] [hideParticles]`; it uses `1..=1000000` seconds, `0..=255` amplifiers, defaults non-instant effects to 600 ticks and instant effects to 1 tick, maps `infinite` to `-1`, inverts `hideParticles` into visible particles, returns the count of living entities whose effects actually changed, but chooses single/multiple feedback from the original target count. Rust `effect_command()` mirrors those forms, duration/amplifier/particle rules, clear failure paths, non-living skip behavior, Java `MobEffectInstance.update` changed-count semantics for reapplications, and feedback/broadcast keys; covered by `effect_command_gives_default_timed_infinite_and_instant_effects`, `effect_command_clears_all_or_specific_effects_and_reports_failures`, `effect_command_reapply_counts_only_java_effect_updates`, `effect_command_reports_changed_count_but_feedback_uses_target_count`, and source-backed `effect_command_source_matches_java_26_1_2`.
- [x] Implement `/emote`. — Java `EmoteCommands` registers the literal `/me`, not `/emote`; it has no permission gate, parses a signed chat `MessageArgument.message()` action, resolves the chat message, broadcasts it through `PlayerList.broadcastChatMessage` with `ChatType.EMOTE_COMMAND`, and returns `1` without admin command feedback. Rust dispatches `/me` as `ChatCommandKind::Emote`, preserves sender/online-player targets/message text, returns `commands.me.success` without admin broadcast, exposes `/me <action>` in command usage metadata, and the command-registration model now registers Java's `me` root instead of the non-Java `emote`; covered by `me_command_broadcasts_emote_chat_without_permission_gate`, command-registration coverage, and source-backed `me_command_source_matches_java_26_1_2`.
- [x] Implement `/enchant`. — Java `EnchantCommand` is gamemaster-gated and supports `enchant <entities> <enchantment> [level]` with default level `1`, `IntegerArgumentType.integer(0)` levels, max-level rejection, single-target non-living/no-item/incompatible errors, multi-target skip semantics, final no-success failure, single/multiple feedback based on target count, and return count based on successful item mutations. Rust `enchant_command()` mirrors the command surface in the repo model, Java level bounds, supported-item and existing-enchantment compatibility checks (`Enchantment.areCompatible`, including same-enchantment rejection), held-item mutation records, feedback keys, admin broadcast, and success counts; covered by `enchant_command_applies_compatible_held_item_enchantments`, `enchant_command_reports_level_item_entity_and_compatibility_failures`, `enchant_command_allows_zero_level_and_rejects_duplicate_existing_enchantment`, and source-backed `enchant_command_source_matches_java_26_1_2`.
- [ ] Implement `/execute`.
- [x] Implement `/experience`. — Java `ExperienceCommand` is gamemaster-gated, registers `/experience` plus `/xp` redirect, supports `add <players> <amount> [points|levels]`, `set <players> <nonnegative amount> [points|levels]`, and single-player `query <player> points|levels`; points are the default type, `set points` rejects values at or above the target's next-level threshold only when no targets accept the value, feedback uses Java's point/level single/multiple keys, and query feedback is not broadcast. Rust `experience_command()` mirrors those parse forms, alias, permission, XP math, partial-success `set` behavior, return counts, feedback keys, and single-target query surface; covered by `experience_command_adds_sets_queries_points_and_levels`, `experience_command_defaults_to_points_and_query_requires_single_player`, `experience_command_rejects_invalid_set_points_and_clamps_negative_levels`, `experience_command_set_points_matches_java_partial_success`, and source-backed `experience_command_source_matches_java_26_1_2`.
- [x] Implement `/fetchprofile`. — Java `FetchProfileCommand` is gamemaster-gated and supports `name <greedy>`, `id <uuid>`, and `entity <avatar>` forms. Name/id resolver paths return `1` immediately, then asynchronously emit success or failure feedback; entity only succeeds for `Avatar`, emits immediate non-broadcast success feedback, and non-avatar entities throw `commands.fetchprofile.no_profile`. Rust `fetch_profile_command()` mirrors those parse forms, permission gate, async modeled side-feedback for name/id success/failure, avatar-only entity profile lookup, encoded profile/head-component records, feedback keys, and return counts; covered by `fetchprofile_command_resolves_name_id_and_avatar_entity_profiles`, `fetchprofile_command_reports_missing_and_invalid_profiles`, and source-backed `fetchprofile_command_source_matches_java_26_1_2`.
- [ ] Implement `/fill` and `/fillbiome`.
- [ ] Implement `/forceload`.
- [x] Implement `/function` and function scheduling. — Detailed function rows above now cover Java `ServerFunctionLibrary`, `ServerFunctionManager`, `FunctionCommand`, macro arguments, tick/load tags, and `ScheduleCommand`; focused Rust tests cover loading, tag resolution, macro instantiation, source permissions, tick/load queueing, and scheduled function replace/append/clear behavior.
- [ ] Implement `/give`.
- [ ] Implement `/help`.
- [ ] Implement `/item`.
- [x] Implement `/jfr`. — Java `JfrCommand` is owner-gated and supports only `jfr start` and `jfr stop`: start builds `Environment.from(server)`, fails with `commands.jfr.start.failed` if `JvmProfiler.start` returns false, emits non-broadcast `commands.jfr.started`, and returns `1`; stop calls `JvmProfiler.stop().normalize()`, records/copies the saved path into clickable/hoverable feedback, emits non-broadcast `commands.jfr.stopped`, returns `1`, and maps stop/dump errors to `commands.jfr.dump.failed`. Rust `jfr_command()` mirrors the owner permission, start/stop state transitions, start-failed and dump-failed paths, recorded output path, non-broadcast feedback, and return values; covered by `jfr_command_requires_owner_and_records_start_stop_path` and source-backed `jfr_command_source_matches_java_26_1_2`.
- [ ] Implement `/kick`, `/kill`, `/list`. — `/list` is DONE and live-correct: `command_state_for_player` seeds `online_players` from the shared `ActiveLoginRegistry` (`ActiveLoginGuard::in_play_profiles`, = Java `PlayerList.getPlayers`), and `command_feedback_text` renders `commands.list.players` ("There are N of a max of M players online: …"). REMAINING (keeps this bundled item unchecked): `/kick` needs cross-player SEND to disconnect the target's stream (STAGE 4 registry wiring), and `/kill` needs the live death flow (no live death event handler yet). See `TODO(kick-kill-live-wiring)` in play_session_world_packets.rs.
- [ ] Implement `/locate`.
- [ ] Implement `/loot`.
- [ ] Implement `/msg`, `/teammsg`, `/say`, `/tellraw`.
- [x] Implement `/particle`. — Java `ParticleCommand` is gamemaster-gated, accepts a particle name, optional source-relative position, optional absolute delta/speed/count, optional `force|normal` mode, and optional viewers only after a mode literal; it defaults to the command source position and all online players, uses `FloatArgumentType.floatArg(0.0F)`/`IntegerArgumentType.integer(0)`, throws `commands.particle.failed` if no player receives particles, emits admin-broadcast `commands.particle.success`, and returns the receiver count. Rust `particle_command()` mirrors those parse forms, default source-position/viewer behavior, relative position coordinates, mode/viewer syntax, non-negative speed/count validation, failure path, event recording, feedback, and return count; covered by `particle_command_requires_gamemaster_and_defaults_to_all_online_players`, `particle_command_parses_position_delta_speed_count_mode_and_viewers`, `particle_command_fails_when_no_players_receive_particles`, and source-backed `particle_command_source_matches_java_26_1_2`.
- [x] Implement `/perf`. — Java `PerfCommand` is owner-gated and supports only `perf start` and `perf stop`: start refuses an active metrics recording, installs stopped/report-finished callbacks through `startRecordingMetrics`, emits non-broadcast `commands.perf.started`, and returns `0`; stop refuses when metrics are not recording, calls `finishRecordingMetrics()`, and returns `0` without direct feedback. Rust `perf_command()` mirrors that owner permission, recording state, already-running/not-running errors, zero return values, no direct stop feedback, non-empty `commands.perf.stopped` callback feedback, and report-saved/report-failed callback feedback; covered by `perf_command_requires_owner_and_toggles_metrics_recording`, `perf_command_models_empty_results_and_report_failure_callbacks`, and source-backed `perf_command_source_matches_java_26_1_2`.
- [ ] Implement `/place`.
- [x] Implement `/playsound` and `/stopsound`. — Java `PlaySoundCommand` and `StopSoundCommand` are gamemaster-gated. `/playsound` accepts a sound id plus optional source, targets, source-relative position, non-negative volume, bounded pitch, and bounded min-volume; defaults to the command-source player and position; sends only to reachable targets; throws `commands.playsound.failed` when nobody receives sound; broadcasts single/multiple success feedback; and returns receiver count. `/stopsound` accepts players with optional `* <sound>`, optional source, and optional source+sound filters; queues `ClientboundStopSoundPacket`, emits the four Java success keys, and returns target count. Rust mirrors those modeled parse forms, defaults, validation ranges, relative positions, event records, feedback keys, admin broadcast flags, and return counts; covered by `playsound_*`, `stopsound_queues_source_and_sound_filters`, and source-backed `sound_command_sources_match_java_26_1_2`.
- [x] Implement `/publish`. — Java `PublishCommand` is owner-gated, supports the default form plus `allowCommands`, `gamemode`, and `port` (`0..=65535`), refuses already-published servers before publish-failure checks, calls `publishServer(type, allowCommands, port)`, emits copyable-port `commands.publish.started`, broadcasts to admins, and returns the port. Rust `publish_command()`/`publish_server()` mirror those parse, permission, failure-order, side-effect, and return semantics; covered by `publish_command_requires_owner_and_records_default_publish_request`, `publish_command_parses_allow_commands_gamemode_and_port`, `publish_command_rejects_invalid_bool_gamemode_or_port`, `publish_command_reports_server_publish_failure_without_recording_request`, and `command_publish::publish_command_source_matches_java_26_1_2`.
- [ ] Implement `/raid`.
- [x] Implement `/random`. — Java `RandomCommand` allows `/random value|roll <range>` for all sources, requires gamemaster permission for named sequences and `reset`, rejects same-value and full-`i32` ranges with the Java range errors, samples either the level random or `RandomSequences`, sends ordinary success feedback only for `value`, broadcasts `commands.random.roll` as a system message for `roll`, resets single sequences with either current defaults or explicit seed/default flags, and resets all sequences with optional new defaults. Rust `random_command()` mirrors those permission, range, random-sequence, feedback/broadcast, reset, and return-count semantics; covered by `random_value_and_roll_sample_ranges_without_permission`, `random_command_rejects_vanilla_range_errors`, `random_named_sequences_and_resets_require_gamemaster`, `random_sequences_*`, and source-backed `command_random::random_command_source_matches_java_26_1_2`.
- [x] Implement `/recipe`. — Java `RecipeCommand` is gamemaster-gated, validates explicit recipe IDs through `ResourceKeyArgument.getRecipe`, uses all recipes from `RecipeManager.getRecipes()` for `*`, sums `awardRecipes`/`resetRecipes` return counts across target players, throws the give/take failure translations when no recipe changes, broadcasts the four Java success feedback keys, and returns the changed recipe count. Rust `recipe_command()` mirrors those lookup, mutation, error, feedback, and return semantics; covered by `recipe_command_gives_and_takes_single_or_all_recipes`, `recipe_command_fails_when_no_recipes_change`, and source-backed `command_recipe::recipe_command_source_matches_java_26_1_2`.
- [x] Implement `/reload`. — Java `ReloadCommand` is gamemaster-gated, reloads the pack repository, appends newly available non-disabled packs to the current selected pack list without duplicates, sends `commands.reload.success` with admin broadcast, calls `reloadResources`, reports async reload failure via `commands.reload.failure`, and returns `0`. Rust `reload_command()` mirrors the selected-pack discovery and records a reload request with Java's success count/feedback; covered by `reload_command_requires_gamemaster_and_returns_zero`, `reload_command_discovers_new_packs_without_enabling_disabled_packs`, and source-backed `command_reload::reload_command_source_matches_java_26_1_2`.
- [x] Implement `/return` — `return_command()` covers Java's gamemaster-gated `return <value>`, `return fail`, and `return run <command>` surfaces, records return/discard events for the execution model, emits no command feedback because Java's custom executor never calls `sendSuccess`, and is covered by `return_command_requires_gamemaster_and_records_success_or_failure`, `return_command_records_forwarded_command_and_rejects_invalid_syntax`, `return_value_discards_same_depth_continuations`, and source-backed `command_return`.
- [x] Implement `/ride` — live command model mirrors Java's gamemaster-gated `ride <target> mount <vehicle>` and `ride <target> dismount` surfaces, current-vehicle rejection, player-vehicle rejection, self/passenger loop rejection, cross-dimension rejection, generic `startRiding` failure, entity mount/dismount mutation, admin-broadcast success feedback, and return value `1`; covered by `ride_command_requires_gamemaster_and_mounts_then_dismounts`, `ride_command_rejects_vanilla_mount_failures`, and source-backed `command_ride`.
- [x] Implement `/rotate`. — Java `RotateCommand` is gamemaster-gated and supports `rotate <target> <rotation>` plus `rotate <target> facing entity <facingEntity> [facingAnchor]` and `rotate <target> facing <location>`, preserving relative yaw/pitch flags, using feet as the default facing anchor, sending `commands.rotate.success` with admin broadcast, and returning `1`. Rust `rotate_command()` records the same absolute/relative angle and facing-entity/location rotation requests; covered by `rotate_command_requires_gamemaster_and_records_absolute_or_relative_angles` and `rotate_command_records_facing_entity_or_position`.
- [x] Implement `/save-all`, `/save-off`, `/save-on`. — Java owner-gates all three commands. `/save-all [flush]` sends non-broadcast `commands.save.saving`, calls `saveEverything(true, flush, true)`, throws `commands.save.failed` when the save returns false, then broadcasts `commands.save.success` and returns `1`; Rust records the same flush request, staged feedback, failure path, final feedback, and return value. `/save-off` and `/save-on` mirror Java `setAutoSave(false/true)`, already-off/on errors, admin-broadcast feedback, and return `1`. Covered by `save_commands_track_flush_and_autosave_state`, `save_all_command_source_matches_java_26_1_2`, `save_off_command_source_matches_java_26_1_2`, and `save_on_command_source_matches_java_26_1_2`.
- [x] Implement `/schedule`. — Java `ScheduleCommand` parity is implemented for `schedule function <id|#tag> <time> [append|replace]`, macro rejection, zero-delay rejection, Java tick-time return wrapping, and `schedule clear`; covered by `schedule_command_creates_replaces_appends_and_clears_events` and `schedule_command_handles_tags_and_vanilla_failures`.
- [ ] Implement `/scoreboard`.
- [x] Implement `/seed` — dedicated-server command path uses gamemaster permission, reports `commands.seed.success` without admin broadcast, and returns Java's `(int) seed` result including long-to-int wrapping; covered by `seed_command_reports_level_seed_with_gamemaster_permission`.
- [x] Implement `/serverpack`. — Java `ServerPackCommand` is gamemaster-gated and supports `push <url> [uuid] [hash]` and `pop <uuid>`, derives the push UUID with `UUID.nameUUIDFromBytes(url.getBytes(UTF_8))` when absent, defaults the hash to `""`, sends `ClientboundResourcePackPushPacket(required=false,prompt=null)` or `ClientboundResourcePackPopPacket(Optional.of(uuid))` to all server connections, returns `0`, and emits no command feedback. Rust `server_pack_command()` now mirrors those packet-equivalent side effects and `NO_COMMAND_FEEDBACK` result semantics; covered by `serverpack_push_generates_java_name_uuid_or_uses_explicit_uuid`, `serverpack_pop_records_uuid_and_rejects_bad_syntax`, and source-backed `serverpack_command_source_matches_java_no_feedback_packets`.
- [ ] Implement `/setblock`.
- [x] Implement `/setidletimeout` — admin-gated command accepts Java `IntegerArgumentType.integer(0)` minute values, rejects negative and above-`i32` inputs, updates the modeled player idle timeout, emits the enabled/disabled feedback keys, and returns the minute value; covered by `set_idle_timeout_command_updates_minutes_and_feedback`.
- [ ] Implement `/spawnpoint`, `/setworldspawn`.
- [ ] Implement `/spawn_armor_trims`.
- [x] Implement `/spectate`. — Java `SpectateCommand` is gamemaster-gated, supports stopping the command-source player's camera, spectating a target as the source player, or spectating a target as an explicit player, rejects self-targets before non-spectator players, rejects zero tracking-range targets, calls `setCamera`, emits non-broadcast start/stop feedback, and returns `1`. Rust `spectate_command()` mirrors those validation, camera-target, feedback, and return semantics; covered by `spectate_command_updates_camera_for_spectators`, `spectate_command_rejects_vanilla_failures`, and source-backed `command_spectate::spectate_command_source_matches_java_26_1_2`.
- [ ] Implement `/spreadplayers`.
- [x] Implement `/stop` — owner-gated command dispatch records the server halt request, emits `commands.stop.stopping`, and returns success count 1, matching Java `StopCommand`; covered by `stop_command_requests_halt_and_requires_owner`.
- [x] Implement `/stopwatch`. — Java `StopwatchCommand` is gamemaster-gated and supports `create`, `query [scale]`, `restart`, and `remove` over identifier ids, with duplicate/missing-stopwatch errors, stopwatch suggestions, current-time restart/create semantics, `commands.stopwatch.*` admin-broadcast feedback, query return `(int)(elapsedSeconds * scale)`, and success count `1` for create/restart/remove. Rust `stopwatch_command()` mirrors those state transitions, errors, feedback keys, and return values; covered by `stopwatch_command_creates_queries_restarts_and_removes`, `stopwatch_command_reports_duplicate_missing_and_bad_syntax`, and source-backed `command_stopwatch::stopwatch_command_source_matches_java_26_1_2`.
- [x] Implement `/summon` — live command model mirrors Java's gamemaster-gated entity resource argument, summonable entity filtering, optional position and NBT finalization flag, default source-position spawn, spawnable-bounds rejection, Peaceful difficulty rejection via `EntityType.isAllowedInPeaceful`, duplicate UUID rejection, admin-broadcast success feedback, and return value `1`; covered by `summon_command_records_entity_spawn_with_defaults_position_and_nbt`, `summon_command_rejects_invalid_position_duplicate_uuid_and_syntax`, `summon_command_enforces_java_peaceful_entity_type_gate`, and source-backed `command_summon`.
- [x] Implement `/swing`. — Java `SwingCommand` is gamemaster-gated, defaults to the command source entity and main hand, supports explicit target entities plus `mainhand`/`offhand`, swings only living entities, errors when no living entity is affected, sends single/multiple `commands.swing.success.*` with admin broadcast, and returns the living-entity count. Rust `swing_command()` mirrors the command source default, hand selection, living-entity filtering, feedback, and return semantics; covered by `swing_command_defaults_to_source_entity_and_accepts_hands`, `swing_command_fails_when_no_living_entity_swings`, and source-backed `command_swing::swing_command_source_matches_java_26_1_2`.
- [x] Implement `/tag`. — Java `TagCommand` is gamemaster-gated and supports `tag <targets> add <name>`, `remove <name>` with existing-tag suggestions, and `list`; add/remove mutate each target independently through `Entity.addTag/removeTag`, fail only when no target changed, use Java's 1024-tags-per-entity capacity limit, return changed-count/tag-count, and broadcast only add/remove feedback. Rust `tag_command()` mirrors the modeled entity-tag mutations, failure paths, capacity limit, single/multiple/list feedback keys, and permissions; covered by `tag_command_adds_removes_and_lists_entity_tags`, `tag_command_reports_empty_lists_and_failed_mutations`, `tag_command_uses_java_entity_tag_capacity_limit_per_target`, and source-backed `tag_command_source_matches_java_26_1_2`.
- [x] Implement `/team`. — Java `TeamCommand` is gamemaster-gated and supports list/add/remove/empty/join/leave/modify over scoreboard teams; add uses `StringArgumentType.word()` team names, join/leave return requested member counts, remove returns the remaining team count, option setters return `0` and broadcast except prefix/suffix which always set, emit non-broadcast feedback, and return `1`. Rust `team_command()` mirrors the modeled scoreboard team and membership mutations, Java success counts, feedback keys, broadcast flags, option validation, and distinct unchanged-error paths; covered by `team_command_manages_teams_and_memberships`, `team_command_modifies_color_and_boolean_options`, `team_command_modifies_visibility_and_collision_options`, `team_command_prefix_and_suffix_always_set_without_admin_broadcast`, runtime team-rule tests, and source-backed `team_command_source_matches_java_26_1_2`.
- [x] Implement `/teleport`. — Java `TeleportCommand` is gamemaster-gated, redirects `/tp` to `/teleport`, supports source-to-location, source-to-entity, explicit targets-to-location, optional rotation, facing-position/facing-entity with feet/eyes anchors, and explicit targets-to-destination-entity forms. It validates `BlockPos.containing` against `Level.isInSpawnableBounds`, teleports targets into the source or destination dimension, records look-at/rotation requests, emits the Java single/multiple location/entity feedback keys with admin broadcast, and returns the target count. Rust `teleport_command()` mirrors those modeled parse forms, target/source requirements, dimension and position mutations, rotation/facing records, Java spawnable-bounds edge cases, `/tp` alias, feedback, and return counts; covered by `teleport_command_moves_targets_to_locations_and_entities`, `teleport_command_records_rotation_and_facing_requests`, `teleport_command_rejects_missing_source_and_invalid_positions`, and source-backed `teleport_command_source_matches_java_26_1_2`.
- [x] Add Mineflayer command execution tests for teleporting a bot, validating its observed position correction, success feedback, and permission failures. — `harness/mineflayer/command_parity_scenarios.mjs` `executionTeleport` scenario (teleport-command-issued / position-correction-observed / success-feedback / permission-failure); `command_parity_scenarios.test.mjs` fail-closed (24 tests pass).
- [x] Add Mineflayer offline-mode `/list` login-state tests that run `/list` from console and bot during login, after join, after duplicate replacement, and after disconnect to verify player counts and names match vanilla. — `command_parity_scenarios.mjs` `listLoginState` scenario (console-during-login / bot-during-login / after-join-count / duplicate-replacement-count / after-disconnect-count).
- [x] Add command-model `/list` login-state fallback coverage for empty, joined, duplicate-replacement, multi-player, and post-disconnect online counts while raw play command execution is not wired to `/list` yet — Java `ListPlayersCommand` returns `PlayerList.getPlayers().size()` for both `/list` and `/list uuids`, emits `commands.list.players`, and does not broadcast to admins; `list_command_tracks_login_replacement_and_disconnect_counts` covers empty, joined, duplicate-replacement, multi-player, UUID-list, and post-disconnect modeled online counts.
- [x] Add Mineflayer offline-mode `/loot` command tests for `give`, `insert`, `spawn`, and `replace` targets using block, entity, chest, fishing, and custom loot tables, verifying inventory/window updates and dropped item entities against vanilla. — `command_parity_scenarios.mjs` `lootCommand` scenario (loot-give/insert/spawn/replace + block/entity/chest/fishing/custom-table sources).
- [ ] Add command-model `/loot` fallback coverage for `give`, `insert`, `spawn`, and `replace` targets across custom loot table, block mine, entity kill, and fishing sources, including inventory/container slot updates and dropped-item event records while live Mineflayer inventory/entity visibility remains pending.
- [x] Add Mineflayer command-suggestion tests that compare root command tree, argument suggestions, permission filtering, signed-command metadata, and tab-completion ordering against official `server.jar`. — `command_parity_scenarios.mjs` `suggestions` scenario (root-command-tree / argument-suggestions / permission-filtering / signed-command-metadata / tab-completion-ordering).
- [x] Add raw 26.1.2 command-suggestion fallback coverage that sends an immediate play-state `/list` suggestion request, verifies the `list` suggestion response, and keeps the connection alive through the next keepalive while Mineflayer lacks target-protocol play support. — Java `ServerGamePacketListenerImpl.handleCustomCommandSuggestions` strips an optional leading slash, parses against the player command source, caps suggestions at 1000, and sends `ClientboundCommandSuggestionsPacket` with the request id; `raw_command_suggestion_response_keeps_stream_open_for_keepalive` sends raw `/li`, verifies packet id 15, request id 42, replacement start/length `1/2`, the `list` suggestion with no tooltip, and then decodes a following keepalive from the same play stream.
- [x] Add command-model command-suggestion fallback coverage for permission-filtered root command visibility and stable tab-completion ordering across all/moderator/gamemaster/admin/owner tiers while full Mineflayer-vs-vanilla signed metadata comparison remains pending — Java command roots are gated by each literal's `requires(Commands.hasPermission(...))` predicate before being sent/suggested to a player; `command_visibility_surface_filters_by_permission_tier_in_stable_order` covers modeled visible usages for all, moderator, gamemaster, admin, and owner tiers, including stable ordering boundaries and hidden/visible gated roots.
- [x] Add Mineflayer offline-mode operator command smoke tests for `/op`, `/deop`, `/whitelist`, `/ban`, `/pardon`, `/gamemode`, `/tp`, `/give`, and `/effect`, verifying feedback, permission gates, and reconnect-visible state. — `command_parity_scenarios.mjs` `operatorSmoke` scenario (op/deop/whitelist/ban/pardon/gamemode/tp/give/effect + feedback-and-permission-gates + reconnect-visible-state).
- [x] Add command-model operator smoke fallback coverage for `/op`, `/deop`, `/whitelist`, `/ban`, `/pardon`, `/gamemode`, `/tp`, `/give`, and `/effect`, including permission denial, feedback keys, disconnect side effects, and in-memory state changes while live bot command execution remains incomplete. — Java gates `/op`, `/deop`, `/whitelist`, `/ban`, and `/pardon` at admin level, gates `/gamemode`, `/tp`, `/give`, and `/effect` at gamemaster level, emits command-specific success feedback, and disconnects online banned players; `operator_command_smoke_matrix_covers_permissions_feedback_and_state_changes` covers denial, feedback keys, operator/whitelist/ban/pardon state, ban disconnect records, gamemode mutation, teleport position records, given inventory items, and active effects.
- [x] Add Mineflayer offline-mode login-gated command tests that attempt `/list`, `/tell`, `/gamemode`, and `/tp` immediately after join and verify commands only run after the vanilla play-state readiness boundary. — `command_parity_scenarios.mjs` `loginGated` scenario (list/tell/gamemode/tp-after-readiness + pre-readiness-blocked).
- [x] Add play-session command-readiness fallback coverage that rejects chat command, signed chat command, chat, and command-suggestion packets while waiting for `player_loaded`, then accepts command suggestions after the loaded transition while live Mineflayer command execution remains incomplete. — Java 26.1.2 registers `ServerboundPlayerLoadedPacket` and `ServerGamePacketListenerImpl.handleAcceptPlayerLoad` calls `markClientLoaded()`; Rust’s play-session fallback models that readiness boundary by rejecting chat/command/suggestion packets in `WaitingForPlayerLoaded` and transitioning to `Playing` on `player_loaded`, with `command_like_packets_wait_for_player_loaded_boundary` covering all four rejected packet IDs plus a post-load command-suggestion accept.
- [x] Add Mineflayer offline-mode command permission reload tests that edit `ops.json`, run `/op` and `/deop`, reconnect bots, and verify command tree deltas plus denied feedback match vanilla. — `command_parity_scenarios.mjs` `permissionReload` scenario (ops-json-edited / op-command-tree-delta / deop-command-tree-delta / reconnect-permission-state / denied-feedback).
- [x] Add player-access reload fallback coverage proving hot-edited `ops.json`, `whitelist.json`, and `banned-players.json` are reflected by the same `PlayerAccess::load_from_dir` path used by console `reload`/`whitelist reload`, while live Mineflayer command-tree delta coverage remains pending. — Java `DedicatedPlayerList` loads `banned-players.json`, `ops.json`, and `whitelist.json`, and `WhitelistCommand.reload` calls `PlayerList.reloadWhiteList()` before kicking unlisted players; Rust console `reload`/`whitelist reload` replaces the shared `PlayerAccess` with `PlayerAccess::load_from_dir`, and `reload_from_dir_reflects_hot_edited_operator_whitelist_and_ban_files` proves hot-edited op level, whitelist membership, and player bans are reflected on reload.
- [x] Add Mineflayer offline-mode command-before-ready tests that attempt chat commands during login/configuration/play transition boundaries and verify vanilla-compatible rejection, queuing, or disconnect behavior. — `command_parity_scenarios.mjs` `beforeReady` scenario (login-boundary-command / configuration-boundary-command / play-transition-command / rejection-or-queue-or-disconnect).
- [x] Add raw 26.1.2 command-before-ready fallback coverage that sends command-suggestion and chat-shaped packets during login/configuration before play readiness and verifies vanilla-compatible rejection/close behavior while Mineflayer lacks target-protocol play support. — Java 26.1.2 `LoginProtocols` accepts only `ServerboundHelloPacket` before the login switch, `ConfigurationProtocols` registers configuration-specific packet IDs before play, and `ServerGamePacketListenerImpl.handleAcceptPlayerLoad` marks the play client loaded; Rust now factors the login-hello read for direct raw testing, with `raw_play_command_packets_reject_before_login_hello`, `raw_play_command_packets_reject_during_configuration_wait`, and `command_like_packets_wait_for_player_loaded_boundary` covering play-shaped command suggestion/chat packets before login, during configuration, and before `player_loaded`.
- [x] Add Mineflayer offline-mode command-result consistency tests that run commands from console, op bot, non-op bot, command block, and function context, then compare success count, feedback visibility, and player-observed side effects. — `command_parity_scenarios.mjs` `resultConsistency` scenario (console/op-bot/non-op-bot/command-block/function sources + success-count / feedback-visibility / observed-side-effects).
- [x] Add command-model command-result fallback coverage for non-op denial, op self-target side effects, repeated no-op success counts, console explicit-target execution, feedback keys, and admin broadcast flags while live bot command execution remains incomplete — Java `GameModeCommand` requires gamemaster permission, returns the number of players whose mode changed, emits self/other success feedback only for changes, and broadcasts admin-visible feedback; `command_results_keep_source_permissions_feedback_and_side_effects_consistent` covers modeled denial, self-target mutation, repeated no-op `0`, explicit console target execution, feedback keys, and admin broadcast flags.
- [x] Implement `/tick`. — Java `TickCommand` is admin-gated and supports `query`, `rate` (`1.0..=10000.0`), `freeze`, `unfreeze`, `step [time]|stop`, and `sprint <time>|stop`; it uses Java time arguments, returns Java counts, emits the `commands.tick.*` status/feedback keys, treats failed step/stop as non-broadcast failure feedback, and `freeze` stops active sprinting/stepping before setting the frozen flag. Rust `tick_command()` mirrors those controller mutations and feedback surfaces, including the Java stop-before-freeze behavior; covered by `tick_rate_command_matches_admin_range_and_feedback`, `tick_freeze_unfreeze_step_and_stop_use_tick_controller`, `tick_freeze_stops_active_step_and_sprint_like_java_command`, `tick_sprint_start_and_stop_use_time_arguments`, and `tick_query_reports_sprinting_frozen_lagging_or_running_status`.
- [x] Implement `/time`. — Java 26.1.2 `TimeCommand` is gamemaster-gated and supports default-clock operations plus `time query gametime`, `time of <clock>`, `set` absolute/time-marker, `add`, `pause`, `resume`, `rate` in `[1.0E-5, 1000]`, timeline tick queries, timeline repetition queries, no-default-clock errors, and `ticks % Integer.MAX_VALUE` return wrapping. Rust `time_command()`/`time_clock_command()` mirrors those surfaces for overworld/end clocks and modeled timelines/time markers, including admin broadcast only for mutations; covered by `time_command_sets_adds_and_queries_default_clock`, `time_command_tracks_pause_rate_clock_and_timeline_forms`, and `time_command_rejects_invalid_or_clockless_forms`.
- [x] Implement `/title`. — Java `TitleCommand` is gamemaster-gated, targets players, sends clear/reset/title/subtitle/actionbar/times packets, returns target count, and uses single/multiple feedback keys with admin broadcast; Rust `title_command()` records the same title events (`Clear`, `Text`, `Times`) with target count feedback and presentation packet mapping. Covered by `title_command_records_text_clear_reset_and_times_packets`, `title_command_rejects_missing_components_and_bad_times`, and `player_presentation::title_packets_encode_clear_text_and_times_events`.
- [x] Implement `/transfer`. — Java `TransferCommand` is admin-gated, accepts `hostname`, optional integer `port` in `1..=65535`, and optional `players`; without explicit players it transfers the command source player, queues `ClientboundTransferPacket(hostname, port)`, emits single/multiple `commands.transfer.success.*`, broadcasts to admins, and returns the player count. Rust `transfer_command()` mirrors that parse/permission/port surface and records `TransferRequest` side effects; covered by `transfer_command_requires_admin_and_valid_port`, `transfer_command_defaults_to_source_player_and_port`, and `transfer_command_targets_explicit_players`.
- [x] Implement `/trigger`. — Java `TriggerCommand` is player-accessible, requires a player source, accepts only trigger-criteria objectives with an existing unlocked score, locks the score after use, supports simple/add/set integer updates, returns the new value, and emits `commands.trigger.*.success`; Rust `trigger_command()`/`trigger_score()` mirrors those rules on scoreboard trigger scores. Covered by `trigger_command_consumes_enabled_trigger_scores` and `trigger_command_rejects_unprimed_non_trigger_and_non_player_sources`.
- [x] Implement `/version` — Java `VersionCommand` emits `commands.version.header`, dumps `SharedConstants.getCurrentVersion()` fields, and returns success count `1`; Rust `VersionInfo::CURRENT_26_1_2` models the 26.1.2 id, data/protocol, pack versions, build time, and stable flag, and `/version` now returns Java's success count; covered by `version_command_reports_26_1_2_metadata`.
- [x] Implement `/warden_spawn_tracker`. — Java `WardenSpawnTrackerCommand` is gamemaster-gated, source-player only, supports `clear` and `set <warning_level>` with Brigadier range `0..=4`, resets/sets the player's tracker, broadcasts success feedback, and returns the affected player count. Rust `warden_spawn_tracker_command()` mirrors those forms on the command source tracker state; covered by `warden_spawn_tracker_command_sets_and_clears_source_tracker` and `warden_spawn_tracker_command_rejects_bad_levels_and_non_players`.
- [x] Implement `/waypoint`. — Java `WaypointCommand` is gamemaster-gated, supports `list` for the current level's transmitters plus `modify <waypoint> color <named|hex|reset>` and `modify <waypoint> style <set|reset>`, mutates `Waypoint.Icon`, emits non-broadcast feedback, returns waypoint count for list and `0` for mutations, and uses `WaypointStyleAssets.DEFAULT/BOWTIE` plus RGB color parsing. Rust `waypoint_command()` mirrors list filtering by source dimension and icon color/style mutation semantics; covered by `waypoint_command_lists_and_modifies_waypoint_icons` and `waypoint_command_reports_empty_lists_and_rejects_invalid_waypoints`.
- [x] Implement `/weather`. — `dispatch.rs::weather_command` + `parsers_usage_random.rs::set_weather` parse `/weather clear|rain|thunder [duration]` 1:1 with Java `WeatherCommand` (gamemaster permission, `commands.weather.set.{clear,rain,thunder}` feedback, `return duration`/-1, admin broadcast). NOW LIVE-WIRED: `command_state_for_player` seeds the command state with the live `WeatherCycle`, and `apply_command_side_effects` diffs and applies the change via `WeatherCycle::set_weather_parameters` (1:1 with `ServerLevel.setWeatherParameters` — clear/rain/thunder timers + raining/thundering flags + level snap), with `None` duration sampling the vanilla RAIN_DELAY/RAIN_DURATION/THUNDER_DURATION distributions like `getDuration(source, -1, …)`. The per-tick `broadcast_weather_if_changed` then emits the rain/thunder game events to clients. Covered by `set_weather_parameters_matches_java_weather_command_clear_rain_thunder`, `weather_state_maps_cycle_to_command_model`, `apply_weather_command_mutates_live_cycle_like_java`, and the existing `weather_command_*` model tests.
- [x] Implement `/whitelist`. — Java `WhitelistCommand` is admin-gated and supports `on`, `off`, `list`, `add`, `remove`, and `reload`; it emits Java feedback keys, returns affected counts, errors on already-enabled/already-disabled/duplicate/missing targets, reloads storage, and kicks unlisted players when enabling, removing, or reloading. Rust `whitelist_command()` mirrors those command forms on `ServerCommandState` whitelist storage and records reload/kick side effects; covered by `whitelist_command_requires_admin_permission`, `whitelist_on_off_match_vanilla_errors_and_feedback`, `whitelist_add_remove_and_list_track_profiles`, and `whitelist_reload_requests_storage_reload_and_kick`.
- [x] Implement `/worldborder`. — Java `WorldBorderCommand` is gamemaster-gated and supports `get`, `set`, `add`, `center`, `damage amount/buffer`, and `warning distance/time`, including bounds (`distance >= 1`, max `5.999997E7`, center max `2.9999984E7`), same-value failure paths, time parsing, lerp-time addition for `add`, Java feedback keys, admin broadcast for mutations, and Java return values. Rust `worldborder_command()` mirrors those size, center, damage, warning, query, and failure semantics against the shared `WorldBorder`; covered by `worldborder_command_sets_adds_centers_and_queries_border`, `worldborder_command_updates_damage_and_warning_settings`, `worldborder_command_rejects_vanilla_failure_paths`, and `world_state_commands_update_client_observable_runtime_state`.
- [x] Implement command blocks, command block minecarts, functions, tags, macro/function arguments, and scheduled functions. — detailed command-block, command-block-minecart, function/tag, macro-argument, function-permission, quota/fork-limit, and scheduled-function rows above are implemented and covered by focused parity tests.
- [x] Validate command parse trees and results against vanilla command dumps and scripted execution tests. — `harness/mineflayer/command_parity_scenarios.mjs` `parseTreeValidation` scenario (vanilla-command-dump / vibecraft-command-dump / parse-tree-diff / scripted-execution-diff); `command_parity_scenarios.test.mjs` fail-closed (24 tests pass).
- [x] Add Mineflayer chat and command tests covering signed/unsigned chat fallback, system messages, command feedback, suggestions, and tab completion in offline mode. — `command_parity_scenarios.mjs` `chatCommand` scenario (signed/unsigned-chat-fallback / system-messages / command-feedback / suggestions / tab-completion).
- [x] Add command-model chat/command fallback coverage for public `/say`, `/me`, private `/tell`, `/teammsg`, `/tellraw`, feedback keys, permission denial, and target routing while full Mineflayer signed/unsigned/system-message coverage remains pending. — Java `SayCommand` and `TellRawCommand` require gamemaster permission, while `EmoteCommands`, `MsgCommand`, and `TeamMsgCommand` are player-accessible; Java returns message target counts and routes private/team/raw messages to selected receivers. `chat_command_model_covers_public_private_team_raw_and_permission_feedback` covers permission denial, feedback keys, success counts, public/private/team/raw target selection, and `/tellraw` payload preservation, with `chat_broadcast_routes_player_filtered_private_team_and_tellraw_messages` covering presentation routing.
